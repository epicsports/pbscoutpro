node_modules/
dist/
.env*
!.env.example
*.log
.DS_Store

# Firebase Admin SDK service account credentials (NEVER commit)
firebase-admin-*.json
service-account*.json

# Ephemeral discovery outputs (HANDOVER: not committed) + per-machine Claude settings
outputs/
.claude/settings.local.json

# Playwright
tests/results/
tests/report/
tests/results-*/
tests/report-e2e/
tests/screenshots/
test-results/
playwright-report/
scripts/reviewers/screenshots/
scripts/reviewers/*-report.md
.claude/worktrees/
scripts/diag/*.local.json

# Player-dedup migration backups (contain catalog PII — keep local, like *.local.json)
scripts/migration/player_dedup_backup.*.json

# Night-run intake (local coordination scratch; shipped work logged in DEPLOY_LOG)
NIGHT_QUEUE.md

# Handoff inbox (Jacek/CD drops: zrzuty, zipy, drafty briefów) — not part of code.
# Keep the dir + its README tracked; ignore everything dropped into it.
_inbox/*
!_inbox/README.md

# Night-mode scratch (game references; not shipped)
.night/

# design-sync (claude.ai/design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Local export/diagnostic artifacts (generated locally; never part of a feature commit)
data-export/
design-review/
scripts/diag/catalog-csv-export.cjs
TeamLogos/
scraper/
scripts/diag/find-missing-i18n.cjs
