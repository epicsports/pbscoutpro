# STATE — authoritative open-items index

> **Source of truth for what's OPEN.** Repo-verified (2026-06-04 reconciliation), not memory.
> Stop re-deriving open work from chat/memory — it mis-flags shipped items as pending.
> One line per item: status + ref. Detail lives in `NEXT_TASKS.md` (sub-boards) + `DEPLOY_LOG.md` (ships) +
> `docs/DESIGN_DECISIONS.md` (§ rationale). Keep this terse; update on every doc-closeout.

**Last reconciled:** 2026-06-08 · main HEAD `fffa8594` (**Catalog SWR + single-flight — training "data disappears" fix** `fffa8594` §111: training participants/lineups blanked mid-use [IDs via onSnapshot OK, but people resolve via version-gated catalog; old `useGatedCatalog` awaited a 3,242-doc refetch with `docs=[]` on version mismatch → `playersById={}` → `.map(pid=>playersById[pid]).filter(Boolean)` collapsed squads/attendees to empty; `cc76f9ad` amplified via ~8 routine bumps]. Fix in shared `useGatedCatalog`: serve non-empty stale IDB payload immediately [`revalidating`] + background refetch single-flighted per `kind:version` [`_catalogInflight`] → never blank; cold start keeps `loading`; cc76f9ad edit-propagation preserved. **Owed: Jacek repro-smoke.** **Deferred [own brief]:** cc76f9ad global-version bump on routine single-doc edits → full refetch per bump per client [bounded, not reduced]. Prior: **KIOSK lobby viewport — decouple W/H + honest fallback** `aefbcb6c` §55.11: lobby didn't launch even on laptops [`kioskViewport` rejected `h<768`; 1366×768 usable ~640]. Part 1 — §27 risk is WIDTH-only [5-tile grid width-driven; short viewport scrolls, doesn't shrink tiles] → `MIN_WIDTH=1024` kept, `MIN_HEIGHT` 768→600 → laptops + iPad-landscape reach the lobby. Part 2 — dropped the invalid §35-HotSheet fallback [CC STEP-0 catch: `selfLog` flag off + single-player own-phone FAB ≠ in-overlay hand-around]: `KioskPostSaveSummary` entry-gates "Przekaż graczom" via `canEverFitKioskLobby` [longer physical screen edge <1024 = phones → only "Następny punkt"]; `KioskLobbyOverlay` routes via `useKioskMode` [lobby / rotate=portrait-tablet-would-fit / message=`KioskRotatePrompt variant=needsDevice`] — no futile "rotate" ever. **Owed: Jacek laptop smoke.** Prior: **KIOSK after the QuickLog winner-pick** `528c0401` §55.10 corrected: the prior MatchPage approach `3549a957` was the WRONG screen [shared/tournament sheet] — reverted; training flow is QuickLogView, where `TrainingScoutTab` now `enterPostSave({force})` so the post-save summary [Przekaż graczom / Następny punkt = OPEN KIOSK / NEXT POINT] shows after the winner pick; KioskPostSaveSummary portrait-responsive; §27 floor stays on the 5-tile lobby [rotate to landscape]. Prior: **KIOSK from the training scout editor** `3549a957` §55.10: MatchPage training now shows OPEN KIOSK / NEXT POINT after the winner pick [savePoint returns the point id; was wired only into QuickLog]; `enterPostSave(force)` + `KioskRotatePrompt` let the coach enter from portrait + rotate to landscape; §27 floor preserved [5-tile lobby still ≥1024]. Prior: **PPT point-logging fixes** `792279e1`: (1) "Nowy punkt" stays in the current training — `setActiveTraining` on wizard entry, not only picker-pick [`213636bb`]; (2) delete a self-log point from TodaysLogsList via §7 ⋮→ActionSheet→ConfirmModal [`168c51f3`, §110] — W5 selfReport, unpropagated-only bare deleteDoc; **OPEN/escalated:** propagated-selfReport delete [un-merge vs block] + unlinked /pendingSelfReports/ delete. Prior: **A/B brief COMPLETE** — **Part B: precision shot in self-log** `7a3a437a` §109.4: self-log shot step gains a precision tap [reuses scouting `ShotDrawer`] alongside zone+bunker; tri-shape state.shots; propagator routes precision `{x,y}` → `pt.shots[slot]` → Step 1 precision [real tap, not centroid → align concern N/A]; kill=self-stat. **Part A: breakout-destination dots** `679f98dd` §109.3 [A/B brief Part A]: PlayerStatsPage heatmap also shows where the player ran on the break [position markers, scouting styling, outcome-colored] alongside the outgoing-zone choropleth; reuses HeatmapCanvas position layer [showPositions + selectedPlayerId + phase='breakout'], no new marker code. Follow-up: extract shared marker module [drawPlayers + HeatmapCanvas]. **Part B (precision self-log shot) queued.** Prior: **#3 zone-shot capture CLOSED** — STAGE 2 `c6e7e6a9` §109.2: new PlayerStatsPage heatmap section renders the player's OWN outgoing zone-shots as a per-player choropleth [fill ∝ frequency], kill-zones emphasized [stronger fill + red outline via new `HeatmapCanvas.calloutZoneKills`]; reuses HeatmapCanvas [points=[]→choropleth-only]; OUTGOING-only legend. **#3 = STAGE 0+1+2 DONE.** Open: B3 INCOMING + fieldSide-swap edge. Prior: **self-log zone-shots aligned onto path∩polygon attribution** `0461fd87`: propagator also writes self-logged zoneIds into `point.zoneShots[slot]` [same field as scouting] → flow through W1 Step 1.5; centroid doc kept for break-shot side stat, no double-count; training points only; e2e 21/21. Prior: **zone attribution corrected to path∩polygon** `8b60667e`, §30: W1 containment was wrong [firing zones lie ON the path, not at the obstacle] → credit a tagged zone if the eliminated player's path [calibration base on their side → elim position] crosses it; `segmentIntersectsPolygon`/`zoneMembership` helpers; `opponentEliminationPositions`+`opponentSide` wired; raw-space only; e2e 21/21. Open follow-ups: self-log align onto path∩zone + fieldSide-swap edge. Prior: **zone-shot capture #3 — largely shipped** [`{zoneId,kill}` binary]: STAGE 0 schema+dual-read `bb7aed97` §109 · STAGE 1 self-log field-tap drawer `d301410d` · attribution W1 callout-zone 3rd kill-credit source `0e71e2d9` §30 · attribution W2 shared `ZoneTapField` + scouting full-field drawer `5a79156b` §109.1. Open: STAGE 2 heatmap render [awaiting smoke+GO] + B3 INCOMING gap. Prior: self-log live visibility `d3660fa6` §108: orphan W5 `/selfReports/` folded into breakout stats, dedup key `propagatedAt`, write-model untouched. PPT cross-type picker `42aeeead` · AnalyticsCanvas extraction `d61fa157`; e2e 21/21. Prior: §90 hotfix — repoint 4 write-path twin accessors to global, restores self-leave/remove/team-create. PaT Stage 3 — `timeline[]` carried through the two-side merge; B24 mojibake import-guard + 16-doc `--live` repair; **§90 Stage 2B/3 FULLY CLOSED** — 2,938 twin/layout docs `--live` deleted; ALL twin rule blocks removed; player delete now super_admin-only + hard global (`d60f8390`). Earlier same day: claim flow 1b · bunker-naming re-key · B4 · Read-volume C Stage 2).

---

## 🔴 In-flight (branch exists / partially shipped)
- _(none)_

_(✅ recently cleared: `fix/scouttab-hooks-310` React #310 cold-launch crash `93ece048`; read-volume B server-side self-log filter `cebcbdf3` — both 2026-06-04.)_

## 🔴 Open (actionable, needs brief/GO)
- **Read-volume C (rollup lever):** **Stage 1 COMPLETE** (1.1 rollup `21019436` · 1.4 hybrid-read `bec3a038` · 1.2 layout-aggregate `5b9ee781`). **Stage 2 COMPLETE & DEPLOYED** (`73aba833`, 2026-06-05 — rules ruleset `d5242ec7`): raw `selfReports`/`shots` CGs scoped → `isMember(resource.data.workspaceSlug)` + shots `playerLinkedUid` self-carve-out; query filters make each predicate query-provable; `/layoutAggregates` write rule live (authed shared-write, 2.4). Emulator isolation matrix green (8 cells); full e2e 20/20. **GATE met (before FIT selfLog go-live).** Closes §90 1.2/1.3 + §94 #3 (selfReports CG leak). **NEXT (optional): Stage 3** IndexedDB cache. **Edge note:** rollup = match-end snapshot; post-merge point edits aren't reflected until re-merge (low-risk; flag if post-end editing becomes a workflow). **Owed: Jacek smoke** (when selfLog enabled — shot-freq/breakout-history parity + cross-tenant denial).
- **§90 A1** — catalog isolation **cutover**. **Stage 1.1 SHIPPED** (`7fa7e780`). **Stage 1.2/1.3 FOLDED into read-volume C — ✅ DONE** (`73aba833`). **Stage 2A ✅ SHIPPED** (`ec5a008c`). **✅ Stage 2B + 3 EXECUTED + CLOSED 2026-06-05** (`840b2fbc` code + `d9a020df` rules + `d60f8390` players-delete; Jacek CONFIRM): twin-writes dropped (`mirrorTwin` + 4 callsites + retire/unretire/**setParentTeam** direct writes) + app deployed → **`--live` deleted 2,634 player + 299 team twins + 5 `/layouts` stragglers = 2,938 docs** (drift +0, 0 remaining; local PII JSON backups at `…/dk/pbscoutpro_backups_90_2b3/2026-06-05T13-55-25-110Z/`) → removed **teams + layouts** twin rule blocks (global catalog `:614`/`:674`/`:699` + relocated breakoutVariants + selfReports + layoutOverlays intact). **✅ Stage 2B.3 CLOSED (`d60f8390`):** player delete is now **super_admin-only + hard global** (`deletePlayerGlobal`; PlayersPage gated via `useIsSuperAdmin`, repointed off the twin; confirm copy = permanent/global) per Jacek decision (b)+"deleted = deleted from DB". Last players-twin writer gone → **players twin rule block dropped + deployed** (global delete rule unchanged, `isSuperAdmin()`-only; §85 carve-outs served by the global block). **§90 catalog-isolation cutover DONE. (2 workspaces: pbfit + ranger1996.)** **Hotfix `cca4b586` (2026-06-05):** the rule-block removals broke 4 write-path twin accessors the 2A read-audit missed — `removeMember` (self-leave/remove BROKEN), `addTeam` (team-create BROKEN), `migrateWorkspaceRoles` (degraded), `mergePlayers` (graceful) → all repointed to global; e2e 21/21. ~~**Dead-code follow-up:** `deleteTeam` + layout CRUD~~ ✅ **DONE 2026-06-05** (`fece6b36`) — removed; **ZERO src refs to any `/workspaces/{slug}/{teams,layouts,players}` twin path remain → §90 code side fully retired.**
- **Events/Sparing — model RESOLVED (Model C `events_index`, §69; Model B superseded — DESIGN_DECISIONS §63.2).** NOT a Model A/B/C decision. **B3 Phase 1b (cold-review self-log, W4) ✅ SHIPPED** (`b1f6e7d4`). **PPT cross-type picker (Option A) ✅ SHIPPED 2026-06-06** (`42aeeead`): player panel surfaces assignment-scoped points across training+sparing+tournament (`fetchAssignedPointsForPlayer` shared core; LIVE trainings→wizard, assigned-unlogged→ColdReviewFlow; `EventTypeBadge`; show-all rejected, opposing-team/wizard D2 dropped). **Open remainder:** (a) **B3 Phase 2** — claim points the player was in but NOT assigned to (needs a participation signal — orthogonal to Events); (b) logged-too "C-direction" (deferred); (c) `useEvents()` stays dormant.
- **Bugs:** ~~B4 (home/landing)~~ ✅ **partial-fix shipped 2026-06-05 (`0c4852a2`)** — Settings is never a cold-open landing (scoped, role-independent); **DEFERRED remainder** = role-aware dashboard / "get started" home + `NoTournamentEmptyState` copy/CTA tuning (FIT-cold-start UX, revisit with real usage + clearer role). · B9 (training squad-matchup roster backfill, awaiting-decision) · B23 (F5/F6/F7, needs-validation) · ~~B24 (player-name mojibake)~~ ✅ **DONE 2026-06-05** (`717be17a` — import guard `repairMojibake` + `--live` repair of 16 ranger1996 catalog names, 0 remaining; FIT unaffected; team-name check = low-pri follow-up) · B26 (repair button — fix shipped `8076f3a6`, smoke owed).
- ~~**Zone-shot capture (#3)**~~ ✅ **CLOSED 2026-06-07** (STAGE 0+1+2 + W1/W2 attribution all shipped). model = **`{zoneId, kill}`** (binary kill; count/hits rejected by Jacek). **Shipped 2026-06-06/07:** STAGE 0 schema+dual-read (`bb7aed97`, §109) · STAGE 1 self-log field-tap drawer (`d301410d`) · attribution W1 callout-zone as 3rd kill-credit source (`0e71e2d9`) **CORRECTED to path∩polygon** (`8b60667e`, §30: path crosses zone, not elim-inside-zone; was containment) · attribution W2 shared `ZoneTapField` + scouting full-field drawer replacing the pill scroller (`5a79156b`, §109.1). ~~STAGE 2~~ ✅ DONE (`c6e7e6a9`, §109.2: PlayerStatsPage outgoing-zone choropleth, kills emphasized) · ~~self-log align onto path∩zone~~ ✅ DONE `0461fd87`. **#3 FULLY CLOSED.** **Remaining (separate, open):** **B3 INCOMING** ("hits taken on break") still net-new/not modeled (only `eliminationPositions`→`deathBunkerCounts`) · path-intersection **fieldSide-swap** start-base edge. OUTGOING ≠ INCOMING. **Owed: Jacek smoke** (drawers · zone-tag→credit · player outgoing-zone heatmap).
- **GDPR** data-subject-rights — BLOCKED-ON-LEGAL (Q1–Q2; do (c) privacy/consent first). `docs/architecture/GDPR_DATA_MAP.md`.
- **Ghost cleanup** — **effectively DONE / dropped from the §90 window (2026-06-05).** Fresh `--dry` found **5 unreferenced accounts, not ~566** — B15 (`cleanup_b15_orphans_full`) + the prior stale-account cleanup already cleared the anonymous auto-join ghosts (Auth now totals 23). The 5 residuals are Jacek's own test/alt accounts (`testowy@`/`testowyfit2@test.com`, `jacek2@`/`info@epicsports.pl`, `jacek.parczewski@cloudity.com`) — NOT bug-ghosts; **left untouched pending per-account say-so.** Backup: `E_ghost_accounts.json` in the §90 2B/3 backup dir.
- ~~**collectionGroup `selfReports` membership-scoping** (§94 #3)~~ — ✅ DONE (Read-volume C Stage 2, `73aba833` 2026-06-05): CG read scoped to `isMember(workspaceSlug)`; cross-tenant leak closed (emulator-proven).
- **Spark headroom ladder #2/#3/#4** — league-scoped catalog load · match-listener scoping · version-read caching (trigger ~N=40–50). `docs/architecture/COST_PROJECTION_SPARK.md`.
- **Point-as-Timeline Stages 4–8** — ~~3 multi-scout `timeline[]` carry~~ ✅ **DONE 2026-06-05** (`1fe4fe56` — `mergeStreamTimelines` per-side union at end-match merge; reframe = union-not-consensus; index-pairing/backfill held out per brief) · **4 typed move-seq — design LOCKED (Model A: per-slot `moveTypes[i]` keyframe attr ∥ `eliminationReasons`; capture = extend `ReasonRadial`; consumer = coach per-stage breakdown), build PARKED pending 2 gates (vocab → FIT; bump-typing model B → Jacek). See `POINT_AS_TIMELINE.md` Stage 4.** · 5 time axis · 6 full scrubber (6-lite shipped) · 7 self-log/kiosk+events · 8 analytics. `docs/POINT_AS_TIMELINE.md`. (Stage 2.5 per-stage reader confirmed already shipped.)
- **§64 Canvas** — ~~AnalyticsCanvas extraction~~ ✅ **DONE 2026-06-06** (`d61fa157` — LayoutAnalyticsPage → BaseCanvas child; last bespoke `<canvas>`/×2-DPR gone, only FieldCanvas/Ballistics ×2 remains). Open: BallisticsPage migration (Opus territory); + [FUTURE] Tactic→DrawingOverlay.
- **§98 follow-ups** — tactics two-store consolidation · drop legacy `dangerZone/sajgonZone/bigMoveZone` dual-write + base `discoLine/zeekerLine` (**still DEFERRED — §98 prod-smoke owed → not prod-confirmed**; was a §90-window ride-along, deferred per the brief's conditional gate). · ~~drop legacy workspace `/layouts` collection (§96)~~ ✅ **DONE 2026-06-05** (§90 Stage 3 — 5 stragglers deleted + rule block removed, `d9a020df`).
- **Account mgmt** — (B) real deletion + (C) guarded admin-SDK `deleteUser` script [ESCALATE: run C now vs GDPR build].
- **Tier D security** — custom claims · per-pid selfReports ownership · `/users` global read · adminUid create-time validation (post-MAX defer).
- **Misc:** More e2e UATs (#4/#5/#6) · Loupe pan-lag perf · PPT picker→`useEvents` (decision owed) · D1 BreakAnalyzer module (spec-only) · D3 schedule-import Pass 0 robustness · C3 TacticPage stage-adoption (scoped-out of §103).

## ⏸ Blocked / awaiting Jacek decision
- **LayoutDetailPage UX pass** (design discussion) · **Switcher UI brief** · **B17** `type:'practice'` keep-or-drop · **`ARCHITECTURE_C4_v2` + §38** (needs desktop+connector).

## ❓ NOT FOUND in repo — confirm or drop (memory artifacts)
- **Color configurator (B4-arch / `useColorPreferences`)** — no such code/task in repo. Confirm it's a real want or drop.
- **"State-doc audit"** — superseded by THIS `STATE.md`. Confirm that was the intent.

## ✅ Recently shipped
- **2026-06-06 (color picker):** HSV color picker (`8f8eaa49`) — any team brand color (SV box + hue slider + hex); optimistic drag + commit-on-release. Preset swatches then removed (`19b9776b`) — picker is the sole control + "↺ Reset to auto color". Owed: smoke.
- **2026-06-06 (canvas):** AnalyticsCanvas extraction (`d61fa157`) — LayoutAnalyticsPage's bespoke `<canvas>` → BaseCanvas child (`AnalyticsCanvas` + `drawAnalyticsField`); ×2-DPR/ResizeObserver/getBoundingClientRect gone; e2e 21/21. Owed: visual smoke.
- **2026-06-05 (team color fix):** team brand-color edits now apply (`fdd6fd13`) — `updateTeam` bumps catalog version (was invisible behind the 30d cache) + optimistic instant feedback; `TEAM_COLORS` palette 12→30. ~~Latent: `updatePlayer` same no-version-bump bug~~ ✅ **FIXED 2026-06-06** (catalog-version bump coverage — `{bump}` opt on all 8 catalog mutations; CSV bumps once; invariant documented).
- **2026-06-05 (PaT Stage 3):** `timeline[]` carried through the two-side end-match merge (`1fe4fe56`) — `mergeStreamTimelines` per-side union (home-scout + away-scout keyframes); Stage 2.5 reader confirmed already shipped; e2e extended (CI-gated). Owed: 2-device smoke.
- **2026-06-05 (B24 mojibake):** import guard + `--live` repair (`717be17a`) — `repairMojibake` util wired into CSVImport (player+nickname); 16 ranger1996 catalog names repaired (0 remaining, backup); FIT unaffected. Low-pri: team-name check.
- **2026-06-05 (§90 2B/3 — FULLY CLOSED):** twin + `/layouts` decommission (`840b2fbc`+`d9a020df`) — 2,938 `--live` docs deleted (2,634 player + 299 team twins + 5 layout stragglers; local PII backups), twin-writes dropped + deployed, teams+layouts twin rule blocks removed. **Then `d60f8390`:** player delete → super_admin-only + hard global (`deletePlayerGlobal`), last twin writer gone → players twin rule block dropped. **§90 catalog-isolation cutover DONE.** Ghost cleanup dropped (5 not 566; Jacek's own test accounts); §98 zone-field drop deferred (smoke owed); dead-code sweep (`deleteTeam`/layout CRUD) low-pri open.
- **2026-06-05:** claim flow 1b cold-review self-log W4 (`b1f6e7d4` — pick assigned scouted point → self-log; events_index+rollup picker A′; quiet at N=0) · layout bunker-naming re-key (`5c578ba6` — display-names keyed by `masterId||id`, fixes two-same-named-obstacles-rename-collision on "NXL EUROPE #2 - UK" Dykta; lazy overlay migration, no global write) · B4 Settings-never-landing (`0c4852a2`) · Read-volume C Stage 2 tenant-isolation CG scoping (`73aba833`, rules `d5242ec7`). **Owed Jacek (manual):** base-rename UK layout `XtQQKhVIegdTylygsbVX` bunker [0] "Dykta"→"Palma" (safe anytime; recoverable via overlay legacy `bunkerNames["b_…ix3m"]="Palma"`) + naming smoke.

See `DEPLOY_LOG.md` (newest first). 2026-06-03/04 block: search/filter A–D (§104) · global-first CRUD (§105) · admin-parity 1–3 (§106) · team branding Phase 1–2 (§107) · SW precache trim · read-volume A · productization doc (DEFERRED) · §94 FIT magic-link access (verified, `afc37f17`) · adminUid repoint (B22) · §98 layout-config build.
