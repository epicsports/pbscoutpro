# Expected-diff register (H0 evidence mechanism)

> Jacek ruling 2026-06-12 (evening): mechanical batches must separate **proven-identical**
> commits from **accepted-visible-change** commits, and every accepted visible change gets a
> row HERE before it ships. Without this register, "proven identity" and "accepted change"
> become indistinguishable and the evidence mechanism loses its teeth. One row per shipped
> visible delta; newest first.

| Date | Commit(s) | Surface | Expected visible diff | Why accepted | Status |
|---|---|---|---|---|---|
| 2026-06-13 | `48ac131d` (detail batch 1) | TeamDetailPage `/team/:id`, ProfilePage `/profile` | **DESKTOP (≥1024): content column caps to 640** (was R.layout 1200 / no-cap); PHONE+TABLET unchanged (migration diff-gate 4/4) | model-C migration — desktop-only cap per Jacek; phone+tablet proven identical | shipped pending smoke |
| 2026-06-13 | `8b4ab8e8` (arc-B model C) | `<Screen>` consumers — TODAY just the 3 pilot pages (ScoutDetail `/scouts/:uid`, ScoutIssues `/my-issues`, ScoutRanking `/scouts`); the ~18 migrated screens add their own rows per batch | **TABLET (≥640,<1024): the 3 pilot pages un-narrow from 640 → full tablet width (~768)** — they were hardcoded-640 at all widths, model C removes the sub-desktop cap. **DESKTOP (≥1024): unchanged** (detail still 640). **PHONE: unchanged** (pilot phone diff-gate 3/3). | Jacek's model-C decision: tablet keeps its natural width (zero narrowing); the cap applies only on desktop. The pilot pages were the only `<Screen>` users and were inadvertently narrowing tablet to 640 — model C corrects that toward the standard. | shipped pending smoke |
| 2026-06-13 | (this commit) (B13) | BunkerCard, CSVImport, StageSwitcher, PlayerEditModal, ScheduleImport, SearchField, ShotDrawer, CoachTabContent, ScoutTabContent, ui.jsx (ConfirmModal), ViewAsIndicator, VisionScan, AdminLayoutsPage, AdminLeaguesPage, AdminPlayersPage, AdminTeamsPage, ChildrenOrphanWarning, TeamDuplicateResolutionView, LayoutDetailPage, LoginPage, MatchPage, PlayersPage, PlayerStatsPage, ScoutedTeamPage, ScoutRankingPage, TacticPage, TeamDetailPage | **PL users see POLISH** where hardcoded ENGLISH rendered before: "Clear search" → "Wyczyść wyszukiwanie"; "Loading player..." → "Ładowanie zawodnika..."; "Loading scouted points..." → "Ładowanie zascoutowanych punktów..."; "Custom name..." → "Własna nazwa..."; "Set primary" → "Ustaw jako główną"; "e.g. 25" → "np. 25"; "Profile number" → "Numer profilu"; "Notes about player..." → "Notatki o zawodniku..."; "Review a point" → "Przejrzyj punkt"; "Delete shot" → "Usuń strzał"; "🔍 Search team…" → "🔍 Szukaj drużyny…"; "No teams yet" → "Brak drużyn"; "🔍 Search by team…" → "🔍 Szukaj po drużynie…"; "Exit impersonation" → "Wyjdź z podglądu"; "Scanning field..." → "Skanowanie pola..."; "Field library" → "Biblioteka pól"; "No base layouts yet — author one below" → "Brak bazowych layoutów — dodaj pierwszy poniżej"; "Leagues admin" → "Admin: ligi"; "Players admin" → "Admin: zawodnicy"; "Sort: updated ↓" → "Sortuj: zaktualizowane ↓"; "Sort: workspace" → "Sortuj: workspace"; "No players" → "Brak zawodników"; "Teams admin" → "Admin: drużyny"; "No teams" → "Brak drużyn"; "How to handle the children?" → "Co zrobić z podrzędnymi?"; "Children:" → "Podrzędne:"; "Player refs:" → "Ref. graczy:"; "Tournament refs:" → "Ref. turniejów:"; "Re-point children's parentTeamId..." → "Przepnij parentTeamId podrzędnych..."; "Draw line/zone" → "Narysuj linię/strefę"; "Delete line/zone" → "Usuń linię/strefę"; "e.g. Kacper" → "np. Kacper"; "Couldn't load this team" → "Nie można załadować tej drużyny"; "Full name *" → "Imię i nazwisko *"; "Nickname" → "Nick"; "Quick shots:" → "Szybkie strzały:"; "Add new player:" → "Dodaj nowego zawodnika:"; "Add a match or import schedule" → "Dodaj mecz lub zaimportuj harmonogram"; "Delete player" → "Usuń zawodnika"; "Default league:" → "Domyślna liga:"; "Capture stage" → "Faza zapisu". "auto ✅" unchanged (emoji status — identical in both langs). **EN rendering unchanged.** 5 intentional non-extractions: "Rysuj"/"Rysuj plan coacha" (Polish aria-labels misidentified), "Division 4→D4" (domain normalization example), 2×"Edits dual-write" (admin developer-doc text). | i18n B13 residual-EN batch — 42 keys extracted, 47 call sites fixed; EN guard down from 69 → 5 warnings; extraction commit `04039b74`, this commit carries PL translations | shipped pending smoke |
| 2026-06-13 | (this commit) (B8) | ScheduleImport (OCR import modal), ScheduleCSVImport (CSV import modal), CSVImport (player/team CSV modal) | **EN→PL (PL users):** ScheduleImport modal title "📋 Import schedule from image" → "📋 Import harmonogramu ze zdjęcia"; "Anthropic API key (saved in browser)" → "Klucz Anthropic API (zapisany w przeglądarce)"; "✅ API key set" → "✅ Klucz API ustawiony"; "Change" → "Zmień"; "Claude is reading the schedule..." → "Claude wczytuje harmonogram..."; "This may take a few seconds" → "To może chwilę potrwać"; "Tournament data" → "Dane turnieju"; "Map teams (N):" → "Mapuj drużyny (N):"; "N matches to import" → "N meczów do importu"; "Import N matches" → "Importuj N meczów"; "Importing..." → "Importowanie..."; "✅ Import complete" → "✅ Import zakończony". **PL→EN (EN users):** ScheduleCSVImport "Turniej:" → "Tournament:"; "Brak turniejów z przypisaną ligą..." → "No tournaments with a league assigned..."; "Zaimportuj N meczów" → "Import N matches"; log "✅ Zapisano..." → "✅ Saved..."; "⚠ Pominięto..." → "⚠ Skipped..."; "✅ Import zakończony" → "✅ Import complete". CSVImport "Drużyny" → "Teams"; "Gracze" → "Players"; "bez zmian" → "no changes"; "Importowanie N rekordów..." → "Importing N records..."; "✅ Import zakończony" → "✅ Import complete". | i18n B8 working as designed; wording unchanged, translations are new coverage for import flow modals. Extraction commit `8bb95888`, this commit carries translations in both directions. | shipped pending smoke |
| 2026-06-13 | (this commit) (B10) | BallisticsPage, PlayerStatsPage, LayoutAnalyticsPage, TacticPage | PL-language users see POLISH where hardcoded ENGLISH copy rendered before: "Ballistics" → "Balistyka", "TAP FIELD TO PLACE SHOOTER" → "DOTKNIJ POLE ABY USTAWIĆ STRZELCA", "FROM FREE POINT" → "Z WOLNEGO PUNKTU", "FROM {name}" → "Z {name}", "Computing... N%" → "Obliczanie... N%", "Tap a bunker…" → "Dotknij bunkra…", "Initializing field…" → "Inicjalizacja pola…", "Computing stats…" → "Obliczanie statystyk…", "All-time stats scan…" → "Statystyki all-time…", "Load all-time stats" → "Załaduj statystyki all-time", "Scout matches…" → "Scoutuj mecze…", zone legend strings (PL-origin: identical in EN), table "Tournament" → "Turniej", "Match" → "Mecz", "Side" → "Strona", "Couldn't load this tactic" → "Nie można załadować tej taktyki", "Loading…" → "Ładowanie…", "Rename tactic" → "Zmień nazwę taktyki", "Delete tactic" → "Usuń taktykę", "Tactic name" → "Nazwa taktyki", "Home" → "Główna"; "3-channel: green=safe·orange=arc·blue=exposed" domain label unchanged in both langs; "SURV"/"W"/"L" stat abbreviations unchanged (domain ruling); "Pt"/"P#" column abbreviations unchanged (domain). EN rendering unchanged. | i18n working as designed; wording not changed for non-PL strings; B10 analytics+ballistics extraction commit `a8a1a04a`, this commit carries PL/EN translations | shipped pending smoke |
| 2026-06-13 | (this commit) (B11) | AdminLeaguesPage, AdminTeamsPage, AdminPlayersPage, ChildrenOrphanWarning, TeamDuplicateResolutionView, MoreTabContent (super-admin section) | PL-language users see POLISH where hardcoded ENGLISH copy rendered before: "Active" → "Aktywne", "All" → "Wszystkie", "+ New league" → "+ Nowa liga", "About leagues" → "O ligach", "Deactivate league?" → "Dezaktywować ligę?", "Deactivate" → "Dezaktywuj", "About teams" → "O drużynach", "Resolve duplicate →" → "Rozwiąż duplikat →", "Restore" → "Przywróć", "Retire" → "Wycofaj", "← Prev" → "← Poprzednia", "Next →" → "Następna →", "Page N of M" → "Strona N z M", "About players" → "O zawodnikach", "Linked (PBLI)" → "Połączeni (PBLI)", "Unlinked" → "Niepołączeni", "Sort: name ↑" → "Sortuj: imię ↑", "Re-point to another parent" → "Przepnij do innej drużyny nadrzędnej", "Cascade retire (retire children too)" → "Kaskadowe wycofanie (wycofaj też podrzędne)", "Orphan (do nothing)" → "Osierocenie (nic nie rób)", "recommended" → "rekomendowane", "Workspaces" → "Workspace'y" + sub, "Layouts" → "Layouty" + sub, "Leagues" → "Ligi" + sub, "Players" → "Zawodnicy" + sub, "Teams" → "Drużyny" + sub; EN rendering unchanged; HERO domain rank = identical in both langs | i18n working as designed; wording unchanged, translations are new PL coverage (B11 admin pages) | shipped pending smoke |
| 2026-06-13 | (this commit) (B12) | ScoutTabContent, DrawToolbar, VisionScan, OCRBunkerDetect, LivePointTracker | PL-language users see POLISH where hardcoded ENGLISH copy rendered before: "New match" → "New match" (unchanged — already English), "Home team" / "Away team" (unchanged), "Add teams" (unchanged), "No matches yet" (unchanged), "Cancel"/"Create"/"Done"/"Add" (reuse generics — unchanged); EN users now see English where hardcoded Polish rendered before: "w polu" → "in field", "Opisz własnymi słowami…" → "Describe in your own words…" | i18n B12 components batch; all other strings were already English so PL rendering unchanged; LivePointTracker PL-origin strings corrected for EN users | shipped pending smoke |
| 2026-06-13 | (this commit) (regression-sweep) | MatchPage, ScoutedTeamPage, QuickLogView | PL-language users see POLISH where hardcoded ENGLISH copy rendered before: "OPEN" → "OTWARTE", "from" → "ze strony", "No point" → "Brak punktu", "Next point" → "Następny punkt", "Same" → "Ta sama", "Swap sides" → "Zamień strony", "Penalties" → "Kary", "Overtime" → "Dogrywka", "eliminated" → "wyeliminowany", "Manage roster" → "Zarządzaj składem", "No matches" (roster search) → "Brak wyników", "Empty roster" → "Pusty skład", "WIN" → "WYGRANA"; "Timeout" unchanged (loanword). EN rendering unchanged. | i18n regression-sweep — strings missed in H1 batches 1–6; translations are new PL coverage, wording not changed for EN. | shipped pending smoke |
| 2026-06-13 | (this commit) (B7) | LoginPage, PlayerEditModal, MergePlayersModal | PL-language users see POLISH where hardcoded ENGLISH copy rendered before (e.g. "Sign in" → "Zaloguj się", "Create account" → "Utwórz konto", "Edit player" → "Edytuj zawodnika", "Teams" → "Drużyny", "Merge N players" → "Połącz N zawodników", "Merging…" → "Łączenie…", auth errors in Polish); EN-language users see ENGLISH where Polish rendered before (e.g. "Link do zdjęcia…" → "Photo link…", "Otwórz profil PBL ↗" → "Open PBL profile ↗", "Usuń link" → "Remove link") | i18n working as designed; B7 highest-traffic surfaces (login+player-edit forms); extraction commit `e80e35b4`, this commit carries both PL→Polish and EN→English translations | shipped pending smoke |
| 2026-06-13 | (this commit) (B9) | TeamDetailPage, ScoutDetailPage, ScoutIssuesPage, BunkerCard | PL-language users see POLISH where hardcoded ENGLISH copy rendered before (e.g. "TEAM PROFILE" → "PROFIL DRUŻYNY", "No league" → "Bez ligi", "Roster (N)" → "Skład (N)", "Add players to this team" → "Dodaj zawodników do drużyny", "New player" → "Nowy zawodnik", "Delete team" → "Usuń drużynę", "SCOUT PROFILE" → "PROFIL SCOUTA", "Match progression" → "Postęp w meczach", "Volume" → "Ilość", "Quality" → "Jakość", "My scouting TODO" → "Moje TODO scoutingowe", "Missing shots" → "Brakujące strzały", "All caught up" → "Wszystko uzupełnione", "Position name" → "Nazwa pozycji", "+ Custom" → "+ Własna", "Mirror (add symmetric bunker)" → "Lustro (dodaj symetryczny bunker)"); EN rendering unchanged; Snake Beam domain name unchanged in both langs | i18n working as designed; wording unchanged, translations are new PL coverage (B9 detail pages) | shipped pending smoke |
| 2026-06-13 | (this commit) (H1 batch 4) | MatchPage, ScoutedTeamPage, QuickLogView | PL-language users see POLISH where hardcoded ENGLISH copy rendered before (e.g. "End match" → "Zakończ mecz", "Delete point?" → "Usunąć punkt?", "No scouted data yet" → "Brak danych ze scoutingu", "Tap to expand heatmap" → "Dotknij, aby powiększyć heatmapę", "Coach plan" → "Plan coacha", "Scout notes" → "Notatki scouta", "No players in this squad" → "Brak graczy w składzie", etc.); EN rendering unchanged | i18n working as designed; wording unchanged, translations are new coverage (H1 batch 4 — match & scout pages) | shipped pending smoke |
| 2026-06-12 | (this commit) | LeagueFormModal, PlayerFormModal, TeamFormModal, WorkspacesAdminPage, BunkerEditorPage | PL users see Polish labels in admin form modals where hardcoded English rendered before (e.g. "New league" → "Nowa liga", "Save & next →" → "Zapisz i dalej →"); EN rendering unchanged | i18n coverage extension per H1; translation commit separated from extraction per batch-hygiene ruling | shipped pending smoke |
| 2026-06-12 | (this commit) (H1 batch 6) | NewTournamentModal (title, type tabs, field labels, placeholders), EntityPickerModal (empty state, title default), ui.jsx SwipeDelete/ConfirmModal/Loading | PL-language users see POLISH where hardcoded ENGLISH copy rendered before (e.g. "Name" → "Nazwa", "Team" → "Drużyna", "— select team —" → "— wybierz drużynę —", "Delete" → "Usuń", "Cancel" → "Anuluj", "Loading…" default, "Select" → "Wybierz", "No matches" → "Brak meczów"); EN rendering unchanged | i18n working as designed; wording not changed, translations are new coverage. Extraction (render-identical) shipped as `3944dcac`, this commit carries the PL translations only | shipped pending smoke |
| 2026-06-12 | (this commit) | TrainingResultsPage, TrainingSetupPage, TrainingSquadsPage | PL-language users now see POLISH where hardcoded ENGLISH copy rendered before (e.g. "Training not found" → "Nie znaleziono treningu", "Who's at practice?" → "Kto jest na treningu?", "Break bunkers" → "Bunkry startowe", "Dismiss" → "Odrzuć", etc.); EN rendering unchanged | i18n working as designed; wording unchanged, translations are new coverage (H1 batch 5 — training pages) | shipped pending smoke |
| 2026-06-12 | (this commit) (H1 batch 3) | LayoutDetailPage, LayoutWizardPage | PL-language users see Polish where hardcoded English rendered before (e.g. "New layout" → "Nowy układ", "New tactic" → "Nowa taktyka", "Calibrate field" → "Kalibracja pola"). Tab labels LABELS/LINES/ZONES/DEATHS/POSITIONS/HITS/TACTICS unchanged (domain terms). EN rendering unchanged. | i18n working as designed; wording not changed, translations are new PL coverage for layout config screens. | shipped pending smoke |
| 2026-06-12 | `85c6b0a4` (H2 batch 2) | MatchPage gradient, PlayerStatsPage | `#080c14` → `COLORS.bg` `#0a0e17` (2 call sites) — perceptually ~zero, pixels change | `#080c14` is the documented phantom colour (REVIEW_CHECKLIST §2: "the old #080c14 was a phantom, never a token"); occurrences are phantom leakage, not design | **shipped** (own commit, separate from the pixel-identical mints `48099cb0`) |
| 2026-06-12 | `bead743f` (H1 batch 1) — **retroactive entry** | TeamsPage, PlayersPage, LayoutsPage, ProfilePage, MainPage | PL-language users now see POLISH where hardcoded ENGLISH copy rendered before (e.g. "Select a tournament…" → "Wybierz turniej albo utwórz nowy"); EN rendering unchanged | i18n working as designed; wording not changed, translations are new coverage. Batch-hygiene corrected from batch 2 on: extraction (render-identical) and translation (visible) ship as SEPARATE commits | shipped, owed Jacek smoke |

## Rules (from the same ruling)

1. **Extraction commit** = render-identical by construction (same string via key). **Translation
   commit** = visible change, own commit + register row.
2. **Epsilon rule for colour stragglers:** literal within epsilon of an existing token →
   normalize to the token (+ register row, since pixels change); literal used systematically
   as a distinct value → mint a named token (pixel-identical, no row needed).
3. Alpha-amber family: minted as exact-value tokens (pixel-identical). Re-bucketing to a tidy
   A10/A20 scale would be a visible normalization → would need its own register row first.
