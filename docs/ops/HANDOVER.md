# Session Handover — PbScoutPro

> **Purpose:** Living state-of-the-project for Opus chats (architect / strategy sessions). Read this before drafting any CC brief or making decisions about direction.
>
> **This replaces the static 2026-04-15 snapshot** (old version history accessible via `git log docs/ops/HANDOVER.md`). New model: updated at the end of every Opus session via a patch committed to this file.

**Last updated:** 2026-04-30 by Opus (post multi-source observations discovery session)
**Live app:** https://epicsports.github.io/pbscoutpro
**Repo:** https://github.com/epicsports/pbscoutpro
**Main HEAD at last update:** `99ae0e1`

---

## 🚧 Currently in flight

**Section 57 implementation queue.** Multi-source observations architecture approved 2026-04-30. Phase 1 (schema + propagator core) sized at ~3-4 days CC. Spec lives in DESIGN_DECISIONS § 57. Architecture diagrams in `docs/architecture/diagrams/multi_source/01..10_*.svg`. Discovery reports archived in `docs/archive/audits/2026-04-30_observations_discovery/`.

Awaiting Jacek green-light to start implementation post-NXL Czechy 2026-05-15.

---

## 🗄️ Pre-2026-04-25 in-flight notes (archived)

**17 ships across 2026-04-23 → 2026-04-24 (PPT + AUTH_ROLES_UNIFIED + ProfilePage § 33.3 + Settings reorg § 50 + four early-2026-04-24 polish briefs PLUS the late-2026-04-24 signup-unblock + PPT-hardening pass of 9 additional ships); no stacked WIP branches. Late-day sprint resolved the signup funnel end-to-end: ProfilePage self-claim restored, PPT sticky-training + Step 1/3/5 UX + bunker dedup, React #310 tournament crash hoisted, relax-player-linking cascade matcher + confirm + skip (LinkProfileModal), Coach live-score parity (shared useLiveMatchScores), PPT unlinked-mode (new pendingSelfReports collection, uid-scoped writes, migrate-on-link — see new § 52), retire team-code gate + auto-enter default workspace (see new § 51) + Členkowie NEW-badge/createdAt-desc, dot-notation bug on setDoc(merge) fixed (nested-map literal), PBLeagues onboarding rewritten to reuse LinkProfileModal + linkSkippedAt skip signal. Signup-to-PPT now works for everyone with zero admin intervention; admin audits reactively via Členkowie. The 2026-04-24 sprint pushed the app into an iPhone-validation-ready state for NXL Czechy Saturday — five surfaces shipped that day: § 50 Settings reorg (consolidated More tab + retired legacy BottomNav + added self-leave path + Członkowie admin override + soft-delete), P0 micro-hotfix batch (scout card live score from points subcollection, side-percentage removal, releaseClaim Sentry fix), heatmap toggle redesign (single-row aligned with scoreboard header grid), shot cone visualization (cones + break dashes replacing thin lines, both scouting canvas + heatmap), and scout completeness card rebuild (5 ranking metrics + composite, replaces inline 2-bar + ScoutScoreSheet with one canonical surface).

**Carry-over items from these sessions (not blocking, no active work):**
- **Brief G proper (full schema migration + rules refactor) — STILL DEFERRED.** AUTH_ROLES_UNIFIED introduced a dual-path reader (`workspace.userRoles` preferred, `users/{uid}.roles` bootstrap fallback). Works today; adds cognitive load. Full unification still wants its own dedicated off-hours window with Firebase Admin SDK.
- **Brief C Option 2** — per-user feature flag overrides. Workspace-wide audience-rule model shipped in Option 1; per-user overrides still deferred.
- **canAccessRoute completeness audit + RouteGuard sweep** — pure-player blocked from `/profile` by default-deny; allowlist extension needed before wider URL-level gating can ship safely. Low priority, isolated.
- **Bug A + Bug C** (per Jacek, 2026-04-21 iPhone validation) — Bug A: Team B positions + shots not visible on match heatmap. Bug C: remove coaching stats percentages from match view entirely. Blocked on HTML mockup owed by Opus per § 1.4 workflow.
- **Stale `*ClaimedBy` / `*ClaimedAt` Firestore field cleanup** — cosmetic hygiene; no code reads them. Run from Console when convenient.
- **selfReports ownership tightening (§ 49.11)** — current PPT rules gate on `isPlayer(slug)`, not on `pid` matching caller's linked player. Invited-workspace model contains attack surface; tightening is defense-in-depth, not urgent.
- **workspace.userRoles self-write diff gap (§ 49.11)** — pre-existing latent privilege-escalation risk in the self-join envelope update rule. Not introduced by AUTH_ROLES_UNIFIED; flagged there for visibility. Fix = field-value validation in rules.
- **Matchup-matching product** (§ 48.10) — coach-side workflow to assign orphan PPT selfReports to matchup/point. Separate product brief when PPT usage demands it.
- **PPT iPhone validation** — PPT shipped 2026-04-23 without device validation (`GO merge + deploy` came before iPhone test). PPT rules hotfix landed same day via AUTH_ROLES_UNIFIED, unblocking writes. Full user-path walkthrough still pending.
- **§ 50.7 follow-ups (NEW from Settings reorg ship)** — per-workspace admin check on `/users/{uid}` writes (replace ADMIN_EMAILS allowlist with custom claims); server-side Firebase Auth deletion (true delete, requires Admin SDK); coach/staff profile entities (only player profiles exist today); legacy URL toast (currently no redirect — pages reachable from Settings); DRY between MoreTabContent + TrainingMoreTab `Training*` helpers.
- **§ 50 iPhone validation** — Settings reorg + Wyjdź flow + Członkowie detail page shipped overnight 2026-04-24 with no device validation. Test path: pure-player sees only WORKSPACE + KONTO; coach/admin sees full menu; admin can navigate Members → user detail → link/unlink/disable; soft-disabled user gets DisabledAccountScreen on next login.
- **2026-04-24 polish sprint iPhone validation** — five surfaces shipped same day with no device validation. Specific test surfaces: (a) match scouting flow back arrow (Sentry should stop firing for releaseClaim); (b) scout tab card scores show live values during LIVE matches with unmerged points; (c) cone radius `0.20 × min(w,h)` on scouting canvas — flag if dense breakouts overcrowd (single constant in `drawQuickShots.js`); (d) heatmap cone radius `0.10` + lower opacity — flag if it reads too faint/dense in aggregate; (e) heatmap team B color migration teal → blue (matches toggle palette); (f) toggle row alignment with scoreboard header grid (left/right capsules under team names, center spacer matches score zone min-width 110px); (g) CompletenessCard 5 metrics + composite color-coded, role-gated to scout/coach/admin; § 27 amber-twice exception (top tier celebration + mid tier warning) — if it reads ambiguously swap mid-tier to `'#fb923c'` orange (single line in `tierFor`).
- **~~CoachTabContent live-score parity~~** — DONE in `fa2f15c` (`f0bfbe8`). `useLiveMatchScores` extracted to `src/hooks/useLiveMatchScores.js`; both Scout + Coach tabs share it. Hook call above the early-return in Coach (React #310 fix applied prospectively).
- **New follow-ups from § 51 / § 52 ships:**
  - Stale `userRoles.<uid>` top-level fields on `workspaces/ranger1996` Firestore doc (pollution from pre-fix `setDoc(merge)` dot-notation attempts before `c81dade`). Harmless — rules don't read them — but cosmetic cleanup via console batch delete when convenient.
  - `pendingSelfReports` not included in `getLayoutShotFrequencies` collection-group query — unlinked PPT shots don't contribute to crowdsource until migrated. Trade-off documented in DEPLOY_LOG for `fa2f15c`; cheap to extend if real data pressure emerges.
  - `onPlayerLinked` migration is per-link, not per-doc retry. If it fails partway, docs stay in pending until next link attempt. Production-grade would be a Cloud Function trigger on `players/{pid}.linkedUid` change; deferred.
  - Unlinked users see ALL workspace trainings in the PPT picker (no team filter — they have no team yet). Acceptable for single-workspace reality; if the picker becomes noisy in a multi-team ranger1996 scenario, a "for me / any" toggle is cheap.
  - `pbliIdFull` is not written on the new onboarding / self-claim link path (uses `selfLinkPlayer` not `linkPbliPlayer`). Admin can fill it via Členkowie if needed; no UI gap today since the read path already falls back to `pbliId`.
  - `linkSkippedAt` is a one-way signal today; no admin UI in Členkowie to reset it per user. Fine for current scale (Firestore console workaround exists).
  - Legacy `parsePbliId` + `linkPbliPlayer` + `PBLI_ID_FULL_REGEX` kept in `roleUtils.js` + `dataService.js` despite no live callers. Safe to drop in a follow-up sprint if they stay unused.
- **§ 49.10 migration policy supersession** — `§ 49` stated "auto-join, nie auto-login" (user still types code). `§ 51` overturns this: new users auto-enter `defaultWorkspace` from their user doc. The `§ 49` text is now historical — if you reread §49 and find contradictions, §51 wins.
- **Path X — deprecate `match.currentHomeSide` cross-coach sync (NEW follow-up, high-value architectural cleanup).** Under Brief 8 v2 (§ 42) per-coach point streams, each coach's `fieldSide` is local. The shared `match.currentHomeSide` field is a relic from the pre-Brief-8 chess model that still writes on winning-point saves + manual flips, propagating to the other coach's view via `useEffect` in `MatchPage.jsx:575-595`. Tonight's `c817516` Path Y hotfix added a `hasDraftData` guard that defers the flip during active placement — fixes the immediate regression without removing the relic. Full cleanup (Path X): remove the sync effect entirely, remove `ds.updateMatch({currentHomeSide})` writes from `savePoint` auto-flip (`MatchPage.jsx:914-926`) + manual flip pill (`MatchPage.jsx:1581-1587`), remove `lastSyncedHomeSideRef`. Three downstream risks to verify before merging: (a) initial perspective on first open — is it derived from `match.currentHomeSide` or from per-point snapshots? (b) heatmap observer — does `HeatmapCanvas` read `match.currentHomeSide` for canonical orientation? (c) single-coach legacy path — `changeFieldSide` runs locally regardless, should be fine but verify. Codify as DESIGN_DECISIONS § 53 or § 42.5 revision that explicitly supersedes the 2026-04-21 Bug 3a revert (commit `29c2be1`).
- **autoEnter 20:43 UTC 403 still firing post-`c81dade` — diagnostic logging added `c817516`.** Dot-notation fix didn't fully resolve. Next failure captures pre-write workspace shape + write payload + error structure to console. Suspect: orphan `userRoles.<uid>` top-level fields from pre-`c81dade` failed attempts polluting `affectedKeys()` → `hasOnly([...])` rejection. Monitor logs; clean up the workspace doc via Firebase console if confirmed.

Next active work starts when Jacek drops the next brief or brings a mockup for Bug A+C.

---

## 🚢 Recently shipped (2026-04-13 → 2026-04-30)

Six sprint days added since last update: **2026-04-25** (NXL Saturday hotfix tail + end-of-MAX security/quality audit phase 1 + Tier A/B/C hardening start), **2026-04-26** (anonymous user purge + Tier C vendor split + auto-swap regression), **2026-04-28** (§ 53 Custom Squad Names design + day-end park), **2026-04-29** (KIOSK rollout — 3 briefs + 6 hotfixes + squad-rename merge in single calendar day), **2026-04-30** (Brief D player-stats KIOSK integration + React #300 hooks-rule hotfix + Brief E 4 entry points + § 56). Chronological, newest first:

| Date | Deploy commit | Summary |
|---|---|---|
| 2026-04-30 | n/a (docs only) | **Multi-source observations architecture (§ 57)** — discovery + decision session. Option C chosen (write-back propagation with sibling _meta arrays). 22 source files analyzed, 10 architecture diagrams produced, schema extensions defined (slotIds, _meta arrays, slotRef), conflict rules table approved, free tier impact verified (~5% daily write limit). Onboarding 9 guidance moments deferred to § 57 Phase 2. |
| 2026-04-30 | `ce8c320` | **Brief E — 4 phone-facing entry points to PlayerStatsPage + auto-default scope** (§ 56). Closes Brief D incentive loop on the player's phone (KIOSK tablet no longer required for stats access). (1) ProfilePage linked-player section gets dedicated surface card with "📊 Moje statystyki" `Btn variant="accent" size="lg"` between edit-form card and Rozłącz row — own card so it doesn't compete with "Zapisz dane gracza" amber CTA (§ 27 anti-pattern: multiple CTAs per surface). (2) ProfilePage not-linked fallback — existing self-claim CTA copy swaps to "Połącz profil żeby zobaczyć statystyki" via new i18n key; single CTA preserved → opens existing LinkProfileModal. (3) More tab → KONTO → "📊 Moje statystyki" `MoreItem` after "Mój profil" in BOTH `MoreTabContent.jsx` (tournament) + `TrainingMoreTab.jsx` (training); gated on `useWorkspace().linkedPlayer`. (4) PPT TodaysLogsList footer link "Zobacz statystyki dnia →" `Btn variant="ghost"` between rows and sticky "+ Nowy punkt" amber CTA — ghost (not amber) by design so sticky CTA retains primary status; rendered only when `playerId && combined.length > 0`. Plus auto-default scope: when `linkedPlayer.id === playerId` AND no `?scope=` param AND trainings loaded → `navigate(?scope=training&tid={latestTid}, {replace:true})`; latestTid derived from already-subscribed `useTrainings()` + client filter on `attendees` (zero new Firestore reads, zero new indexes, zero new helpers). STEP 0.5 surfaced 2 brief deviations Jacek-approved (toast inside TodaysLogsList vs wrapped around it; reuse existing subscription vs new fetch helper). |
| 2026-04-30 | `dd2d37a` | **React #300 hotfix — TrainingScoutTab hooks-rule violation.** Hotfix #6 (`b210b86`) added `useMemo(liveMatchupIds)` + `useLiveMatchScores(...)` AFTER TWO conditional returns in TrainingScoutTab (`if (qlMatchup) return <QuickLogView/>` line ~109 + `if (!training) return <EmptyState/>` line ~191). When `training` resolved mid-mount or QuickLog opened/closed, hook count changed between renders → React infinite-loop recovery → "Minified React error #300" crash on training launch. **Third occurrence of same anti-pattern** — already documented in KioskLobbyOverlay split outer-guard/inner-data (2026-04-29) and ScoutTabContent commit `950ab79` (2026-04-24). Fix: hoist `current` (matchups.filter wrapped in useMemo for stable ref), `liveMatchupIds`, `useLiveMatchScores` above both early returns; hooks now run unconditionally; early returns short-circuit only the JSX. Build + precommit clean. |
| 2026-04-30 | `80cc945` | **Brief D — PlayerStatsPage scope=training fix + KIOSK toast deep-link.** Four items closing the audit findings: (a) Field resolution — was passing `field: null` to `computePlayerStats`, leaving zone/bunker stats blank for training points. Now resolves training layout via `resolveFieldFull(syntheticTournament, layouts)` and threads it through. (b) Self-log aggregation — KIOSK self-log data now flows into player profile: new `dataService.fetchSelfLogShotsForPlayer(playerId, trainingId)` collectionGroup query (single-field index, post-fetch refine to `source='self'` + tournamentId); PlayerStatsPage attaches `selfLog` (from `point.selfLogs[playerId]`) and `selfShots` (grouped by pointId) to each player point; `playerStats.computePlayerStats` counts `selfLoggedElim` when coach didn't mark elim, falls back to self-log breakout for position/bunkerCounts when no coach assignment, classifies self-shot zones via `getBunkerSide`. (c) `getSquadName` — opponent label respects § 53 custom squad names (was hardcoded R1-R4). (d) Post-KIOSK toast — sticky bottom toast with "Zobacz swój dzień" amber CTA after self-log save in `KioskLobbyOverlay`; 8s auto-dismiss + × manual; CTA navigates to `/player/{id}/stats?scope=training&tid={tid}`. Closes incentive loop: player self-logs in KIOSK → sees same-day stats → motivated to self-log next training. i18n keys `kiosk_save_toast_title`/`_cta` PL+EN. |
| 2026-04-29 | `f717fda` | **KIOSK Brief C — Prefill Resolver (Source A scouting + Source D coach elim).** Final brief in 3-part KIOSK rollout. § 55.4 + § 55.5. Source A: scouting positions + shots from `homeData.players[slot]` (positions) + `homeData.shots[slot]` (shot list). Source D: coach Live Tracking eliminations via slot-indexed schema `homeData.eliminationStages[slot]` etc., read via `deathTaxonomy.readNormalizedEliminations(teamData)[slot]`. **Source B (drawing on layout) + Source C (Quick Log zone narrowing) deferred** — Source B needs UI design + storage schema; Source C `point.<side>Data.zones` field doesn't exist (QuickLogView `zones` is local React state, never persisted) — adding zone persistence is a separate brief. Resolver returns `bunkerPickerFilter: null` for Source C miss → KIOSK Krok 1 picker shows full layout-wide top 6 (no narrowing — same as vanilla PPT). Plus reason-key translation for Source D legacy slug → § 54 canonical map. KIOSK feature complete for tablet landscape MVP modulo deferred sources. |
| 2026-04-29 | `b210b86` | **Hotfix #6 — squad-names merge + live matchup score (bundled).** BUG 2 (squad-names merge — Path 2A): squash-merged `feat/custom-squad-names` (originally `ece9246`, parked 2026-04-28). Implements DESIGN_DECISIONS § 53 — purple 5th squad (#a855f7), `getSquadName(training, key)` resolver, squad-count limit raised 4→5, `SquadEditor` zone header tappable opens rename Modal with Pencil affordance + 16-char cap + revert-to-default. Display propagation across TrainingScoutTab + TrainingCoachTab + TrainingResultsPage + MatchPage (training adapter) + PlayerStatsPage. 5 i18n keys × PL+EN (rename_squad_*). BUG 1 (matchup score on Mecze tab): `MatchupCard` read `matchup.scoreA/B` which Brief 9 Bug 2 Option A deliberately stops writing during LIVE play — cards showed 0:0 / "— : —". Same fix pattern as tournament tab via shared `useLiveMatchScores` hook (3rd-arg `subscribeFn` for training path). **Carried a hooks-rule violation** — useLiveMatchScores call placed below `if (qlMatchup)` + `if (!training)` early returns; surfaced as React #300 next day, fixed in `dd2d37a`. Also surfaced squad-rename input bug (Input contract mismatch) fixed in `e31016b`. |
| 2026-04-29 | `519b34b` + `ae2625f` + `4dfb88a` + `41f0c47` + `a786ad8` + `332f77f` | **KIOSK Brief B — Player Verification lobby + post-save summary (§ 55) + 5 hotfixes same day.** Lobby = full-screen overlay at App root, driven by `KioskContext.{lobbyOpen, postSaveOpen}`. Composition per v3 mockup: header (back chevron + "Punkt #N — kliknij swoje imię" + progress pill) → 5-tile player grid filtered to point's scouted side → "Wcześniejsze punkty" missing-data section. Tap tile → `kiosk.activePlayerId` arms wizard host → on save, lobby refreshes via Firestore snapshot ✓. **Hotfix series codified three process gaps:** (1) `ae2625f` React #310 fix — `useMemo(olderPoints)` was after `lobbyOpen` early return; split into outer-guard / inner-data components. (2) `4dfb88a` t-double-call — i18n keys with function values get called by `t()` internally; pattern `t('key')(args)` failed; fixed to `t('key', ...args)` in 5 sites. (3) `41f0c47` schema correction — spec text said `point.homeData.players[]` for IDs but actual schema is `homeData.assignments[]` for IDs, `homeData.players[]` for positions; pointNumber doesn't exist as field (derive from sorted-by-order index); use canonical `matchScore(points)` not stale matchup.scoreA/B. (4) `a786ad8` Path 2 amendment — HotSheet predates § 36 + § 54; rewrote KIOSK wizard to host PPT step components via `KioskWizardHost` (saves wizardState to point.shots/ subcoll instead of selfReports). (5) `332f77f` § 54.3.1 — `elim_break` is its own reason; skip cascade in player surfaces. Brief B unblocked Brief C (resolver couldn't write without lobby). E6 viewport gate: phone/portrait → no KIOSK, coach proceeds via existing FAB + Tier 1 HotSheet. |
| 2026-04-29 | `ef94637` | **KIOSK Brief A — Death Reason Taxonomy (§ 54) + coach 2-step inline picker.** Foundational brief for KIOSK rollout. Defines canonical death-reason vocabulary: 4-stage axis (alive / break / inplay / endgame, per § 54.3 amendment 2026-04-29) + 7 canonical reasons (gunfight / przejscie / faja / na_przeszkodzie / za_kare / nie_wiem / inaczej). Slot-indexed schema D1.A: `homeData.eliminationStages[slot]`, `homeData.eliminationReasons[slot]`, `homeData.eliminationReasonTexts[slot]`. Reader helper `deathTaxonomy.readNormalizedEliminations(teamData)` handles both new + legacy `eliminationCauses` shapes (D2.no-migrate). Coach 2-step inline picker in MatchPage replaces single-tap elimination toggle with stage → reason → optional text. § 54.3.1 break-is-its-own-reason rule (no cascade for `elim_break` in player surfaces). PPT wizard later adopted same vocabulary in Brief B Path 2 amendment. |
| 2026-04-28 | `333b91f` | **Day-end park** — `feat/custom-squad-names` branch pushed to origin awaiting Jacek smoke test (8 scenarios per CC_BRIEF_CUSTOM_SQUAD_NAMES STEP 4); auto-swap regression fix `13837e4` and Tier C forward fix `f604343` deployed but not user-validated. § 53 design doc shipped (`4e27073`) but not yet implemented in code. KIOSK Brief A blocked on missing § 40 spec content + missing `outputs/MOCKUP_KIOSK_v2.html` (resolved 2026-04-29 via § 55 spec write + v3 mockup). |
| 2026-04-26 | `13837e4` | **Auto-swap regression fix — same-team consecutive points.** Brief 9 Bug 2 Option A side effect: when consecutive points in the same matchup share the same team picker (e.g. coach scouts Team A back-to-back), the auto-swap toggle lost memory between points and stayed off. Restored via per-team memory ref scoped to the matchup. Pre-existing single-team behavior preserved; concurrent-coach behavior unchanged (currentHomeSide path retired in Path X 2026-04-25). |
| 2026-04-26 | `e0b8ee4` + `f604343` + `a221e2e` | **End-of-MAX Tier C — vendor manualChunks split + ADMIN_RUNBOOK §§ 12 + 13 completion.** Tier C: `vite.config.js` `build.rollupOptions.output.manualChunks` splits vendor bundle into `vendor-react`, `vendor-firebase`, `vendor-sentry` for parallel CDN download + better cache invalidation per dependency tree. **Carried a regression** — `react-router-dom` + `lucide-react` accidentally split out from `vendor-react`, causing `createContext` runtime error on first prod load; fixed in `f604343` by tightening manualChunks key match. ADMIN_RUNBOOK § 12 (incident response checklists) + § 13 (Firestore manual-fix recipes for `*ClaimedBy` orphans + stale `userRoles.<uid>` dotted keys + selfReports orphan migration) shipped in `a221e2e`. |
| 2026-04-26 | `ed855cc` + `32d35b8` | **Anonymous user purge — 611 docs deleted via Admin SDK script.** `scripts/purgeAnonymousUsers.mjs` enumerates `users/{uid}` where `email IS NULL OR email == ''` (legacy anonymous-auth signups before email/password gate), confirms each batch, deletes via Admin SDK. 611 docs purged. **Orphaned `/users/{uid}` docs intentionally retained** as "Unknown scout" fallback for legacy points whose `scoutedBy` UID no longer matches a real user. Smoke-test still pending Jacek (non-blocking — purge was idempotent). |
| 2026-04-25 | `bed5d05` | **End-of-MAX Tier B — Firestore rules hardening.** Two lockdowns: (a) `passwordHash` write protection — workspace doc rule extended to reject any update that touches `passwordHash` from non-admin caller (was previously open via affectedKeys allowlist that didn't list passwordHash explicitly). (b) `/users/{uid}` disabled-family lockdown — soft-delete `disabled` flag + `disabledAt` + `disabledBy` can only be written by admin (per ADMIN_EMAILS allowlist; per-workspace check still deferred to custom claims migration). Audit doc + DEPLOY_LOG record both threats + verification path. Rules deployed via `firebase deploy --only firestore:rules` before client. |
| 2026-04-25 | `e8abb7b` | **End-of-MAX Tier A — `.gitignore` tightening + orphaned PBLI helper drop.** `.gitignore` adds `*.local`, `node_modules.bak/`, `*.tmp.md`, `_scratch/` to prevent accidental commits of working files. Orphaned PBLI helpers (`linkPbliPlayer`, `parsePbliId`, `PBLI_ID_FULL_REGEX`) flagged in 2026-04-24 follow-up — confirmed zero live callers, dropped from `roleUtils.js` + `dataService.js`. Net −180 LOC. |
| 2026-04-25 | `8396146` + `51f3fa3` | **End-of-MAX production audit (phase 1 — security + quality).** Two audits: `8396146` security audit covers Firestore rules surface (10 findings, 3 P0 — passwordHash, /users disabled lockdown, self-link missing-field; addressed in Tier B + `d548ad3`), Anthropic API key rotation (P0 outstanding — Jacek-only via console.anthropic.com), anonymous user purge (P0 — addressed `ed855cc`). `51f3fa3` quality audit covers code smells + DEPLOY_LOG completeness + ADMIN_RUNBOOK gaps — drove Tier A cleanup + ADMIN_RUNBOOK §§ 12 + 13. Both audits archived to `docs/archive/audits/`. |
| 2026-04-25 | `f7a23ad` | **Path X — single-coach side flip (partial follow-through on currentHomeSide deprecation).** Long-tracked HANDOVER follow-up partially executed: `savePoint` no longer writes `match.currentHomeSide` when no concurrent coach is active (single-coach + per-coach-streams architecture per Brief 8 v2 / § 42 means the field is local-only in single-coach mode). Manual flip pill still writes (intentional — explicit user gesture on a shared field). Three downstream risks verified: (a) initial perspective derived from per-point snapshot, not currentHomeSide; (b) HeatmapCanvas reads per-point snapshots; (c) single-coach legacy `changeFieldSide` runs locally regardless. **Full Path X (remove sync effect entirely) still deferred** — current state codifies the Bug-3a-revert behavior into a proper architecture rather than leaving it as accidental. |
| 2026-04-25 | `0484120` | **Restore back nav on Settings ZARZĄDZAJ pages.** § 50 Settings reorg accidentally dropped `back={{ to: '/' }}` on `LayoutsPage`, `TeamsPage`, `PlayersPage` — pure-player + scout users had no way to return to Settings tab from those destinations (had to use browser back). Three-line fix per page, restored `back` prop on `<PageHeader>`. |
| 2026-04-25 | `d548ad3` | **Self-link missing-field rules fix.** Self-link Firestore carve-out `resource.data.linkedUid == null` rejected players whose docs predate the `linkedUid` field entirely (legacy player docs from before § 33.3 self-claim flow). Fix: switch to `resource.data.get('linkedUid', null) == null` — `data.get()` returns the supplied default for missing fields, accepting both null-explicit and field-absent shapes. Idempotent re-claim already shipped via Path 2 (`c817516`). Rules deployed before client merge. |
| 2026-04-24 | `c817516` | **3 batched Saturday-prep fixes** — 1 commit (`0ba285a`) + rules redeploy. (a) **Concurrent-scout flip guard (Path Y minimal)** — coach B's field flipped when coach A saved a winning point mid-placement. Added `hasDraftData` short-circuit to MatchPage sync effect (`MatchPage.jsx:575-595`), mirrors existing editingId guard's intent. Path X architectural cleanup (full `match.currentHomeSide` deprecation per Brief 8 per-coach-streams model) deferred to tracked HANDOVER follow-up with three downstream risks flagged. (b) **autoEnter diagnostic logging** — c81dade dot-notation fix didn't fully resolve prod 403s (Jacek's 20:43 UTC log fired 52min post-fix). Static re-analysis can't repro; added catch-block instrumentation capturing pre-write workspace shape (members type/length/caller-inclusion, userRoles dotted-key detection, top-level dotted keys, caller existing roles, rolesVersion), write payload keys, full FirebaseError structure. Console-only; next failure lands actionable. Suspect: orphan `userRoles.<uid>` dotted top-level fields from pre-`c81dade` failures polluting `affectedKeys`. (c) **Defensive self-link rule (Path 2)** — `firestore.rules` self-link carve-out broadened from `resource.data.linkedUid == null` to `(resource.data.linkedUid == null || resource.data.linkedUid == request.auth.uid)` — idempotent re-claim permitted. Security unchanged (post-state invariant still blocks hijack). Rules deployed before client. |
| 2026-04-24 | `2f8f971` | **Brief RELAX_PBLEAGUES_ONBOARDING** — Tier 1, 1 commit. The second signup blocker after retire-team-code. `PbleaguesOnboardingPage` had strict `NNNNN-NNNN` regex via `parsePbliId` + a dead-end "Nie znaleziono gracza" branch whose only action was Wyloguj się. Rewrote the page as a shell (logo top-bar + softened card + pbleagues.com external link) with `<LinkProfileModal open={true} onClose={handleSkip} onSelect={handleLink} />` rendered atop — reuses the same 5-priority pbliMatching cascade + "Czy to ty?" confirm + "Pomiń na razie" skip shipped in `fa2f15c` for ProfilePage. Zero logic duplication. New persistence signal `users/{uid}.linkSkippedAt: serverTimestamp()` via `ds.skipLinkOnboarding`; `App.jsx` gate updated to `if (!linkedPlayer && !userProfile?.linkSkippedAt)`. Link uses `ds.selfLinkPlayer` (consistent with ProfilePage) + `onPlayerLinked` migrates pending PPT drafts afterwards. No rules change — user self-write on `/users/{uid}` already permits arbitrary field writes. Legacy `parsePbliId` / `linkPbliPlayer` / `PBLI_ID_FULL_REGEX` kept in place but no longer imported by UI. Self-heals for users stuck pre-deploy: next visit shows new UI with visible Pomiń — one tap unblocks. See DESIGN_DECISIONS § 51 for the full signup-flow redesign narrative. |
| 2026-04-24 | `c81dade` | **Fix autoEnter dot-notation** — 1 commit. `autoEnterDefaultWorkspace` (and the pre-existing `enterWorkspace`) used `update[\`userRoles.${uid}\`] = ...` on `setDoc(merge:true)`. Unlike `updateDoc`/`tx.update`, **`setDoc(merge)` does NOT parse dot-notation** — it creates a literal top-level field named `userRoles.<uid>`. firestore.rules self-join envelope's `affectedKeys().hasOnly([...])` then rejected because the dot-named key wasn't in the allow-list → 100% of fresh signups hit "Permission denied" despite the retire-team-code ship. Fix: nested-map literal `update.userRoles = { [uid]: [...] }`. `setDoc(merge)` does recursive map-merge so existing entries are preserved. Audit verified no other `setDoc(merge)` + dot-notation sites in the codebase (`dataService.js` hits all use `updateDoc` or `tx.update`, both safe). Memory saved for future agents. |
| 2026-04-24 | `c9d99eb` | **Brief RETIRE_TEAM_CODE_AUTO_JOIN_MEMBERS_AUDIT** — Tier 2, 1 commit. **Part 1 — retire team-code gate:** `src/pages/LoginGate.jsx` DELETED (109 LOC). `App.jsx` no longer routes `!workspace` through a password-entry screen; shows `<Loading text="Preparing your workspace..." />` while auto-enter runs + `<AutoEnterErrorScreen>` with sign-out escape on failure. **Part 2 — auto-enter:** new `autoEnterDefaultWorkspace` in `useWorkspace.jsx`. Target slug = `userProfile.defaultWorkspace` or `DEFAULT_WORKSPACE_SLUG` fallback. Skips password check (system-trusted target). Write shape = existing self-join envelope → no rules change. Auto-approve when slug matches user's default + bootstrap roles array non-empty. Drive effect fires once per session via `useRef(false)` re-entrancy flag. `enterWorkspace(code)` path stays for admin Settings workspace-switch. **Part 3 — Členkowie audit surface (Variant 3 reactive moderation):** `useUserProfiles` extended to return `createdAt`. `MembersPage` active members sorted `createdAt` desc. `MemberCard` renders small green "Nowy" badge for ≤7d joiners (§ 27 — green, non-interactive; amber reserved for interactive). Section header sub-count "N nowych w tym tygodniu" (green). Admin retains delete via § 50.4 unchanged. NOTE: this ship carried the dot-notation bug in autoEnter — caught + fixed same day via `c81dade`. |
| 2026-04-24 | `fa2f15c` | **Bundle: Step 5 sticky + Coach live-score parity + PPT UNLINKED-MODE** — 3 commits (`ed1d524` + `f0bfbe8` + `e94aafa`). (a) Step 5 "Zapisz punkt" pinned to viewport bottom via position:fixed + safe-area + gradient fade + 120px spacer (mirrors the Step 3 fix from `34755ce`). (b) `useLiveMatchScores` extracted to `src/hooks/useLiveMatchScores.js`; CoachTabContent wired with the hook above the early-return (Rules of Hooks safe by construction — the React #310 trap from `950ab79` doesn't recur). Closes the symmetry gap called out in `629edc8`. (c) **PPT unlinked-mode** — biggest change: new collection `/workspaces/{slug}/pendingSelfReports/{auto-id}` with `uid` field for ownership. Firestore rule deployed via `firebase deploy --only firestore:rules` (create/read/update/delete all gated on `uid == auth.uid`). Service: `createPendingSelfReport(uid, payload)` + `getTodaysPendingSelfReports(uid)` + `migratePendingToPlayer(uid, playerId)` + terminal `onPlayerLinked(uid, playerId)` (flushes local uid-namespace offline queue to canonical player path, then migrates server-side pending docs). `pptPendingQueue` + `usePPTSyncPending` gain `mode` param ('player' / 'uid') for namespace-separated queues. `usePPTIdentity` returns `uid` + falls back to ALL workspace trainings when unlinked. `WizardShell` branches `handleSave` between paths. `TodaysLogsList` reads from pending path when unlinked. Translucent-amber unlinked banner on both surfaces routes to /profile. Migrate-on-link wired into both `ProfilePage.handleClaim` + `PbleaguesOnboardingPage.handleSubmit`. See DESIGN_DECISIONS § 52 for the architecture narrative. |
| 2026-04-24 | `83c929b` | **Brief RELAX_PLAYER_LINKING** — Tier 2, 1 commit. Real users couldn't self-link to player profiles from ProfilePage due to PBLI ID format mismatches (legacy matcher didn't strip `#`, short-circuited `pbliIdFull` lookups). Replaced substring matcher in `LinkProfileModal` with 4-priority cascade in new `src/utils/pbliMatching.js`: exact `pbliId` / exact `pbliIdFull` / first-segment extract / substring. `matchPlayers(query, players)` single entry — empty query → alphabetical unlinked roster, PBLI-ish input → cascade, alpha input → nickname/name substring. `LinkProfileModal` rewritten as 3-state in-place swap (list / confirm / no-match) with **ALWAYS-required** "Czy to ty?" confirmation gate before write + "Pomiń na razie" skip-link CTA when 0 hits. Defensive write-side normalize in `PlayerEditModal` + `CSVImport`. PPT empty-state gained prominent "Połącz teraz" CTA. Shared modal means Členkowie admin panel picks up same behavior (with confirm gate). Option B chosen per Checkpoint 1 audit — PPT unlinked mode was DEFERRED here but shipped hours later in `fa2f15c`. |
| 2026-04-24 | `bbad249` | **Fix React #310 tournament scouting crash** — 1 commit. Tournament Scout view crashed with React #310 immediately on open, blocking 100% of scouting. Root cause: P0 Fix 1 (commit `629edc8`, the earlier same-day `useLiveMatchScores` hook) added `useMemo(liveCandidateIds)` + `useLiveMatchScores(...)` at lines 223 / 227 of `ScoutTabContent.jsx`, **below** the existing `if (!tournament) return` early return at line 141. On first render with tournament bootstrapping, 17 hooks ran; on the next render tournament resolved, 19 hooks ran — hook-count mismatch → React assertion fires. Fix: hoist `filtered` (plain const) + both hooks above the early return; removed the duplicate computation below. Functional behavior preserved; P0 Fix 1 live-score feature still works. No revert needed. Audit confirmed `CoachTabContent` + `CompletenessCard` don't have the same pattern. |
| 2026-04-24 | `34755ce` | **PPT hotfix follow-up** — 1 commit (direct to main). (a) Step 1 (breakout) bootstrap path wasn't deduped — mirrored the Step 3 `bunkerListFromLayout` fix into `Step1Breakout.jsx`. (b) § 48.3 spec called for sticky Dalej CTA on Step 3 but implementation rendered it inline; on layouts with many bunkers the CTA scrolled off-screen mid-selection. Pinned to viewport bottom with `position: fixed` + safe-area inset + gradient fade + 120px spacer. |
| 2026-04-24 | `31c1f7d` | **Brief PPT_HOTFIX_BATCH_2** — Tier 1, 2 commits. Jacek's iPhone validation pre-Saturday training surfaced 2 P0 regressions. (a) **Sticky training** — picker re-appeared on every "+ Nowy punkt" tap when `liveTrainings.length !== 1`. New `src/utils/pptActiveTraining.js` (date-stamped localStorage), entry logic prioritizes sticky over `showList` / single-LIVE inference, `handleSave` resets state in-place + bumps local pill count + inline toast (no navigation), training pill becomes the "Zmień trening" affordance (clears sticky + forces picker), Step 1 back arrow uses `?leave=1` to break the sticky-redirect loop. (b) **Shots picker dedup** — `bunkerListFromLayout` only filtered `role==='mirror'` but `layout.bunkers` can carry duplicate entries per `positionName` (legacy docs without `role`, or `BunkerEditorPage`'s master+mirror persistence pattern). `BunkerPickerGrid` keys order-badges by `positionName` → twin-badge symptom on single tap. Defensive first-write-wins dedupe in Step 3's local helper. Step 1 not touched per brief acceptance (fixed in follow-up `34755ce`). |
| 2026-04-24 | `04ff7fc` | **Brief PROFILEPAGE_HOTFIX_BATCH** — Tier 1, 3 commits. 3 regressions from § 50 settings-reorg batched. (a) Restored § 33.3 linked-player self-claim empty-state UI (only linked-state rendered post-reorg). New `ds.selfLinkPlayer` / `ds.selfUnlinkPlayer` transactional helpers with self-link rule carve-out (`resource.data.linkedUid == null`). (b) Missing i18n keys (`profile_roles_label`, `profile_player_*`, `profile_claim_*`, `profile_unlink_*`) added in PL+EN — `t('key') \|\| 'fallback'` fallback doesn't fire for missing keys because a raw key string is truthy. (c) Removed misplaced "Podgląd: Admin" floating pill — `ViewAsIndicator` + `ViewAsPill` neutralized; `ViewAsContext` returns null; sessionStorage cleared on mount. Old files kept for easy revival. |
| 2026-04-24 | `02752ae` | **Brief SCOUT_COMPLETENESS_REBUILD** — Tier 2, 1 commit. Match view's two prior completeness surfaces (inline 2-bar Breaks/Shots mini-summary inside Points list block + scout-only `ScoutScoreSheet` 3-row card) replaced with one canonical `CompletenessCard` (`src/components/scout/CompletenessCard.jsx`) visible to scout/coach/admin (pure-player hidden). All 5 ranking metrics + composite shown — Breaks (35% weight) / Shots (20%) / Przypisania (20%) / Biegacze (10%) / Eliminacje (15%) / Ogólny wskaźnik. New `computeMatchBreakdown(points)` exported from scoutStats; `computeMatchCompleteness` refactored to one-line wrapper for ScoutDetailPage back-compat. 4-tier color scale: ≥90 amber+⭐ celebration / 70-89 green / 50-69 amber warning / <50 red+⚠ incomplete — § 27 amber-twice exception flagged (precedent set by existing `compositeColor()`); strict-§27 alternative would swap mid-tier to `'#fb923c'` orange. ScoutScoreSheet.jsx (256 lines) + inline 2-bar (~50 lines) deleted. 8 new i18n keys under `completeness_*` namespace. |
| 2026-04-24 | `5db6a95` | **Brief SHOT_CONE_VISUALIZATION** — Tier 2, 1 commit. Shot rendering on scouting canvas (`drawQuickShots.js`) and match heatmap (`HeatmapCanvas.jsx`) switched from thin lines / ticks to angled cones (obstacle, 18% fill + 80% stroke) or 3 radiating dashed radii (break, from EXACT player center to 75% of cone radius). New `src/utils/shotGeometry.js` utility — `TEAM_DIRECTIONS` lookup (A: dorito -30°, center 0°, snake +30° / B mirrored: 210°/180°/150°), `shotDirectionDeg(zone, fieldSide, doritoSide)` resolver with viewport-mirror + top/bottom dorito flip, `tracePathCone` Canvas2D path builder (SVG sweep-flag distinction translated as no-op — Canvas2D's default arc with `a1 < a2` naturally draws the SHORT arc bulging outward for both teams), `getBreakShotDashEndpoints`, `vectorDirectionDeg` for heatmap. Scouting canvas radius `0.20 × min(w,h)`; heatmap reduced to `0.10` with lower opacity for aggregation context. Heatmap team B color migrated teal → blue (`TEAM_COLORS.B`) aligning with toggle redesign palette. Kill 💀 cluster layer untouched. Data layer: zero changes. FieldCanvas gains optional `team` prop; MatchPage passes `activeTeam`. |
| 2026-04-24 | `acb28c7` | **Brief HEATMAP_TOGGLE_REDESIGN** — Tier 2, 1 commit. `PerTeamHeatmapToggle.jsx` restructured from two-row stacked layout (with team-name capsules + amber-active chips) to single-row mirroring the scoreboard card flexbox above the heatmap exactly: `[Team A capsule (flex:1)][spacer (minWidth:110)][Team B capsule (flex:1)]` matching MatchPage scoreboard's `[Left team flex:1][Score zone minWidth:110][Right team flex:1]`. Each capsule = 44px tall segmented control (background `surfaceDark`, border + 10px radius) holding two chips ("Positions" / "Shots") with `flex:1`. Chip active = full team color fill (red `#ef4444` for A, blue `#3b82f6` for B) + white text. Chip inactive = transparent + dim text + transparent border. Team-name labels removed (scoreboard above carries them). 36px chip touch (acceptable for analysis context per brief). Used flex (not the brief's grid) because header card is itself flex — perfect alignment via mirror. i18n reuses existing `conf_pill_positions/shots`. |
| 2026-04-24 | `629edc8` | **P0 micro-hotfixes batch** (`hotfix/p0-batch-2026-04-23`) — Tier 1, 3 fixes batched into one deploy. (a) **Scout card score** — Brief 9 Bug 2 architectural decision (commit `da36f49`) deliberately stops `savePoint` from writing `match.scoreA/B` during LIVE (coachUid race). New `useLiveMatchScores(tournamentId, matchIds)` hook in ScoutTabContent subscribes to `subscribePoints` per non-closed match, derives `{a,b}` via canonical `matchScore` helper extracted from MatchPage to `src/utils/helpers.js` (replaces dead `{w,l,t,total}` export). MatchCard accepts optional `liveScore` prop. Side-effect fix: LIVE/Scheduled classification bug (same root cause). Closed matches not subscribed. CoachTabContent has same bug — out of scope, follow-up if needed. (b) **Side percentages** — `<CoachingStats>` block in MatchPage role-gated heatmap section deleted entirely; `computeCoachingStats` function preserved for ScoutedTeamPage. (c) **releaseClaim ReferenceError** — two orphan call sites in MatchPage back handlers (Brief F leftover); deleted. `grep releaseClaim` returns zero. |
| 2026-04-24 | `0fe8739` | **Brief SETTINGS_REORG_NAV_CLEANUP — § 50** — Tier 2, 3 checkpoints, 4 commits. **Settings menu** restructured to Jacek's exact six-section spec (SESJA / ZARZĄDZAJ / SCOUTING / WORKSPACE / KONTO / ADMIN) with strict per-section + per-item gating per § 49; tab "More" → "Ustawienia" via new `tab_settings` i18n. **Legacy BottomNav.jsx deleted** — AppShell role-tab bar (Scout/Coach/Gracz/Ustawienia) is now the only bottom nav app-wide; routes remain reachable via Settings → ZARZĄDZAJ / SCOUTING (no redirects added — pages aren't dead). **Wyjdź workspace** flow: `ds.leaveWorkspaceSelf` wraps `removeMember`, ConfirmModal + last-admin guard, two Firestore rule carve-outs (workspace self-leave envelope + player self-unlink). **Członkowie full UX** (closing the AUTH_ROLES_UNIFIED CP4 gap): new `/settings/members/:uid` UserDetailPage (AdminGuard) with admin link override (LinkProfileModal — search players, conflict surface with conflicting user's email, atomic re-link via `ds.adminLinkPlayer`), unlink, deliberate role edit, soft-delete via `users/{uid}.disabled` flag. AppRoutes bootstrap watches the disabled flag and renders DisabledAccountScreen — user can re-authenticate but bounces back. Soft-delete rule via ADMIN_EMAILS allowlist (jacek@epicsports.pl) — single-admin reality; per-workspace check requires custom claims (deferred). KONTO sign-out ungated (was admin-only, locking pure-player out of UI logout). Four spec deviations documented in DEPLOY_LOG (no Podgląd jako placeholder, no legacy redirects, no coach/staff entities, sign-out ungated). |
| 2026-04-23 | `7e69a2f` | **§ 33.3 — ProfilePage roles display + linked-player self-edit** — Single-checkpoint follow-on to § 49. ProfilePage gains read-only Roles section (`RoleChips` from `useWorkspace().roles`) + conditional Player data section when active workspace contains a player doc with `linkedUid === auth.uid`. 6 editable fields: nickname/name/number/age/nationality/favoriteBunker. Team / PBLI ID / role / class stay admin-only via Firestore rules `affectedKeys.hasOnly([...])` whitelist. PhotoURL editor *removed* per Jacek interrupt ("more players with their photos"). Self-edit Firestore rule deployed via `firebase deploy --only firestore:rules` before client merge. Propagation via existing `onSnapshot` — Members/PPT Gracz/scout ranking/training squads see edits within ~200ms with no new wiring. |
| 2026-04-23 | `18be720` | **Brief AUTH_ROLES_UNIFIED — user-doc roles + strict tab matrix (§ 49)** — 4 checkpoints. User-doc schema adds `roles: string[]` (bootstrap) + `defaultWorkspace: 'ranger1996'`. `enterWorkspace` auto-approves for default workspace (mirrors roles, skips pending). Canonical role resolver: `workspace.userRoles[uid]` (authoritative) → `userProfile.roles` (bootstrap) → `[]`. **Strict tab visibility matrix replaces § 47 permissive** — Scout/Coach/Gracz/More each gated by single-role `requiredAny`. **New Gracz tab** (🏃, between Coach and More) routes to `/player/log` on tap; Brief E Option 2 wchłonięte. **Viewer role retired** — new `ASSIGNABLE_ROLES` = 4 roles; legacy viewer data preserved via `ROLES` (5). **PPT Firestore rules hotfix** folded in — selfReports subcollection + collection-group read rules deployed via `firebase deploy` at Checkpoint 2; § 48 had shipped without them and was default-deny-blocked in prod. Migration policy: new users only. |
| 2026-04-23 | `f8416d7` | **Brief PPT_MASTER — Player Performance Tracker (§ 48)** — Tier 2, 5 checkpoints, 21 files, +3800 lines. Full 5-step wizard for pure-player performance logging during training. Route `/player/log` (picker or today's list) + `/player/log/wizard?trainingId=X` (5 steps). `createSelfReport` writes to `/workspaces/{slug}/players/{pid}/selfReports/{auto}` per § 48.5 schema. Adaptive pickers (bootstrap vs mature per § 48.6). Offline queue (`pptPendingQueue.js` + `usePPTSyncPending`) flushes on `window.online` + route changes. Glove-friendly touch targets (88/76/72/64/44 — 2× Apple HIG min). Reusable `BunkerPickerGrid` shared between Step 1 + Step 3. Firestore composite index `(layoutId, breakout.bunker, createdAt desc)` deployed via `firebase deploy --only firestore:indexes` on merge. **iPhone validation still pending** as of HANDOVER update. |
| 2026-04-22 | `afb47c5` | **Brief G Option B — role + membership defensive shims** — `getOrCreateUserProfile` stops writing junk `role` string; `parseRoles()` shim accepts array/string/undefined; session-restore slug normalization via existing `slugify()`. Zero Firestore data migration. § 33.1 + § 33.2 codify deprecation. |
| 2026-04-22 | `57fc3e6` | **Brief F — concurrent scouting cleanup** — ~40 BUG-B/C diagnostic logs removed + claim system retired (homeClaimedBy/At + heartbeat + side-picker LIVE state). § 18 DEPRECATED. Net −232 lines. |
| 2026-04-22 | `f6d4910` | **Brief E Option 1 — SessionContextBar removal + role-gated tabs** — inline bar function deleted; TAB_DEFS gets requiredAny per tab; pure-player sees only More. F2 (quick scouting only in training) explicitly dropped. |
| 2026-04-22 | `e7b3f78` | **Brief D — Members + Mój profil targeted fixes** — `useUserProfiles` hook, inline role chip toggles with last-admin guard, Remove-from-workspace destructive styling + expanded confirm body, ProfilePage displayName dedup. |
| 2026-04-22 | `3688786` | **Brief C Option 1 — Scouting MoreSection + Feature flags inline edit** — handedness toggle (first UI for the long-existing localStorage key); Feature flags promoted to admin-only MoreSection with inline enable toggle + audience cycle pill. |
| 2026-04-22 | `d029856` | **Brief B — copy cleanup + language-flag single-source** — Przeglądaj→Zarządzaj; LangToggle removed from PageHeader (affects all 22 pages); LanguageSection in More stays canonical. |
| 2026-04-22 | `67b9a49` | **Brief A — tournament setup polish (I1 / I2 / H1)** — duplicate Add team CTA XOR-gated; Add-team modal multi-select with batch add; NewTournamentModal + EditTournamentModal multi-division. |
| 2026-04-21 | `19527a0` | **Brief 9 + Bug 3a revert** — canonical order; Option A score semantics; auto-flip toast suppression; 3a guard reverted same day. |
| 2026-04-21 | `30d169e` | **Brief 8 v2 — per-coach point streams + end-match merge (architectural redesign)** — doc IDs `{matchKey}_{coachShortId}_{NNN}`; idempotent merge; URL entry semantics. |
| 2026-04-21 | `8669b91` / `dc62f9c` / `bc6954d` | **Briefs 6 + 7 + 7-bis — surgical fixes** — savePoint joinable narrowed; edit-vs-new side pointer separation; L852 mirror. |
| 2026-04-20 | `eb0f247` | **Documentation cleanup** — root reduced 14 → 4 `.md`, new `docs/{architecture,ops,product,archive/audits}/`, 28 CC briefs archived. |
| 2026-04-20 | `e11e845` | **Player Self-Report MVP Tier 1** — FAB + HotSheet bottom sheet in MatchPage. |
| 2026-04-19 | (polygon SHA) | **Unified polygon zone editor** — Google-Maps-style drag/midpoint-insert/delete; iOS magnifier suppressed. |
| 2026-04-19 | (multi-branch) | **Notes + Big Moves + Kluczowi gracze refinements** — Coach Notes, BigMoves zone, multi-key sort, section renames + Lucide icons. |
| 2026-04-19 | (training fix) | **Training match navigation hotfix** — PlayerStatsPage respects `isTraining` flag. |
| 2026-04-18 | `0f4ef8a` | **Coach Brief View** — ScoutedTeamPage Sławek's 4 priorities; `<SideTag>` component. |

See `DEPLOY_LOG.md` for fuller entries with known-issues notes.

---

## 🎯 Next on deck (priority order)

### 1. Section 57 implementation Phase 1 (post-NXL window)
- Spec: `docs/DESIGN_DECISIONS.md` § 57
- Diagrams: `docs/architecture/diagrams/multi_source/`
- Estimate: 3-4 days CC, single commit
- Deliverables: schema extensions (slotIds, _meta arrays, slotRef), useMatchupPropagator hook, findMatchingPoint matcher, bunkerToPosition utility, conflict resolver, late-log auto-trigger
- Definition of done: § 57.11

### 2. Section 57 Phase 2 — provenance UI (~1-2 days)
- Heatmap visual differentiation per _meta.source
- ScoutedTeamPage "filled by" badges
- Insights confidence weighting (optional)

### 3. Section 57 Phase 3 — onboarding integration (~1.5 days)
- 9 guidance moments per `docs/architecture/ONBOARDING_GUIDANCE.md`
- onboardingFlags storage on users/{uid}
- Per-moment trigger logic + Polish microcopy
- Settings reset option

### 4. Player Self-Report Tier 2 + Integrations (carried from 2026-04-20 backlog)
- "Mój dzień" section in `PlayerStatsPage` (own points list, completion status, Tier 2 edit modal for killer/notes)
- Shot accuracy section per player (top targets, hit rate, filter by layout/tournament/global)
- `ScoutedTeamPage` hybrid view when scouted team is own team (self-reported shots layer alongside scout data)
- Tactic page shot suggestions (if current tactic schema supports shots — needs confirmation before brief)
- Rationale: Tier 1 lives now but flywheel payoff comes from letting players see their own data

### 5. Validate Tier 1 on iPhone before Tier 2
- Still blind-coded UI — no Playwright test on device yet
- Thresholds (5 / 20) may need tuning
- Breakout collapse animation may need polish
- Could block Tier 2 if fundamentals need rework

### 6. Brief G (full schema migration + rules refactor) — DEFERRED, high effort
- **Scope:** audit script (read-only Admin SDK, enumerates inconsistencies) → migration script (dry-run + execute, idempotent, backup-preserved) → consolidate `workspace.members` into `users.workspaces` as single source of truth → `firestore.rules` refactor on canonical membership check → dead-field cleanup (`*ClaimedBy`, `*ClaimedAt`, `passwordHash`, `adminUid` where redundant with `userRoles`).
- **Pre-req:** dedicated windowed session with Jacek available at every checkpoint. NOT continuous-flow compatible per brief's own risk protocol.
- **Trigger:** today's Option B shims stopped the leak (no new junk data from fresh sign-ups; legacy bad strings still survive but are unread). Will resurface when more multi-user activity hits dead-schema issues or Jacek schedules dedicated time.
- 5-checkpoint progression per parent brief's risk assessment.

### 7. Bug A + Bug C — match heatmap redesign
- **Bug A:** Team B positions + shots not visible on match heatmap. Architectural regression from single-toggle assumption.
- **Bug C:** remove coaching stats percentages (dorito/snake/disco/zeeker/center/danger/sajgon) from match view entirely — statistically noisy at small sample size, belongs only on ScoutedTeamPage aggregate.
- **Blocked on:** HTML mockup owed by Opus per DESIGN_DECISIONS §1.4 workflow (koncept → prototyp → design → kod). 2026-04-21 session closed before mockup was built.
- Related clarification needed: "nie można wejść bezpośrednio jako scout do danej drużyny" — repro steps + intended flow.

### 8. canAccessRoute completeness audit + RouteGuard sweep (Brief E follow-up)
- Pure-player currently blocked from `/profile` by default-deny branch in `canAccessRoute` (roleUtils.js:88-95).
- Extending RouteGuard to `/teams`, `/players`, `/my-issues` etc. for URL-typing defense-in-depth requires allowlist extension first (at minimum `/profile`, probably `/scouts`).
- Low priority but blocks future role-gated route additions.

### 9. Brief C Option 2 — per-user feature flag overrides
- Option 1 (workspace-wide flags + admin toggle UI) shipped 2026-04-22.
- Option 2: individual user-level override for testing/rollout (`users/{uid}.featureFlagOverrides` map layering over workspace defaults, or explicit `userIds` allowlist in audience spec).
- Requires Firestore rules audit for `users/*` admin read access.

### 10. User-reported F-bugs (from April PXL weekend)
- **F2 DROPPED** per 2026-04-22 user decision — quick scouting stays available in all current contexts. Removed from backlog.
- **F3:** Quick shots dual mode — verify shipped vs partial (`CC_BRIEF_QUICK_SHOTS.md` archived)
- **F4:** Sample size indicator — `CC_BRIEF_TEAM_STATS_CARDS.md` archived, verify shipped
- **F5:** Self-scouting / counter analysis — partially addressed by SelfLog hybrid view; may need own brief
- **F6:** Tournament profiles — per Jacek may be solved by quick shots dual mode
- **F7:** Training data → break selection — adjacent to SelfLog flywheel; wait for data to accumulate first

### 11. Stale Firestore claim fields cleanup
- `*ClaimedBy` / `*ClaimedAt` fields on existing match docs — no code reads them post-Brief-F.
- Cosmetic Firestore hygiene only. Can run from Console with batch `updateDoc(deleteField())`. Harmless if deferred indefinitely.

### 12. BreakAnalyzer module (carried)
- Specs exist: `docs/architecture/BREAK_ANALYZER_SPEC.md` + `docs/architecture/BREAK_ANALYZER_DOMAIN_v2.md`
- Implementation scaffolded but needs tuning against real field data
- Opus territory per NEXT_TASKS.md (`don't touch src/workers/ballisticsEngine.js`)

### 13. Tournament tendencies analytics (carried)
- Cross-tournament lineup/player patterns
- Blocks on: sufficient scouted data volume + SelfLog maturity

### 14. Matchup-matching product (§ 48.10 + § 49.11 follow-up)
- Coach-side workflow to assign orphan PPT `selfReports` (`matchupId: null`, `pointNumber: null`) to matchup/point. Unblocks coach analytics of player self-reported data.
- Scope: new admin/coach tool, likely inside ScoutedTeamPage or MatchPage. Reads uncategorized selfReports + matches them to canonical point ids.
- Waits on PPT usage data accumulating first — no point building this until a player has logged enough points that matching matters.

### 15. selfReports ownership tightening (§ 49.11)
- Current `match /selfReports/{sid}` rules gate on `isPlayer(slug)`, not on `pid` matching the caller's `linkedUid`. Defense-in-depth improvement.
- Workspace-invited model contains attack surface today — this is future-proofing, not urgent.

### 16. workspace.userRoles self-write diff gap (§ 49.11 — pre-existing)
- The self-join envelope update rule in `firestore.rules` allows a user to write arbitrary values to their own `userRoles[uid]` slot during enterWorkspace. Latent privilege-escalation risk.
- Not introduced by any recent brief; surfaced during § 49 audit.
- Fix = field-value validation in rules (e.g. `hasOnly(['player'])` on non-admin writes).

---

## ⏸️ Awaiting decision (needs Jacek input)

| Topic | Question | Blocks |
|---|---|---|
| **§ 57 implementation start date** | Pre-NXL Czechy or post-? Ship dual-write schema before tournament so weekend data populates _meta arrays from day 1, OR avoid mid-season risk and start post-NXL? | Phase 1 deploy timing |
| **Bug A+C mockup** | HTML mockup for match heatmap redesign — 4 toggles (Team A pos/shots + Team B pos/shots) vs team picker + data-type toggle vs segmented control. Also: completely remove coaching stats from match view, hide behind "Show advanced stats" toggle, or move lower in page hierarchy? | Brief for Bug A+C fix |
| **"Wejść bezpośrednio jako scout do danej drużyny"** | Doprecyzuj — brakuje entry pointu z match card, czy side picker został zlikwidowany zostawiając ambiguity którego team scoutujesz? | Bug A+C mockup design |
| **Brief G (full migration) scheduling** | When does full Firestore migration + rules refactor happen? Trigger: multi-user activity hitting dead schema, operational tolerance runs out, or Jacek schedules dedicated off-hours window | Whether Brief G runs soon or waits months |
| **`match.currentHomeSide` under Brief 8 architecture** (carried) | Per-coach streams store `fieldSide` per doc, yet manual flip-pill + auto-flip after scored point still write the shared `match.currentHomeSide`. Should this field be deprecated entirely, kept as shared-flip signal (current post-Bug-3a-revert behavior), or rewired to per-coach state? Today's behavior is intentional pragmatic choice, not a designed endpoint. | Post-Saturday concurrent refactor (if any); future claim-system cleanup migration |
| ~~**`PlayerSelfReportV4.jsx` mockup** (carried)~~ | ~~Provide mockup or accept extrapolated UI as-is? Brief referenced it across 2 sessions but never landed in repo~~ — **Resolved 2026-04-30:** § 57 write-back model makes mockup question moot | ~~Polish pass on Tier 1 + Tier 2 UI~~ |
| **Tactic schema shots support** (carried) | Current tactic schema does NOT carry shots per-position. Add shot field to tactics, or skip tactic-page suggestions? | SelfLog Integrations (tactic suggestions task) |
| **F5 vs F6 vs F7 priority** (carried, F2 dropped 2026-04-22) | Which user-reported feature is most important pre-NXL Czechy 2026-05-15? | Which brief gets written next |
| **BreakAnalyzer ship date** (carried) | Module scaffolded but needs tuning. Block NXL release on it, or defer post-NXL? | Engineering capacity allocation |
| **Security refactor scope** (carried, partially resolved) | Option B defensive shims shipped 2026-04-22. Full rewrite (Brief G proper) still TBD — Cloud Functions cost implications, rules consolidation scope | Sequencing vs NXL deadline |

---

## 🏛️ Architecture decisions reference

Long-form architecture docs live in `docs/architecture/`. Opus should read the relevant one before drafting specs touching that area.

| Doc | Topic | When to read |
|---|---|---|
| [`BALLISTICS_SYSTEM.md`](../architecture/BALLISTICS_SYSTEM.md) | Euler integration, hitboxes, 3-channel visibility | Anything touching `src/workers/ballisticsEngine.js` or BallisticsPage |
| [`BREAK_ANALYZER_SPEC.md`](../architecture/BREAK_ANALYZER_SPEC.md) | BreakAnalyzer Phase 1 spec | Before BreakAnalyzer feature work |
| [`BREAK_ANALYZER_DOMAIN_v2.md`](../architecture/BREAK_ANALYZER_DOMAIN_v2.md) | Domain vocabulary + constants (companion to SPEC) | Same as above |
| [`BUNKER_RECOGNITION.md`](../architecture/BUNKER_RECOGNITION.md) | NXL 2026 bunker taxonomy (15 types), shape classification | Before Vision scan, bunker editor, or shape-aware features |
| [`HALF_FIELD_SPEC.md`](../architecture/HALF_FIELD_SPEC.md) | Mirror system (master + computed mirrors), `computeMirrors()`, calibration transform | Before FieldCanvas changes or bunker storage schema |
| [`MULTI_SOURCE_OBSERVATIONS_INDEX.md`](../architecture/MULTI_SOURCE_OBSERVATIONS_INDEX.md) | Master index for § 57 decisions, schema, diagrams, migration plan | Before § 57 implementation work |
| [`PLAYER_SELFLOG.md`](../architecture/PLAYER_SELFLOG.md) | Two-tier model, unified shots collection, synthetic coords, flywheel thresholds | Before Tier 2 / Integrations work |
| [`TACTIC_WORKFLOW.md`](../architecture/TACTIC_WORKFLOW.md) | Tactic editor scouting-style flow, second position (bump), curve cycling | Before tactic page changes |

---

## 📐 Recent design decisions

`docs/DESIGN_DECISIONS.md` holds 57 numbered sections + sub-sections 5.7 / 33.1 / 33.2 / 35.5 (rewritten) / 35.7 / 54.3 (amended) / 54.3.1 / 55.2 (revised) / 55.6 / 55.8 / 55.10 / 55.11 / 56.1 / 56.2 / 56.3. Most recent (newest first):

| § | Topic | Date | Notes |
|---|---|---|---|
| 57 | Multi-source observations architecture | 2026-04-30 | Option C (write-back), batch end-of-matchup propagator, _meta arrays sibling pattern, free tier safe |
| 56 | Player stats entry points + auto-default scope=training for self-view | 2026-04-30 | Brief E. 4 phone-facing entry points to `PlayerStatsPage`: (1) ProfilePage CTA `Btn variant="accent" size="lg"` "📊 Moje statystyki" on its own surface card (1 card = 1 CTA per § 27); (2) ProfilePage fallback — existing self-claim CTA copy swaps to "Połącz profil żeby zobaczyć statystyki" via new i18n key (single CTA preserved); (3) More tab → KONTO → MoreItem in BOTH `MoreTabContent.jsx` + `TrainingMoreTab.jsx`, gated on `useWorkspace().linkedPlayer`; (4) PPT TodaysLogsList footer link `Btn variant="ghost"` (not amber — sticky "+ Nowy punkt" stays primary), gated on `playerId && combined.length > 0`. Auto-default scope: when `linkedPlayer.id === playerId` AND no `?scope=` param → redirect to `?scope=training&tid={latestTid}` with `replace: true`; latestTid from already-subscribed `useTrainings()` + client filter on `attendees` (zero new Firestore reads / indexes / helpers — STEP 0.5 deviation B from brief, Jacek-approved). Brief E gaps 4 (QR/share) + 6 (sub-nav) deferred. |
| 55 | KIOSK Player Verification mode | 2026-04-29 | 3 briefs A→C + 6 hotfixes shipped same day. § 55.1 § 55.2 lobby (5-tile player grid filtered to scouted side, single-grid no per-squad sections — § 55.2 revision) + § 55.3 wizard host (KioskWizardHost composes PPT step components per Path 2 amendment / Hotfix #4 — replaces stale HotSheet) + § 55.4 prefill resolver (Source A scouting positions/shots from slot-indexed `homeData.players[slot]` + `homeData.shots[slot]` per Hotfix #3 schema correction) + § 55.5 Source D coach elim (slot-indexed `eliminationStages[slot]` etc., read via `deathTaxonomy.readNormalizedEliminations`) + § 55.6 missing-data older points + § 55.8 multi-tablet truth + § 55.11 v3 mockup. E1 context-direct identity (no useSelfLogIdentity hook recreated; KIOSK lobby renders wizard with `playerId={kiosk.activePlayerId}` overriding email-matched player). E3 overlay not route. E4 training only for MVP. E6 viewport gate (≥1024×768 landscape required; phone/portrait → existing FAB + Tier 1 HotSheet). Source B drawing + Source C zone narrowing deferred to separate briefs. |
| 54 | Death Reason Taxonomy — canonical dictionary | 2026-04-29 | Foundational for KIOSK rollout. § 54.1 7 canonical reasons (gunfight / przejscie / faja / na_przeszkodzie / za_kare / nie_wiem / inaczej). § 54.3 4-stage axis (alive / break / inplay / endgame — endgame added as amendment 2026-04-29 to original 3-state). § 54.3.1 break-is-its-own-reason rule — no cascade for `elim_break` in player surfaces. § 54.5 D1.A schema: slot-indexed `homeData.eliminationStages[slot]`, `homeData.eliminationReasons[slot]`, `homeData.eliminationReasonTexts[slot]`. § 54.5 D2 no-migrate policy — reader helper `deathTaxonomy.readNormalizedEliminations(teamData)` handles both new + legacy `eliminationCauses` shapes. Coach 2-step inline picker in MatchPage (stage → reason → optional text) replaces single-tap toggle. Brief A `ef94637`. PPT wizard later adopted same vocabulary in Brief B Path 2 amendment. |
| 53 | Custom Squad Names + 5-squad limit | 2026-04-28 (spec) / 2026-04-29 (shipped) | Spec doc shipped `4e27073` 2026-04-28; implementation parked on `feat/custom-squad-names` branch (originally `ece9246`) awaiting Jacek smoke test, merged via Hotfix #6 (`b210b86`) 2026-04-29 alongside live matchup score fix. Adds 5th purple squad (#a855f7), `getSquadName(training, key)` resolver reading `training.squadNames[key]` with fallback to legacy R1-R5 via `squadName()`, `buildDefaultSquadNames()` for new training docs. Squad-count limit raised 4→5 in `SquadEditor`. Zone header tappable opens rename Modal with Pencil affordance, 16-char cap, empty-string reverts to default. `dataService.updateTrainingSquadName(tid, key, name)` helper. 5 i18n keys × PL+EN (rename_squad_*). Display propagation: TrainingScoutTab + TrainingCoachTab + TrainingResultsPage + MatchPage (training adapter) + PlayerStatsPage (Brief D Item c). Hotfix #6 carried hooks-rule violation in TrainingScoutTab — surfaced as React #300, fixed `dd2d37a` 2026-04-30. |
| 52 | PPT unlinked-mode — pendingSelfReports + migrate-on-link | 2026-04-24 | Shipped in `fa2f15c` (bundle commit `e94aafa`). New workspace-scoped collection `/workspaces/{slug}/pendingSelfReports/{id}` with per-doc `uid` ownership. Firestore rule gates create on `request.resource.data.uid == auth.uid`, read/update/delete on `resource.data.uid == auth.uid` — no coach visibility (drafts). Service: `createPendingSelfReport` + `getTodaysPendingSelfReports` + `migratePendingToPlayer` (200/batch with per-doc fallback) + terminal `onPlayerLinked(uid, playerId)`. `pptPendingQueue` + `usePPTSyncPending` gain `mode` param for namespace-separated queues. `WizardShell` + `TodaysLogsList` accept `uid` + branch storage. Unlinked banner on both surfaces. Migrate-on-link wired into ProfilePage `handleClaim` + PbleaguesOnboardingPage `handleSubmit`. |
| 51 | Signup flow redesign — access-first + Variant 3 reactive moderation | 2026-04-24 | Three ships codified together: `c9d99eb` (retire team-code + auto-enter + Členkowie audit) + `c81dade` (setDoc(merge) dot-notation fix) + `2f8f971` (relax PBLeagues onboarding). **Supersedes § 49.10 "auto-join nie auto-login" philosophy** — new users now auto-enter `defaultWorkspace` on first session. Team-code `LoginGate` DELETED; `App.jsx` guard uses `if (!linkedPlayer && !userProfile?.linkSkippedAt)`. `PbleaguesOnboardingPage` rewritten to reuse `LinkProfileModal` + 4-priority PBLI cascade + "Czy to ty?" confirm + "Pomiń na razie" skip fallback. Členkowie panel surfaces new joins: createdAt-desc sort + green NOWY badge ≤7d + "(N nowych w tym tygodniu)" sub-count. Variant 3 rationale: adoption is the blocker; admin audits reactively via Členkowie delete/unlink. `setDoc(merge)` dot-notation trap documented — use nested-map literals. |
| 49 | Unified auth + roles + tab visibility | 2026-04-23 | Brief AUTH_ROLES_UNIFIED (4 checkpoints). `users/{uid}.roles` bootstrap default + `defaultWorkspace: 'ranger1996'` scalar pointer. `enterWorkspace` auto-approves for default workspace (mirror + skip pending). Canonical role resolver: workspace.userRoles (non-empty) → userProfile.roles → []. **Strict tab matrix** replaces § 47 permissive — Scout/Coach/Gracz/More each gated by single-role `requiredAny`. New Gracz tab (🏃, routes to `/player/log`). Viewer retired from `ASSIGNABLE_ROLES` (ROLES kept for legacy parse). **PPT Firestore rules hotfix** folded in (§ 48 had shipped without them). Migration policy: new users only; existing untouched. Supersedes § 47 + wchłonie Brief E Option 2. |
| 48 | Player Performance Tracker (PPT) — wizard flow + training picker | 2026-04-22 (spec) / 2026-04-23 (shipped) | Brief PPT_MASTER (Tier 2, 5 checkpoints). Full 5-step wizard for pure-player performance logging. Route `/player/log` (picker + today's list) + `/player/log/wizard?trainingId=X`. `createSelfReport` → `/workspaces/{slug}/players/{pid}/selfReports/{auto}` with § 48.5 schema. Adaptive pickers (bootstrap vs mature, thresholds 5 player-logs / 20 layout-shots). Offline queue via `pptPendingQueue` + `usePPTSyncPending` (flush on online + route change). Glove-friendly touch targets (88/76/72/64/44). Firestore composite index `(layoutId, breakout.bunker, createdAt desc)` deployed at merge. `BunkerPickerGrid` shared between Step 1 + Step 3. |
| 47 | Role-gated tab visibility + pure-player More rule (§ 47 permissive — **superseded by § 49 strict** 2026-04-23) | 2026-04-22 | Brief E Option 1. AppShell TAB_DEFS with `requiredAny`; Scout/Coach/More matrix; effective-admin bypass; persisted-tab fallback effect. Pure-player `isPurePlayer` predicate hides scout/coach-level MoreSections. Route-guard sweep deferred. |
| 46 | Settings IA — Scouting section + Feature flags home + deferred per-user override | 2026-04-22 | Brief C Option 1. More tab IS the Settings surface (§ 31). New `ScoutingSection` (handedness toggle). Feature flags promoted to own admin-only MoreSection; inline enable toggle + audience cycle (all/beta/admin); writes to `/workspaces/{slug}/config/featureFlags`. Per-user overrides deferred as Option 2. |
| 45 | Members page inline role editing + profile identity single-render | 2026-04-22 | Brief D. B1 fallback chain (linkedPlayer → displayName → email → fallback; no UID fragments). B2 live role chips for admins + self-admin self-protect (§ 38.3) retained. B3 last-admin guard at chip + menu + confirm. C1/C2 ProfilePage displayName single-render rule. |
| 44 | Brief 9 polish — canonical docs, score semantics, toast suppression | 2026-04-21 | Canonical merge docs get `order: Date.now()+i`. Option A: score written only at end-match merge (0:0 during active). Bug 3b auto-flip toast suppressed. Bug 3a `modeParam !== 'new'` guard reverted same day (killed § 2.5 auto-flip). |
| 43 | URL entry semantics for scouting | 2026-04-21 | Rule 1/2/3: Scout CTA = `&mode=new` always-new, list card = `&point=<id>` edit, auto-attach fallback search removed. Brief 8 Problem A. |
| 42 | Per-coach point streams + end-match merge | 2026-04-21 | Replaces shared-doc chess-model. Doc ID `{matchKey}_{coachShortId}_{NNN}`, `useCoachPointCounter` hook (localStorage), idempotent `endMatchAndMerge` per-index lockstep. Brief 8 Problem B. |
| 41 | Edit-vs-new side pointer separation | 2026-04-21 | `point.fieldSide` snapshot vs `match.currentHomeSide` live pointer. Defense-in-depth: G2 auto-swap effect `if (editingId) return;` + savePoint post-write `&& !editingId`. Brief 7. |
| 40 | Per-team heatmap visibility toggle | 2026-04-21 | V4 chip groups per team, 2×2 toggle block, HeatmapCanvas optional `visibility` prop. Brief 3. |
| 39 | Scout score sheet — role-gated match summary | 2026-04-21 | `ScoutScoreSheet` for pure scouts; existing CoachingStats for admin/coach. Brief 2 (G3+G4). |
| 38 | Security Role System + View Switcher (v2) | 2026-04-17 (v1) + 2026-04-20 (v2) | 5 roles ARRAY per user, PBleagues ID mandatory onboarding, admin approval gate. Path A chosen. |
| 37 | Documentation discipline | 2026-04-20 | Where decisions live, CC brief lifecycle, chat-is-not-SoT rule |
| 36 | Adaptive picker thresholds | 2026-04-20 | Breakout < 5, shots < 20, weighted hit=2/miss=1/unknown=0.5 |
| 35 | Player Self-Report UI patterns | 2026-04-20 | Two-tier model, FAB, bootstrap collapse, cycle-tap shots, outcome colors, shared variants |
| 34 | Field Side Representation Standard | 2026-04-18 | `SideTag` canonical component, `COLORS.side.*`, terminology (dorito/snake/center) |
| 35.5 / 35.7 | Player Self-Report outcome states + two-product scope clarifier | 2026-04-22 (rewritten for PPT) | § 35.5 rewrite: 3-state outcome enum (alive / elim_break / elim_midgame). § 35.7: clarifies § 35 patterns apply to BOTH HotSheet (Tier 1) AND PPT. Brief PPT_MASTER Checkpoint 1 docs. |
| 33 | User Accounts + Scout Ranking | 2026-04-15 | Email/password auth, scout leaderboard. **Sub-sections 33.1 (user.role deprecation) + 33.2 (canonical workspace slug shape) added 2026-04-22 by Brief G Option B.** Further extended by § 49 (2026-04-23) — new `users/{uid}.roles` plural bootstrap + `defaultWorkspace` pointer; `/users/{uid}.role` singular stays deprecated. |
| 27 | **Apple HIG Compliance — MANDATORY** | 2026-04 | Touch 44px, elevation, amber = interactive only, anti-patterns. MUST READ before any UI work |
| 18 | Concurrent scouting (claim system) — **DEPRECATED 2026-04-22** | (original April 2026) | Superseded by §42-§44. Section kept for historical context; Brief F retired the implementation (diagnostic logs + claim writes removed). |
| 5.7 | Tournament setup polish — multi-division + Add teams multi-select + single Add-team XOR | 2026-04-22 | Brief A. Data model always supported array; this fixed UI single-select bug + batch add UX + CTA dedup. |

Sections 1–26 + 28–32 cover foundational decisions (canvas, match page, tournament page, teams page, data model, delete pattern, layout wizard, ballistics, coaching stats, point summary cards, obstacle shots, player stats, HERO rank, scout/coach toggle, coach team summary redesign, obstacle play shots, metric formulas, bottom tab navigation, training mode). Full list at `grep '^## [0-9]' docs/DESIGN_DECISIONS.md`.

---

## 🔄 Update protocol

**At the end of every Opus chat** (or Claude Code session that changes direction):

1. Patch **this file** with changes in:
   - "Currently in flight" (new branch? complete?)
   - "Recently shipped" (add row to table if something deployed)
   - "Next on deck" (promotions / demotions / new tasks)
   - "Awaiting decision" (add new blockers / mark resolved)
   - "Recent design decisions" (add row if §N+1 appended to DESIGN_DECISIONS)

2. Bump **Last updated** date + author + **Main HEAD at last update**.

3. Commit: `chore(docs): update HANDOVER.md — <summary of changes>`

4. If Opus chat generated a new patch for `DESIGN_DECISIONS.md` or `PROJECT_GUIDELINES.md`, ship that patch in the same or adjacent commit, before the chat ends (per § 37.2 / § 10.3 chat-is-not-SoT rule).

5. **Proactive patching** — if Opus produced design decisions in a *previous* chat that never made it to repo (as happened with § 38, decided 2026-04-17 but codified 2026-04-20), the first task on opening the next CC session is to transfer them. Chat is reasoning archive; repo is source of truth. Delays compound — get it into the repo as soon as you notice the gap.

**Rule of thumb:** this file should be the first thing a fresh Opus chat reads to get situational awareness. If it's stale, fix it before asking Opus anything strategic.
