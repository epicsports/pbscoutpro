# Session Handover — PbScoutPro

> **Purpose:** Living state-of-the-project for Opus chats (architect / strategy sessions). Read this before drafting any CC brief or making decisions about direction.

> **⏸ RESUME AFTER PC RESET — 2026-07-02 (main HEAD `2c4e46a3`, CI deploying at wrap).** On restart do CLAUDE.md "On open" (read BACKLOG, git log, **scan `_inbox/`**), then:
> 1. **RE-ARM the hourly `_inbox` check** — the cron (`3241f2db`, hourly at :17) is **session-only** and dies on reset. Recreate it (CronCreate, prompt = scan+process `_inbox/` per §CD-DRAFT, silent if nothing new). Jacek open Q: make it `durable`? (survives resets, writes `.claude/scheduled_tasks.json`).
> 2. **CD channel = `DesignSync` tool (LIVE).** The "direct CD channel" is the claude.ai/design MCP — CC reads/writes the `reads ⊖ Design System` project (`2cebcdd5-9bac-4f54-9f6c-75805d30759f`) directly (`DesignSync` methods). CD's files live there, not `_inbox/`. This session pulled CD briefs from it + wrote back `_CC_RECONCILE_tactical.md` + `_CC_REQUEST_tactical_portrait.md`. `_inbox/` still works as a fallback.
> 3. **🧵 ACTIVE WORKSTREAM = TACTICAL REWORK (tactics UNFROZEN — Jacek: "nikt z tego nie korzysta").** New `DrawingCanvas` engine (`src/components/tactical/DrawingCanvas.jsx`) + Coach Taktyki screen (`TacticsCanvasPage`, route `/layout/:id/tactics-canvas`) — **on prod (beta)**. Reach: Coach tab → tournament → team → under "Analizuj przeciwnika" → tactics button (newEngine). Legacy board untouched (direct URL only). **Decisions: `docs/briefs/TACTICAL_REWORK_DECISIONS.md`** (A1-A9/B1-B8). Persist = additive `tacticalElements` field on the tactic doc (phase-tagged typed elements) — legacy `players/shots/...` fields untouched. Done: precision shot (crosshair) · bounce (new) · pin=speech-bubble (new) · editable entry (new) · layers · per-slot player colours · **phases Breakout→Settle→Mid with carry-forward** (players copy from prior phase, shots fresh — scouting `seedStageDraft`) · compact axis. **OPEN/owed:** (a) **purple-flash-on-drag** — CC couldn't repro after dropping the cone fill; Jacek to re-test + say which element. (b) **portrait layout** — no good variant (rail+field side-by-side squishes the field); **design request sent to CD** (`_CC_REQUEST_tactical_portrait.md`), CC waits. (c) **B6 element inspector** (time/colour) — PARKED, CD to design. (d) **A9 field look** — CD to design. (e) Not-yet-in-engine: A2 runners, A3/A4 bumps/bump-shots.
> 4. **EU Pro team logos — DONE + on prod.** 14 AVIF backfilled (`public/team-logos/`, `scripts/logos/`) + `logoUrl` wired **relative** on 13 canonical (ext-preferred) docs `--live` + catalogVersion bump. Padded to `ART_FRAC=0.79` so TeamBadge scale(1.5) sizes them right. **Cloudflare fix-list:** the ~23 PRE-EXISTING `logoUrl` are ABSOLUTE (`epicsports.github.io/pbscoutpro/...`) → migrate to relative before the Cloudflare base-cutover. Not-in-DB (no doc created): Paris Camp Carnage, Section Annecy.
> 5. **Other prod fixes this session:** division tabs added to **CoachWide** (desktop coach was missing them) · **zone-shot picker** (`ZoneTapField`) height-capped (was huge on desktop) · tactics door moved out of the More menu → coach tournament screen. **VS-intro "stopped" = Jacek's computer had Reduce-Motion ON** (not a bug). Draw-chip (scout Rysuj) superseded by the bottom-right reposition (merged `c1997dc7`).
> Full backlog + owner triage: `BACKLOG.md`. Field-cluster otherwise ❄️ frozen (Birmingham 2–5.07); scout capture untouched (guard e2e green each merge). Two prior P0 scout bugs FIXED; team-consolidation ✅, bugfix-red ✅.

> **🧵 ACTIVE WORKSTREAM — "Point as Timeline" (Punkt jako Oś czasu).** Charter: `docs/POINT_AS_TIMELINE.md` (D1–D3 LOCKED 2026-06-02). Current stage = **Stage 2** (phase-spine + end-state, scout-side, additive). Opus writes the Stage 2 build brief next.

> **🔚 SESSION WRAP — 2026-06-26→27 (CC, marathon day + night autonomy; Jacek "go as far as you can"). Main HEAD = `3b82500c`.** Render-first design-sync marathon, then the crest package overnight.
> **Ships (all merged + deployed via CI, newest-first):** **Crest Krok 2 — "Najczęstsze rozbiegi"** (`ad679327`) — `computeBreakoutRuns` (Break-keyframe position → nearest bunker via the reused `findNearestBunker`; count/success/share) + a collapsible table in ScoutedTeamPage. **Data-honest:** shipped the breakout-TARGET version; the literal wireframe "od→do" lane→bunker is deferred (start lane not captured → capture-flow change). Table is in the FULL ScoutedTeamPage, not a separate CoachWide summary panel (lo-fi wireframe simplification). Populated table composition-verified only (fixtures lack breakout positions) — smoke on real data. · **Crest unification Krok 1** (Design System 5, `3b82500c`) — 3-tier team identity (`logo → country flag → color+initials`) in `TeamBadge`/new `CrestBand`; premium catalog rows (tall band + **Oswald** city/nick wordmark, prototype 808-824) + team-detail header + **country form field** + `src/utils/flags.js` (13 inline-SVG flags) + `FONT_COND`/`rdShade`/`rdTint`/`splitTeamName`. Scoreboards/MatchCard keep the small inline `TeamBadge` (approved). · **Tactics-board** rail→LEFT + swipe-right-to-edit (`f460a157`) · **CSV import** pre-selects the active tournament (`cd9ae1f8`) · **field-views-sync** (simplified Preloader everywhere + team logos +50% + a pat-stage25 CI-flake fix that had stuck prod) + tablet-live/opponent universal field-view pattern (earlier in the day).
> **🎓 DURABLE LESSON (reinforced HARD):** **render-verify catches what the e2e gate cannot.** The crest agent shipped a page-crashing missing import (`CrestBand` used in ScoutedTeamPage, never imported → the whole team-detail page crashed to the error boundary) and the e2e gate was **GREEN 110/0 through it** — the scoutedteam specs don't exercise that render path, and `vite build` doesn't catch undefined-component refs. Only screenshotting prod via `tests/e2e/_render-harness.spec.js` found it. Same harness caught that the first crest pass was too conservative vs the prototype → upgraded to the Oswald premium rows + re-verified @834. **Always render the actual screen before declaring a redesign done.**
> **🔴 OPEN / OWED (crest):** (1) **country DATA back-fill** — the flag fallback only shows for logo-less teams with `team.country`; the form sets it going forward but a bulk back-fill needs a team→country **source from Jacek** (the 16 prototype-mapped NXL teams are mostly US but HAVE logos → no visible flag; logo-less teams unknown). (2) **Crest Krok 2** — Coach team-detail data (heatmap + timeline + rozbiegi table + deeper-analysis CTA); wireframe `Coach Team-Detail - Wireframe.html` in the package; needs per-team aggregates redesign; NOT started. (3) **pixel-diff baselines** regenerated `-win32` only (global Oswald webfont shifts ALL baselines; deploy gate `test:e2e` excludes `pixel-diff|model C` so they never gate — consider `-linux` baselines / Oswald preload if the local diff suite matters). **Smoke owed (Jacek, prod):** the crest look on a real logo'd NXL team + the flag once a `country` is set on a logo-less team.

> **🔚 SESSION WRAP — 2026-06-23/24 (CC, long day with Jacek). Main HEAD = `6b2cc7f1`.** Finished the premium redesign, then a core-screens workstream + three production-bug rescues + two durable infra fixes.
> **What shipped (all merged + deploying through CI):** (1) **Premium redesign COMPLETE** — Groups A·B·C + icon sweep + workspace switcher + CSV import (each: branch→diff-review→merge→smoke). (2) **Core-screens workstream** (audit-scoped: MatchCard→ScoutTab→overview→MatchPage) — **#1 MatchCard** (nav contract preserved), **#2 ScoutTabContent + shared `DivisionTabs`** (filter unified to *lenient*, preserving the `c10a282e` no-vanishing fix — Jacek chose lenient over strict to avoid re-breaking it), **#3a StandingsTable** (phone + CoachWide, on the already-computed `computeTeamRecords`), **`matchClassify` util** dedup. (3) **`PAINTBALL_INTELLIGENCE.md`** foundation doc + Europe access-lock note at `sendInviteEmailLink`.
> **🚨 THREE production bugs (user-reported, bug-mode → fixed + shipped):**
> 1. **i18n raw key-names in the UI** (38 `t('k')` keys never defined → rendered as the key, worst on the wide shell; the `t('k')||'fallback'` idiom never fired because the key string is truthy). Registered all 38 pl+en + added **`scripts/lint-i18n-missing.js` precommit guard** so it can't recur.
> 2. **Tablet wide shell broken** — menu dead, event un-switchable, "duplicate menu/settings", no layout. **Root cause:** `AppShellPremiumWide` renders its OWN bodies (ScoutWide/CoachWide) **INSTEAD of `{children}`**; the `NavDrawer`/`TournamentPicker`/modals were nested as `<AppShell>` children → on tablet they **never mounted**. Fix: overlays moved to top-level in MainPage (siblings of AppShell). + sidebar de-dupe + ScoutWide detail pane → real field image.
> **🎓 DURABLE LESSONS:**
> 1. **e2e was silently testing the WRONG APP.** The emulator config's `reuseExistingServer: !CI` made Playwright reuse whatever sat on **:5173** — and another local project ("Parczewscy HQ") held it → every spec loaded a non-PbScoutPro app → spurious "login timeout" failures that masked real signal for the **whole** training + core-screens workstream (we ran CI-gate-only for days because of it). **Fix (`chore/e2e-port-5199`): port→5199 + `reuseExistingServer:false`** (an emulator suite must ALWAYS boot its own seeded server). Local e2e is reliable again — verify locally before merge. Update [[project_deploy_dual_path]] / [[project_emulator_e2e_shared_state]].
> 2. **Wide-shell overlays-as-children pitfall:** any overlay/modal that must work on BOTH shells belongs at MainPage top-level, NOT as an `<AppShell>` child (the wide shell swaps `{children}` for its own bodies). The wide shell has **ZERO e2e** (gated off under `VITE_USE_EMULATOR`) — which is exactly why the tablet bugs reached the user. **Adding wide-viewport e2e coverage is the flagged highest-value next safe item.**
> 3. **The wizard-net pattern generalized:** for a data-critical surface, bank a green e2e net BEFORE touching it. The PPT wizard got a purpose-built `self-log-wizard.spec.js`; MatchPage already had a strong net (`capture-parity` golden + `log-point` + `matchreview-rail` + `concurrent-merge`, 21/21) — confirmed green as the pre-re-skin baseline.
> **🔴 OPEN / OWED:** (1) **MatchPage #4 — CD-GATED.** Readiness signal DONE (net 21/21 green; visual-only behaviour-frozen 2-pass proposal: review block → editor+wide layout). Waits for **CD's ≥720 wide-layout sign-off + Jacek's separate green-light** — do NOT skin until then. (2) **Wide-shell e2e coverage = ZERO** (⭐ highest-value next safe item). (3) Follow-ups: ScoutWide heatmap overlay · `ScheduleList` grouping component · MyProfile/TeamProfile full 2-col wide · layout editor (parked, IA discussion). (4) **Smokes owed (Jacek, prod) — see NEXT_TASKS;** esp. the TABLET set (hard-refresh first) + the wizard HARD smoke (log a point → persist+read-back).

> **🔚 SESSION WRAP — 2026-06-22 (CC, full day with Jacek + Claude Design as the design agent). Main HEAD = `b5159ae1`.** Premium "North Star" redesign push — AND a major infra discovery.
> **🚨 HEADLINE — the deploy was SILENTLY BLOCKED for days.** Prod is served from the **GitHub Actions artifact** (`deploy.yml` → `actions/deploy-pages`), **NOT the `gh-pages` branch** (that branch is vestigial; `npm run deploy` is a NO-OP now). The deploy job is gated on the **emulator e2e** (`playwright.emulator.config.js`), which runs at **Desktop-Chrome 1280px (≥720)**. The moment the wide shell landed it rendered in EVERY spec → red e2e → `build-and-deploy` skipped → **nothing published since the first shell increment** (prod stuck on a pre-shell artifact). Diagnosed by comparing live bundle-hash vs local/gh-pages. **Fix (`ba3f3dc0`):** gate the wide shell off under `VITE_USE_EMULATOR` (set only for the e2e webServer, never prod) → suite renders the mobile shell again (green) → prod ≥720 still gets the wide shell. **Unblocked the whole backlog in one deploy.**
> **🎓 LESSONS (durable):** (1) **The e2e gate at 1280px is the real arbiter — verify affected specs LOCALLY (portable JRE + `npm run test:e2e`) BEFORE merging anything touching render/loading.** Two self-inflicted gate-bounces today (wide-shell-reds-suite; a preloader `useScreenLoader` hook placed AFTER PlayerStatsPage's `if (!player) return` → "rendered more hooks than previous render" crash) would BOTH have been caught pre-merge. (2) **Prod = artifact, not gh-pages branch.** (3) **Cross-agent file bridge:** Claude Design builds the prototype in ITS environment — every new prototype file (`preloader.jsx`, `redesign7.jsx`/forms-wide) must be **physically exported to `Desktop/dk/prototype/`** before CC can port it; there is no auto-sync. Update [[project_deploy_dual_path]].
> **Ships (newest-first, all merged + on prod):** **Team logo** (`b5159ae1`) — real logo back on coach rows (gradient + logo) + a **Logo URL field on the team form** (`TeamFormModal`, §93 external-URL pattern; the form had no logo/color input before). **Scouted-teams list premium** (`CoachTabContent` `06c7e3c5`) — closes the "wyscoutowane drużyny" phone gap (option-E gradient rows + W-L + hide→"Ukryte · N" restore, shared `reads.coach.hiddenTeams` key with the wide CoachWide). **Premium determinate Preloader** (`9fe55833`) — 1:1 port of `prototype/preloader.jsx` (coach tactical stroke + target reticle, determinate bar + tabular %, real-completion snap, `useDeferredOn` anti-flicker) wired into opponent/player-stats/ranking. **Wide shell increments 1-3** — `AppShellPremiumWide` (sidebar + master-detail) dispatched at `useDevice().width>=720`, mobile byte-identical: ScoutWide (matches→tournament scouting) · CoachWide (teams master-detail + hide) · PlayerWide (dashboard hub) — all on REAL app hooks/routes, one nav. **Design-review pass** — §27 amber removed from structural eyebrows (both screens); hero card hardened (initials fallback, no-`#?`, 2-line surname); RdGaugeCards volume-rank + survival-semantic ring. **Player-stats premium redesign** + the 5 data-viz primitives. **Delete-player "confirm does nothing"** fix (one-shot catalog cache → optimistic removal).
> **🔴 OPEN / OWED:** (1) **Redesign GAP AUDIT (verified) — back half of the redesign is NOT wired:** opponent analysis full re-skin (`OpponentAnalysisWide` — only chrome shipped) · scout ranking · today's points/PPT · my-profile · team-profile · point-logging wizard · match cards · players/teams lists · hitability (partial). CD built `*Wide` variants in `redesign6.jsx` for several — NONE wired except the 3 shell tab bodies. (2) **forms-wide BLOCKED** — `redesign7.jsx` (NewPlayerWide/NewTeamWide) NOT on disk → needs CD export. (3) **Wide-shell e2e coverage = ZERO** (gated off in the suite) — add a wide-viewport Playwright project + specs. (4) **Preloader true per-stage stepping** — all 3 screens expose a single loading boolean → creep-and-snap; promote `heatmapLoading` to a fetch-done→compute-done signal for real stepping. (5) **Smokes owed (Jacek, prod):** wide shell (tablet/desktop ≥720) · scouted-teams + team logo · preloader cold-load. **Env:** portable Temurin JRE 21 at `%LOCALAPPDATA%\jre-temurin` (emulator e2e runnable — use it pre-merge). **Next gap to take:** opponent/coach-summary full re-skin (`ScoutedTeamPage` body / `OpponentAnalysisWide`).

> **🔚 SESSION WRAP — 2026-06-16→17 (CC, night autonomy; "Go for anything waiting on go and continue" → morning). Main HEAD = `bd0b2961`.** Three GO-gated features shipped, all merged + deployed + rules-deployed + closed out (DEPLOY_LOG/NEXT_TASKS newest-first), prod-verified live (site 200 + Reads Mini chunk fetches 200). **Headline lesson (reinforced): run the e2e fail-first BEFORE merge — it earns its keep.** The Packing run (denied earlier, released by "go") crashed on first execution and surfaced **3 real bugs** that build + precommit had passed clean: (1) the canonical `t()` (hooks/useLanguage.jsx) **auto-invokes function keys**, so the shape is `t('key', ...args)` — `t('key')(...)` throws and blanks the screen; (2) shared **`Btn` silently dropped `data-testid`/`aria-label`** (no `...rest`) → added `testId`/`ariaLabel` props (file's existing convention; additive, zero-impact); (3) `FONT_SIZE.md` is undefined in theme.
> **Ships (newest-first):** **Reads Mini "Take a Break"** STAGE 2 (`186071e6`) — Game&Watch catch game (pure DOM/SVG, lazy chunk) + global arcade leaderboard `leaderboards/readsMini/scores/{uid}` (Spark, anti-cheat rules deployed: own-doc/int 0–9999/`[A-Z]{3}`/monotonic/5s-cooldown/no-delete; §117.6 cross-ws read + arcade-initials by design); entry at the very bottom of BOTH More drawers (shared `TakeABreakSection`); e2e covers UI flow + leaderboard write vs REAL rules incl. cross-uid denial (bridge dead-eliminated from prod); gate 84/84. **Packing Checklist** (`a73a7744`) — per-user `users/{uid}/appState/packing` (degrade-to-memory), owner-only rule deployed. **`src/data/` lint-skip** (`30fa3996`). Board reconciled (view-as/Playbooks marked done; stale view-as-broken + drawLineFromTo items retired — the helper already exists + is used by all 3 sites).
> **🔴 Held / owed when you're back:** (1) **tactics consolidation** — NOT done autonomously by design (its brief is labelled "NOT night-autonomous" + it's a `--live DELETE` of 24 prod docs = Hard-ESCALATE). Needs you present; backup-first + fresh per-doc id-coverage read right before the delete. (2) **Reads Mini audio asset** — drop `public/sounds/sky-catcher-loop60.m4a` (60s AAC loop); bg music is silent until then, SFX already work. (3) **Reads Mini STAGE 3 App Check** = separate GO. **Smokes owed (phone):** Reads Mini (drawer→bottom→"Take a Break"→Game A) + Packing (drawer→KONTO→Checklista). Carried from prior wraps: §93 logo iPhone bisect; ci-flake `hitability-responsive`; loupe pan-lag.

> **🔚 SESSION WRAP — 2026-06-15→16 (CC, full day + night autonomy). Main HEAD = `191439d0`.** Big multi-arc run; all ships merged+deployed+closed out (DEPLOY_LOG/NEXT_TASKS newest-first). Jacek mid-session: "approve all file changes, stop asking" + night autonomy. **Headline lesson (recurring): stranded users despite correct data ≠ logic bug — verify the actual store + deployed bundle first.** The "Maks can't get in / can't save / re-invite errors" reports each resolved to a DIFFERENT precise cause found by admin-SDK reads + fail-first e2e (never the first guess), and Jacek's own hypotheses (gate-skips-self-claim; email_verified blocker) were overturned by the data twice.
> **Ships (newest-first):** source-of-truth entry fix (`userRoles` authoritative for ENTRY not just the gate — `4ddbf0b2`); durable **email-link invite onboarding** (express-reg + email-keyed self-claim; verified-email tenant-isolation rules CONFIRM+deployed — `a8ed9cad`); already-member self-claim (`b81fc558`); createEmailInvite idempotent re-send (`7f4a0f40`); player self-edit catalog-bump best-effort (`db8d4fc2`); **ITEM-1 drawing-tools unify** (TacticPage → shared DrawingOverlay/DrawToolbar — `4ae31cfc`); tactics `freehandStrokes` drift fix (`96809879`); Polish-lint refine (`fefcbc7c`). Recovery: Maks + 3 stranded accounts stamped (admin-SDK `--live`, GO'd).
> **Discoveries (read-only → Opus/Jacek):** **tactics consolidation** (`docs/architecture/TACTICS_TWO_STORE_DISCOVERY.md`) — it's a **THREE-store** situation: `layoutOverlays/.../tactics` (21, live) · `tournaments/.../tactics` (9, live) · `layouts/{id}/tactics` (24, **NO reader → orphaned legacy**). Full consolidation = **Tier-2** (`--live` migrate the 9 + cleanup the 24; § 97.5 = retire the tournament store). i18n triage: lint was 94% noise (now fixed); ~63 genuine hardcoded-PL but judgment-heavy (domain-data + log-message traps) → attended batch.
> **🔴 Decisions OWED (block further autonomy):** (1) **arc-B width model A/B/C** — the single biggest item, gated on Jacek's desktop-width aesthetic pick (A tier-everywhere / B responsive-min / C desktop-only-cap); CC will NOT pick the visual model unattended. (2) **tactics consolidation** = Tier-2, needs an Opus brief + GO (9-doc migrate + 24-doc cleanup). (3) **ITEM-2 folded-rail** = parked (needs phase-view ratified + mockup). **Smokes owed:** ITEM-1 (existing tactic drawing under perfect-freehand) · email-invite end-to-end (fresh email, diff browser) · Maks/Tymek/biuro relogin · player profile save · tactic-duplicate keeps drawing.

> **🔚 SESSION WRAP — 2026-06-09 (CC, full day; "wrap up for tonight"). Main HEAD = `432d2483`.** Six ships + two discoveries; all ships merged + deployed + closed out (DEPLOY_LOG/STATE newest-first). **Headline:** first run of the new **faithful-mockup loop** (design gate = approved HTML mockup + environment gate = real-phone preview) — it shipped §113 cleanly after the WIP first got the landscape rule backwards (fixed-rail-with-field-remainder → corrected to field-100%-hero + residual rail). Lesson reinforced: a "list is empty / nothing saved" report is usually **broken-feedback, not broken-logic** — admin-SDK reads proved both the league (l_cdf) and the import data were written; the UI just didn't refetch/filter ([[feedback_button_does_nothing_diagnosis]]).
> **Ships (newest-first):**
> - **§113 Hitability responsive Canvas/Tool archetype** (`9f46c322`) — first responsive-canvas pilot. Portrait-lock removed; **landscape rule: field = 100%-height HERO (CSS aspect-ratio → width), rail = RESIDUAL (`flex:1`, min-width railMin), field yields only under pressure** — NOT a fixed-width rail. Reusable **`CanvasRailLayout`** primitive (Report+Canvas screens reuse next). §81 aligned (canvas-primary). Coordinate guardrail preserved across reflow → **tap-after-rotate maps correctly** (fail-first e2e #7 `hitability-responsive.spec`, verified fails-with-gate/passes-with-redesign on the emulator). **NEW preview-deploy mechanism:** `VITE_PREVIEW=1` → base `/pbscoutpro/preview/` + SW-off → `gh-pages --dest preview --add`. DESIGN_DECISIONS §113.
> - **Faithful-mockup loop = standard** (`6e88394c`) — PROJECT_GUIDELINES **§12**: real-screen snapshot → Opus mockup → Jacek design gate → build + fail-first test → real-phone preview (environment gate) → merge. Neither gate alone is sufficient.
> - **Admin leagues list-refresh** (`79f039e9`) — `useAllLeagues` one-shot getDocs never refetched in-session → a created league looked unsaved (it was). Fix: `refreshLeagues()` pub/sub after create/edit/(de)activate. (NOT rules — leagues write = `isSuperAdmin()` ⊇ bootstrap email.)
> - **Schedule import — league-aware divisions** (`9585207f`) — `resolveScheduleDivision(raw, leagueDivisions)`: decorated-export alias map (NXL) THEN match the selected league's `/leagues` divisions by name/id, case+diacritic-insensitive. Any new league imports without hand-added aliases (fixes CDF "Premiere").
> - **Schedule import — flexible/optional dates** (`35e30e11`) — multi-format `parseScheduleDateTime` (long-EN, ISO, DD/MM[/YYYY], US MM/DD fallback) + tolerant of no date; undated matches order by a `#`/`index`/`lp` column (else row order) via `gameNumber`; `addMatch` respects explicit `date:null`.
> - **Schedule import — team matching by name + full catalog** (`c33abbc5`) — the "Dopasuj" list was empty (only league-tagged teams). Now offers ALL global teams; name-match pre-selects an existing profile; confirming adds this league's division to it (`updateTeam` merge, best-effort try/caught). Dedup onto authorized globals.
> **Discoveries (read-only → Opus/Jacek):** **archetype-model validation** (folded to main `432d2483`, `docs/ARCHETYPE_MODEL_DISCOVERY.md`) — 5 archetypes hold + one gap ("Report + promotable Canvas section"); `<Screen>` shell justified (~20 screens converge). **`<Screen>` base-shell pilot STEP 0** (snapshot on branch `docs/screen-pilot-step0`; checkpoint returned) — pilot = **ScoutDetailPage**; recommended API: `bg` + maxWidth default = tier `responsive()` + **override (ScoutDetail = 720 reading-width)** + paddingBottom **80** safe-area + **document-scroll + sticky PageHeader** + folded PageHeader; landscape reflow = **2-col sections** (not maximize+rail — plain Detail). **AWAITING Opus mockup → Jacek design gate → build.**
> **🔴 Open / owed when you're back:** (1) **smokes** on all 6 ships (Hitability already phone-smoked at merge; the 4 import fixes + the leagues fix want a quick prod smoke — re-import CDF #1 2026 exercises 3 of them at once); (2) the **`<Screen>` mockup→approve→build loop** (ScoutDetailPage, then ScoutIssuesPage parallel proof, then the ~20-screen hex migration); (3) **name-match pre-select UX call** — currently batch-confirm (pre-selected), revertable to per-team yes/no if Jacek prefers; (4) CDF "Francais Paintball" / l_cdf is a **real active league** now (keep or delete — say so); (5) carried: DRY `drawLineFromTo`, cc76f9ad global-version-bump frequency (now also touched by the team-tag write), apply `CanvasRailLayout` to the Report+Canvas screens. **Env:** portable Temurin JRE 21 at `%LOCALAPPDATA%\jre-temurin` (emulator e2e gate runnable). **Reference branches (not merged):** `audit/style-audit` (STYLE_AUDIT), `docs/hitability-step0` + `docs/screen-pilot-step0` (mockup snapshots).

> **🔚 SESSION WRAP — 2026-06-05/06 (CC, full day; "finalizujemy, idę spać"). Main HEAD = `223192a7`.** Big day — full §90 cutover end-to-end + 2 prod-bug fixes + PaT Stage 3 + a canvas refactor + 3 discoveries. All merged + deployed + closed out (DEPLOY_LOG/STATE newest-first). **Headline lesson:** the §90 "twins dep-free" audits were READ-only and missed live WRITE-path workspace accessors **three times** (`deletePlayer`, `removeMember`, `addTeam`) — two were live prod breaks caught by the emulator e2e. Memory saved: [[feedback_dep_audit_check_writes_not_just_reads]].
> **Ships (newest-first):**
> - **AnalyticsCanvas extraction** (`d61fa157`, 2026-06-06) — LayoutAnalyticsPage bespoke `<canvas>` → BaseCanvas child (`AnalyticsCanvas`+`drawAnalyticsField`); last ×2-DPR/ResizeObserver/getBoundingClientRect gone (only FieldCanvas/Ballistics ×2 left). **Owed: visual smoke `/analytics`.**
> - **§90 dead-code sweep** (`fece6b36`) — removed dead `deleteTeam` + `bp()/layouts` CRUD → **zero src refs to any workspace twin path** (§90 code side fully retired).
> - **Team color fix** (`fdd6fd13`) — `updateTeam` now bumps catalogVersion (color edits were invisible behind the 30d cache) + optimistic feedback + palette 12→30. **Latent twin: `updatePlayer` has the SAME no-version-bump bug** (player name/number edits invisible up to 30d) — Jacek to decide if the one-liner ships. **Owed: color smoke.**
> - **§90 prod-regression hotfix** (`cca4b586`) — the 2B/3 rule removal broke `removeMember` (self-leave/remove) + `addTeam` (team-create) which still READ/WROTE the ruleless twins → repointed to global; `migrateWorkspaceRoles`/`mergePlayers` too. Caught by e2e `account-leave`. **Self-leave + team-create were briefly broken in prod today; restored.**
> - **PaT Stage 3** (`1fe4fe56`) — `mergeStreamTimelines` carries `timeline[]` (Settle/Mid keyframes) through the two-side end-match merge (per-side union, not consensus); Stage 2.5 reader confirmed already shipped. **Owed: 2-device smoke.**
> - **B24 player mojibake** (`717be17a`) — `repairMojibake` import guard (player+nickname) + `--live` repair of 16 ranger1996 catalog names (0 remaining; backup). FIT unaffected.
> - **§90 Stage 2B.3** (`d60f8390`) — super_admin-only hard player delete; players twin rule dropped → §90 cutover DONE (detail in the entry below).
> **Discoveries (read-only, → Opus/Jacek decide):** PaT **Stage 4** typed-move design folded into the charter (`176a0be7`, Model A LOCKED; build PARKED on 2 gates — vocab→FIT, bump-typing→Jacek). **B24-teams** = 0 teams affected (all-ASCII names) → no repair needed. **AnalyticsCanvas** discovery → the build above.
> **🔴 Open / owed when you're back:** (1) the 6 owed smokes above; (2) `updatePlayer` version-bump decision; (3) the 5 residual "ghost" accounts = your own test/alt accounts (`testowy@`/`testowyfit2@`/`jacek2@`/`info@epicsports.pl`/`jacek.parczewski@cloudity.com`) — say which, if any, to delete; (4) §98 legacy zone-field drop still deferred (prod-smoke owed); (5) PaT Stage 2.5/Stage 4 vocab awaits FIT. **Env:** portable Temurin JRE 21 reinstalled at `%LOCALAPPDATA%\jre-temurin` (the emulator e2e gate is runnable again — it earned its keep today).

> **🔚 §90 STAGE 2B/3 EXECUTED + FULLY CLOSED — 2026-06-05 (CC session, Jacek in-session CONFIRM; ran the Monday window early). Main HEAD = `d60f8390`.** The destructive twin-decommission window ran today instead of waiting for Mon 8 Jun. Sequence: autonomous prep (single `--dry` + LOCAL PII backups, STOP) → CONFIRM → drop twin-writes (`mirrorTwin` + 4 callsites + retire/unretire/**setParentTeam** direct writes; `840b2fbc`) → app deploy (stops twin regen) → `--live` delete **2,938 docs** (2,634 player + 299 team twins + 5 ranger1996 `/layouts` stragglers; drift +0, 0 remaining) → remove **teams + layouts** twin rule blocks + deploy (`d9a020df`). Backups: `…/dk/pbscoutpro_backups_90_2b3/2026-06-05T13-55-25-110Z/` (outside repo, NOT Drive — restorable). Tools: `phase_90_2b3_dry_backup.cjs` + `phase_90_2b3_live_delete.cjs`.
> - **✅ Stage 2B.3 CLOSED (`d60f8390`) — players twin rule block dropped.** The 2A "twins dep-free" audit was READ-only and missed a live WRITE-dep: `deletePlayer` (PlayersPage roster delete) wrote the workspace players twin. Jacek decision (b)+"deleted = deleted from DB": player delete is now **super_admin-only + hard global** (`deletePlayerGlobal`; gated via `useIsSuperAdmin`, repointed off the twin, confirm copy = permanent/global). Last twin writer gone → players twin rule block dropped + deployed (global delete rule unchanged, `isSuperAdmin()`-only; §85 carve-outs served by the global block). **§90 catalog-isolation cutover DONE. Lesson for future "dep-free" claims: audit WRITES too, not just reads.**
> - **Ghost cleanup DROPPED** — fresh `--dry` found **5, not ~566** ghost accounts (B15 + prior cleanup already cleared the auto-join ghosts; Auth = 23 total). The 5 residuals are Jacek's own test/alt accounts (`testowy@`/`testowyfit2@`/`jacek2@`/`info@epicsports.pl`/`jacek.parczewski@cloudity.com`) — NOT bug-ghosts; untouched pending per-account say-so.
> - **§98 legacy zone-field drop DEFERRED** — §98 prod-smoke still owed → not "prod-confirmed"; per the brief's conditional gate, this ride-along piece is deferred only (did not block the FIT-isolation core).
> - **Dead-code follow-up:** `deleteTeam` + `subscribeLayouts`/`addLayout`/`updateLayout`/`deleteLayout` are now unreachable (removable in a later cleanup, alongside the players-block decision).

> **🔚 SESSION WRAP — 2026-06-05 (CC session, full day). Main HEAD = `6a929fcd`.** Four ships, all merged + deployed + closed out (DEPLOY_LOG/STATE/NEXT_TASKS newest-first):
> - **Read-volume C Stage 2** (`73aba833`) — tenant-isolation CG scoping + rules deployed (detail below).
> - **B4 — Settings is never a cold-open landing** (`0c4852a2`) — don't persist `'more'` + AppShell once-on-mount guard → lands on role's content view; viewer-only stays on Settings. e2e 21/21. DEFERRED remainder: role-aware dashboard + `NoTournamentEmptyState` copy (needs mockup).
> - **Layout bunker-naming re-key** (`5c578ba6`) — per-workspace display-names re-keyed off `positionName` → `masterId||id` stable identity (fixes two same-named obstacles colliding on rename, e.g. UK "Dykta"); lazy overlay-only migration, no global `/layouts` write; `utils/bunkerNames.js`. **OWED JACEK:** base-rename UK layout `XtQQKhVIegdTylygsbVX` bunker [0] "Dykta"→"Palma" (safe anytime; recoverable via overlay legacy `bunkerNames["b_…ix3m"]="Palma"`) + naming smoke.
> - **Claim flow Phase 1b — cold-review self-log (W4)** (`b1f6e7d4`) — pick an assigned scouted point → self-log after the fact (matcher/propagator-free). Write-model fork RESOLVED = W4. Picker = decision A′ (events_index 30d → read-volume-C rollup-hybrid → live-`selfLogs` freshness; NO collectionGroup('points') scan — Option A would've re-opened point-level isolation). Entry CTA quiet at N=0 on own PlayerStatsPage. `ColdReviewFlow.jsx` + `fetchColdReviewCandidates`; spec in `PLAYER_SELFLOG.md`. Sparse-assignment accepted as the 1b boundary (claiming un-assigned points = future phase). **OWED JACEK smoke.**
> - **§90 cutover reconciled (no code):** Stages 1 (1.1 `7fa7e780` + 1.2/1.3 folded into read-volume C) + 2A (`ec5a008c`) ALL SHIPPED; teams twin confirmed dep-free. **Only DESTRUCTIVE Stage 2B + 3 remain — SCHEDULED for the MONDAY 8 JUNE 2026 window + live CONFIRM.** Fresh `--dry` (2026-06-05): players-twin **2,634** · teams-twin **299** · breakoutVariants-twin-remnant **0** · /layouts stragglers **5**. Monday session: run "§90 Stage 2B/3 prep" → `--dry` re-enumerate + JSON backup + readiness report → STOP for Jacek CONFIRM before 2B.1 drop mirrorTwin (`dataService.js:162`+callsites) / 2B.2 `--live` delete twins / 2B.3 remove twin rule blocks (`firestore.rules` players :327 / teams :437, NOT global :614/:674) / 3.1 delete /layouts stragglers / 3.2 remove rule. (A session-only cron was set for Mon 08:57 but is `[session-only]` — won't survive the PC restart; THIS doc is the durable reminder.)
> - **Env note:** portable Temurin JRE 21 at `%LOCALAPPDATA%\jre-temurin` enables the emulator e2e gate (no system Java); re-download from Adoptium if a PC wipe removes it (see [[project-emulator-needs-portable-jre]]).
>
> **── Read-volume C Stage 2 detail ──**
> **Read-volume C Stage 2 — tenant-isolation CG scoping — SHIPPED + rules DEPLOYED** (Jacek CONFIRM'd; ruleset `d5242ec7`, 2026-06-05 08:34Z). Closes the cross-tenant `request.auth != null` read leak on the raw `selfReports`/`shots` collectionGroups — **folds §90 1.2/1.3 + §94 #3 + Spark ladder #5**, the GATE before FIT multi-tenant selfLog go-live.
> - **Corrected for "rules-are-not-filters":** a CG rule keyed on `resource.data.X` rejects any query not constraining X. So every CG consumer now adds the filter: PPT `getEventShotFrequencies`/`getTrainingSelfReports` + `where(workspaceSlug)`; `fetchSelfLogShotsForPlayer` → `where(workspaceSlug, tournamentId)` (playerId+source client-side, parity-identical); `usePlayerBreakoutHistory` → `where(playerLinkedUid == own uid)` (self-only). Composite indexes Enabled (`shots(workspaceSlug,tournamentId)`, `selfReports(workspaceSlug,trainingId)`, `shots.playerLinkedUid` CG override).
> - **Rules:** `selfReports` CG `read if isMember(resource.data.workspaceSlug)`; new `shots` CG `read if isMember(workspaceSlug) || (authed && resource.data.get('playerLinkedUid',null)==auth.uid)`; `/layoutAggregates` + `write if authed` (2.4 shared-write pool — increment-only enforced client-side on nested maps, fully recomputable). The `/{path=**}/` rules authorize the *CG query path*; per-doc nested rules already gate `isMember(slug)` → OR-semantics never loosens isolation.
> - **Load-bearing emulator gate GREEN:** new `tests/e2e/cg-isolation.spec.js` (2 tenants seeded — `demo-ws`/`other-ws`) proves the 8-cell matrix against the REAL rules: member reads own CGs / denied cross-tenant; other-tenant player reads OWN self-log shots via carve-out / denied a cross-tenant sweep; layoutAggregates shared-write OK. **Full e2e 20/20, no regression.** Live ruleset verified byte-for-byte == branch. (Env: installed a portable Temurin JRE 21 to `%LOCALAPPDATA%\jre-temurin` — no admin/machine change; the Firestore emulator needs Java; outside the repo, ignorable.)
> - **Read-volume C Stages 1–2 COMPLETE.** Optional Stage 3 = IndexedDB result-cache. **Owed: Jacek smoke** when selfLog enabled (PPT/coach shot-frequency + player breakout-history parity; cross-tenant denial).
> - **Still scheduled — §90 Stage 2B + Stage 3 (DESTRUCTIVE twin decommission): MONDAY 8 JUNE 2026 window + live CONFIRM** (drop `mirrorTwin` → JSON backup → `--live` delete ~2,634 player + ~299 team twins → remove rule blocks → `/layouts` straggler cleanup). Unaffected by today's work.

> **🔚 SESSION CLOSE — 2026-05-29 (Opus session). Main HEAD = `61c63347`.** **Morning: Opus brief incoming** (Jacek will pass it). Day was Phase 2.2.d + workflow/doc reconciliation — all doc-or-Stage-1, no risky prod beyond one additive backfill. Newest-first:
> - **§ 90.12 data-model split + revised cutover scope** (`61c63347`, doc). Global (player/team catalog **incl. team membership** + layout-technical config + tournament schedule) = **workspace-read-only / super_admin-write**; workspace-private = layout overlay (naming/zones/tactics) + scouting. **Isolation = WHO-can-write (a server-side rules READ predicate), not where a field lives → Stage 6 (thin doc) is OFF the FIT critical path.** § 90.10's 2↔6 coupling is superseded. **Cutover trigger:** the FIRST tenant doing real private scouting (NOT FIT's end-June read-mostly roster). **2 audits owed before a date can be set:** (a) scouting-data isolation map; (b) layout-overlay current shape (naming/zones/tactics on the global layout doc, or separate?). → those are the natural next read-only discovery items.
> - **§ 90.11 durable principles** (`71afe3bd`, doc): N-tenant (route by `ownerWorkspaceId`, never hard-code count) · cost-first (merged reader's ~3242/session catalog load = #1 read-audit target, run post-cutover) · identity≠membership (multi-team handled via `teams[]`, multi-**role** is the real gap → Stage 6).
> - **§ 90.10 isolation cutover DEFERRED** to the production-version push (`071cfa36`, doc). **Stage 2 was STOOD DOWN — no code written** (escalated pre-code).
> - **Lost-&-regressed audit** (read-only; findings in chat, NOT committed, nothing promoted). Top recovery candidate = **PWA offline = HALF-BUILT** (highest live-tournament risk): SW (`public/sw.js`) caches only the app shell + skips cross-origin, so **Firestore data is un-cached → the app opens offline but can't scout**; B21 root cause = `main.jsx:18` registers `sw.js` with **no `.catch()` / no scope** (silent reject). Recommend raising B21 above "low" + enabling Firestore IndexedDB persistence. Also: review suite REGRESSED (Vision reviewers `scripts/reviewers/*` dormant since their only commit 2026-04-03, `design-contract.js` dead, "REVIEW SUITE" section gone from CLAUDE.md; precommit+lint-ui survive). Orphans: `ModeTabBar.jsx`, `FeatureGate.jsx`. 1-min win: `loading="lazy"` on `LayoutsPage.jsx:51`. ~10 SHIPPED-SINCE items still clutter IDEAS_BACKLOG + 3 stale § 12 lines. **`useUndo` is USED (not orphaned)** and onboarding/empty-states are HEALTHY — two brief premises corrected.
> - **§ 90 Phase 2.2.d STAGE 1 SHIPPED + deployed** (`33b0d453` merge / `f51ff203` closeout). Merged `global ∪ /workspaces/{activeWs}/{players|teams}` readers (dedup + `pbliId` class-preference) + `findPlayerByPbliId`→global + **42 ws-only pbliId backfill** (additive `--live`, global 3200→3242, ws-only 0). **OWED: prod UI smoke once the Firestore daily read-quota resets** (rosters/pickers show no dup/missing players) — quota was exhausted today by admin-SDK scans + the backfill (time-gate, resets daily).
> - **Workflow doc reconciliation** (`46744758`, doc). CLAUDE.md now: multi-agent model (CC=implementer / Opus=architect / Jacek=final GO; **no merge/deploy — incl. doc-only — without GO**), canonical **Firebase-autonomy policy** (reads/`--dry`/indexes free; `--live`+rules = task-GO'd; **tenant-isolation rules predicate → explicit in-brief CONFIRM**), and an Opus session protocol with a connector-unavailable HEAD fallback. The catalog-data-isolation item was merged into the Phase 2.2.d NEXT_TASKS row (same structural fix).

> **⚠️ POST-RESET — read `docs/ops/RESUME_AFTER_RESET.md` FIRST (2026-05-28).** A PC reset was planned. Things git won't restore: `.env` (Firebase web config, gitignored), the admin SA key (`dk/…adminsdk…json`), and `~/.claude` memory — back these up or recreate after the wipe. **Live-app config issue RESOLVED** this session (deploys had shipped placeholder Firebase config; refetched real config via `firebase apps:sdkconfig WEB`, wrote `.env`, redeployed — app works). After a wipe, recreate `.env` (fastest: `firebase apps:sdkconfig WEB --project pbscoutpro` with the SA key) before any local deploy. Full details in that doc.

> **🆕 SHIP — 2026-05-28 (§ 90.7.3 legacy selfReports path RETIRED from code + rules).** **Main HEAD = `91caf489`** (merge of `chore/retire-legacy-selfreports-path` / `9a757e49`). App deployed (`npm run deploy` Published) + rules deployed (`firebase deploy --only firestore:rules` — compiled + released, run directly by CC via the SA key). With the legacy docs gone (§ 90.7.2): PPT per-player readers (`getTodaysSelfReports`/`getSelfReportsForPlayer`/`getPlayerBreakoutFrequencies`) now flat-only (1 query not 2); `dedupePreferFlat` removed (3 collectionGroup readers back to plain iteration); the dead nested `/players/{pid}/selfReports/` rules block deleted. **§ 90 Stage 1 fully closed** — legacy selfReports path retired end-to-end. No data change. **Next real track: Phase 2.2.d** (workspace-local players/teams relocation — FIT isolation) — gets its own Opus brief/discovery.

> **🆕 DONE — 2026-05-28 (§ 90.7.2 legacy selfReports cleanup — nested path EMPTY).** One-shot delete script `scripts/migration/phase2_stage1_legacy_selfreports_cleanup.cjs` (CG-optimized, per-doc twin check, orphan hard-stop) **executed live: 53 scanned / 53 twinned / 0 orphans / 53 deleted / 0 remaining** (flat intact at 53; idempotent). Legacy nested `/players/{pid}/selfReports/` is now EMPTY → **Phase 2.2.d unblocked**. **Workflow change:** CC now runs Firebase directly (firebase-admin SA key at `dk/pbscoutpro-firebase-adminsdk-fbsvc-*.json` + firebase CLI 15.18 + project `pbscoutpro`); Jacek granted **full autonomy on the active task** (CC runs `--dry`→`--live`/deploy back-to-back, gating on the dry output). No more paste-back. Low-pri follow-up: legacy rules block + `dedupePreferFlat` shim now removable (deferred).

> **🆕 SHIP — 2026-05-28 (§ 90.7.1 selfReports Stage 1.B.3 cutover — UNPARKED + done).** **Main HEAD = `01b1280b`** (merge of `feat/phase2-stage1-selfreports-cutover` / `e14b51a9`), deployed `npm run deploy` (Published). The previously-PARKED cutover is **DONE via design (b), index-free** — writers go flat-only; the matcher reuses collectionGroup `getTrainingSelfReports` (1 query, grouped by `playerId`) instead of a per-player flat query, so the abandoned `(playerId, trainingId)` composite (`1cb6777d`) was **DROPPED** and **NO `firebase deploy` was needed** (index diff empty; rules change comment-only). New `dedupePreferFlat` makes the 3 collectionGroup readers prefer the flat copy (stale legacy can't shadow override/dismiss). Legacy nested `/players/{pid}/selfReports/` now WRITE-DEAD (read-only until legacy-doc cleanup). **§ 90 Stage 1 COMPLETE** (1.B.1+1.B.2+1.B.3). **Firebase pending: NONE** — the only parked Firebase item is cleared. **§ 37.2 correction folded in** (`getTrainingSelfReports` lives at `playerPerformanceTrackerService.js`, collectionGroup — prior brief's `dataService:247` was wrong). **Next track:** legacy-doc cleanup → unblocks Phase 2.2.d (Stage 4). Smoke: DEPLOY_LOG § 90.7.1 5-step (prod, no deploy). Note: matcher reuse uses a call-time-safe circular import (dataService ↔ PPT).

> **🆕 SHIP — 2026-05-28 (§ 93 Workspace logo + § 92.7 one-row consolidation).** **Main HEAD = `05cfa9b7`** (merge of `feat/workspace-logo` / `dd76164a` logo + `f083ae56` consolidation), deployed `npm run deploy` (Published). Bundled: **(1) § 92.7** — the workspace was duplicated across two More-tab rows; per Jacek's pick the `<WorkspaceSwitcher/>` is now the single identity row and the former "name + Wyjdź" row was slimmed to a single-purpose 🚪 Leave row (`leave_workspace_row`, Wyjdź kept); `TrainingMoreTab` brought to parity (static row → switcher). **(2) § 93** — optional external `logoUrl` on `/workspaces/{slug}` (image URL, **NOT Firebase Storage** — quota-friendly per the 2026-05-27 limit). New `WorkspaceLogo` component (graceful 🏠 fallback, never a broken-image glyph) shown in the switcher row + picker rows + AppShell tournament context bar; set in the § 91 surface (create-modal field + manage-view editor). `dataService.createWorkspace(…,logoUrl)` + `setWorkspaceLogo`. **NO rules change** (workspace-doc write via `isAdmin(slug)`). **Login screen excluded** (no workspace context pre-auth; context bar is tournament-scoped). Smoke: DEPLOY_LOG § 93 4-step.

> **🆕 SHIP — 2026-05-28 (§ 92 Workspace switcher — OPERATION).** **Main HEAD = `4bda4e75`** (merge of `feat/workspace-switcher` / `aa68b73d` + `9a05e524`), deployed `npm run deploy` (Published). The static "Mój workspace" More-tab row is now a **switcher**: tap → bottom-sheet picker of the user's APPROVED workspaces → tap to switch active context **code-free** (new `useWorkspace.setActiveWorkspace(slug)` → persists local+session storage + best-effort `lastAccess` + **hard reload**). Reload is deliberate (data hooks bind to `bp()`; `<ViewAsProvider key={slug}>` remounts children before App's `basePath` effect runs → in-place swap would bleed stale-workspace data). Picker = `useUserWorkspaces()` (own memberships only, never all-workspaces) **filtered to assigned roles** (pending `[]` self-joins excluded per Jacek). Single-workspace users keep the static row. **Complements § 91** (manage any workspace WITHOUT switching) — this is the OPERATION need; the earlier-rejected switcher was the *management* one. NO rules change. **Open scope:** `TrainingMoreTab.jsx` parallel "Mój workspace" row left static (mirror `<WorkspaceSwitcher/>` if training-mode switching wanted). Smoke: DEPLOY_LOG § 92 5-step.

> **🆕 SHIP — 2026-05-28 (§ 91 super_admin Workspaces surface).** **Main HEAD = `413d9e0d`** (merge of `feat/superadmin-workspaces-access` / `5915f16e`), deployed via `npm run deploy` (Published). super_admin-only **Workspaces** page (`/admin/workspaces`, SuperAdminGuard + More → Super Admin): list all workspaces (member/pending counts), create one **without switching active context** (`createWorkspace`, distinct from context-switching `enterWorkspace`), and manage ANY workspace's members / pending / roles. **PART A made the long-ignored `_wsSlug` param load-bearing** via `dataService.wsPath(wsSlug)` (explicit slug → that workspace, null → `bp()`) across `approveUserRoles`/`updateUserRoles`/`transferAdmin`/`removeMember`/`migrateWorkspaceRoles` — **non-breaking** (existing callers pass active slug or null). **NO `firestore.rules` change** — super_admin cross-workspace power already exists via `isSuperAdmin()` in `isAdmin(slug)`. Replaces the deleted workspace switcher; **unblocks FIT onboarding** (tenants self-join pending → super_admin approves + assigns role here, incl. designating a tenant's own workspace_admin via the `admin` chip). Two read-only discoveries preceded it this session — `outputs/DISCOVERY_WORKSPACE_PROVISIONING.md` + `outputs/DISCOVERY_PRIVILEGE_MODEL.md` (ephemeral, uncommitted). **Still open (promoted to NEXT_TASKS):** catalog data-isolation (workspace_admin can write own-workspace-owned `/players` `/teams` via `isWorkspaceAdminOf` — § 90.9 says super_admin-only) + the FIT rank-and-file join carrier (magic-link; `defaultWorkspace` is the wrong carrier per the provisioning discovery). Smoke (Jacek): § 91.5 + DEPLOY_LOG 5-step.

> **🔄 SESSION RESET CHECKPOINT — 2026-05-28 (end of day)** ("we are out of limit for today in firebase. Lets continue tomorrow"). **Main HEAD = `54ceb472`** (BaseCanvas drawMode gate fix DEPLOY_LOG entry; last deployed code = `d2fd4023`). **Today's ships: 8 deploys + 2 doc commits + 1 backfill script + 1 backfill execution.** Highlights: **§ 90 Catalog + Tenant data model** doc committed (`a0cfd598`) — strategic foundation for the Phase 2 cleanup. **§ 90 Phase 2 Stage 1.B.1 selfReports dual-write** shipped (`8a548f35`) — flat path `/workspaces/{ws}/selfReports/{sid}` with explicit `playerId` field; rules + writers + readers transitioned via writeBatch + Promise.all dual-read + collectionGroup dedupe. **Stage 1.B.2 backfill** script shipped (`096f3440`) + collectionGroup-optimized to bypass Spark quota (`0f6281fc`) + LIVE EXECUTED — **52 docs scanned / 49 copied / 3 SKIP-EXISTING / 0 conflicts / 0 errors / parity 52==52**. Three **latent-bug-not-regression fixes** (matching pattern from yesterday's § 86 lessons): admin ActionSheet `onClick`→`onPress` rename across 3 admin pages (`0fe5e1a1`); B3 repair CTA feedback (toast + tinted summary — root cause was broken-feedback not broken-logic, `a99e1344`); BaseCanvas drawMode gate (Coach Plan Draw on ScoutedTeam never worked end-to-end since § 78 ship — gate `gesturesEnabled` didn't include `drawMode`, `d2fd4023`). Players batch select + merge + CSV photo backfill + profile lightbox (`92c661f4`, earlier in session). **PARKED for tomorrow:** Stage 1.B.3 cutover branch `feat/phase2-stage1-selfreports-cutover` (commit `1cb6777d`) — composite index `(playerId, trainingId) COLLECTION` queued in `firestore.indexes.json` but **Firebase daily quota exhausted** before deploy. Tomorrow: `firebase deploy --only firestore:indexes` → verify Enabled in Console → roll PART A/B/C code (writers stop dual-write; readers stop fallback; rules TODO comment) → merge + `npm run deploy` → DEPLOY_LOG `[indexes]` + `[deploy]` entries → § 90.7 row 1 ✅. **Memory captured:** `feedback_button_does_nothing_diagnosis.md` ("~50% of click-does-nothing reports are broken-feedback not broken-logic — always run PART A before assuming logic bug"). **Three latent-bug fixes in one week** share the same shape: feature shipped at deploy where smoke exercised an adjacent flow rather than the intended one; explicit "exercise the new flow once" acceptance criteria recommended in future briefs.

> **Previous reset checkpoint — 2026-05-27 22:xx** ("zamykamy dzisiejszy dzień"). **Main HEAD = `9e05c738`** (B5 / § 89 scout autosave deployed; doc-closeout commit). **16 ships that day** (15 deploys + 1 admin script): gap #1 adminUid · B7 + B6/B8 closures · gap β rules · gap α rules · § 87 zones doc · **§ 88 unified zones v1 (full feature)** · B16+B17+B18 dead-code · B13+B19 mini-hygiene · KIOSK + B14 · gap β sibling rules · dualwrite-orphans + B15 scripts · **A2 v2 ShotDrawer drag+menu** · **B10 LogRow eyebrow+labels** · **B5 / § 89 scout autosave**. Active queue #1 (A2 v2) + B10 + B5 + B12 (closed as moot — was already shipped 2026-05-01) all closed. **Strategic state for next session:** Active queue #2 (Phase 2.2.d+2.3.d dual-write cleanup) is the only Opus-brief-required item left; everything else open is decision/mockup-blocked (B4 Home, B9 squad-matchup, B20 presence) or parked for discussion (LayoutDetailPage UX pass — flagged by Jacek 2026-05-27 post § 88: "niespójna nawigacja, miejscami trudny interfejs"). B15 cleanup script ready; Jacek runs at desktop. PART 3 `linkedUid` backstop collapse `[ESCALATE]` — Jacek's decision (default: keep the net). Smoke debt outstanding (per NEXT_TASKS Active #6): all today's deploys + the older § 81 / § 80 / § 79 / § 78 / § 77 / § 76 / § 61 lists.

> **Previous reset checkpoint — 2026-05-26** (archived): Canvas ladder consolidated; HIGH batch + § 86 v1 ShotDrawer migration + 2 hotfixes shipped; § 86 lessons in PROJECT_GUIDELINES § 9; Med-batch diagnosis (B6+B7+B8) passed to Opus. Replaced by 2026-05-27 above after the 16-ship day.

> **Previous reset checkpoint — 2026-05-24** (archived): Canvas Steps 2-4 shipped (Track A).

**Last updated:** 2026-05-28 by Opus (**Session closed — Firebase quota exhausted, continue tomorrow**. Today's ships: § 90 doc + Stage 1.B.1 dual-write deploy + Stage 1.B.2 backfill script (CG-optimized) + LIVE-executed clean (52/49/3/0/0/parity 52==52) + 3 latent-bug fixes (admin ActionSheet `onPress`, B3 CTA feedback, BaseCanvas drawMode gate). **PARKED:** Stage 1.B.3 cutover branch — composite index `firestore.indexes.json` committed on `feat/phase2-stage1-selfreports-cutover` @ `1cb6777d` but Firebase daily quota out → tomorrow: `firebase deploy --only firestore:indexes` → wait Enabled → PART A/B/C code → merge + deploy → § 90.7 row 1 ✅. Yesterday's parked items still parked (B15 cleanup, PART 3 linkedUid backstop decision, LayoutDetailPage UX pass). Three-latent-bug pattern captured in memory + DEPLOY_LOG lessons. iPad smoke debt accumulated across today's ships.)

**Previous entry:** 2026-05-27 by Opus (**Session closed — "zamykamy dzisiejszy dzień"**. 16 ships that day; Active #1+B5+B10+B12 closed; only Active #2 (Phase 2.2.d cleanup) requires an Opus brief next session. Decision-blocked items + LayoutDetailPage UX pass parked for Jacek. B15 cleanup script + PART 3 linkedUid awaits Jacek action. Smoke debt across that day's ships logged in NEXT_TASKS Active #6 — biggest visible deploys for iPad smoke: § 88 zones / A2 v2 ShotDrawer / B10 LogRow eyebrow / B5 autosave indicator.)

**Previous entry:** 2026-05-26 by CC (**Session pause + Med-batch diagnosis passed to Opus** — Jacek "wstaję jutro". Stan: NEXT_TASKS Active queue gotowy z 5 items "CC, awaiting Opus brief" (A2 v2 / Phase 2.2.d+2.3.d cleanup / Phase 3.c.2 / B7+B8 / dead-code B16+17+18). Med-batch diagnosis (B6+B7+B8) zakończona — wszystkie 3 row w Open bugs zaktualizowane: **B6 = NO-OP closed** (fixed 2026-04-28 via `teamSideMemoryRef` — different shape than briefed `nextPointSide`, ale rozwiązuje oba problemy bez Firestore writes; preserved cross-team leak fix + restored same-team auto-swap via per-team in-session memory). **B7 = awaiting-decision** (Jacek 3 pytania: reposition target, collapse default, localStorage persist). **B8 = HARD ESCALATE** (Jacek/Sławek coaching-semantics call: "MOGLI strzelać" = (A) "placed AND not runner" current = no-op? OR (B) "placed AND not runner AND has explicit shot decision" ~5 LOC? OR (C) bigger shift?). Algebraicznie current denom `nonRunnerPlayers` w computeCompleteness EQUALS brief candidate `N×5 − placedRunners − unplaced` jeśli "undeclared" = unplaced — może być no-op LUB realna zmiana zależnie od semantyki. Earlier today: § 86 v1 + 2 hotfixes `4d16f118` + `22933aa0` + `c61905e5` (ShotDrawer → BaseCanvas, sizing flex-parent measure, editable+onDeleteShot wire); doc-only `019d2ed3` (PROJECT_GUIDELINES § 9 anti-pattern notes from § 86 forensics + NEXT_TASKS Active queue rebuilt for Opus brief authoring). HIGH batch + canvas ladder consolidation both COMPLETE. **Next session start:** read NEXT_TASKS Active (5 items + B7/B8 status) + this checkpoint + wait for Opus brief(s) on Active queue and/or Jacek decisions on B7/B8.

**Previous entry:** 2026-05-26 by CC (**§ 86 B11/A2 ShotDrawer → BaseCanvas** — SHIPPED + deployed `4d16f118` (merge of `fix/b11-shotdrawer-migrate` / `41cc1e60`); normal client-only deploy. Closes B11/A2: ShotDrawer migrated z `<img>` + native scroll + ad-hoc touch → BaseCanvas mount z custom drawFn. **Ostatnia canvas surface poza § 64 ladderem** — po § 86 wszystkie field-rendering surfaces siedzą na BaseCanvas + dziedziczą § 75 grammar (Match scout / Tactic / LayoutDetail / Bunker / Analytics / ScoutedTeam heatmap / ShotDrawer). Bundled: dead-X icon cleanup (drawPlayers.js:138-143 deleted — był renderowany na MAIN canvas, hit-testable tylko gdy modal okluduje, dead afford. by construction); `findShot` switch X-offset → shot-center (radius 22px, finger-friendly); usunięte `players[selectedPlayer]` precondition z mode='shoot' branch (ShotDrawer enforces upstream); MatchPage main canvas już NIE wchodzi w `mode='shoot'`. **STEP 0 viewportSide gate verified** — `BaseCanvas:235-244` pans canvas tak że requested half stays in container; `touchHandler.getRelPos` poprawnie reverses pan → full-field 0-1 returned. **Design deviation:** brief mówił "InteractiveCanvas" + "consumer draw function" — sprzeczne (IC ma fixed drawFn, no customization). Wybrano BaseCanvas direct (matching brief spirit: canvas ladder + § 75 grammar + retire ad-hoc; unikając IC clutter — diagonal origin lines from off-screen player, zones, quickShots — niepasujące do shot-placement surface). **v1 scope:** essentials (pinch/pan/loupe/tap-place/tap-delete). **Drag-move-shot + tap-element-menu DEFERRED** do v2 — pre-§86 ShotDrawer nie miał NIC z tego, więc v1 jest strict upgrade. ShotDrawer rewrite ~165 LOC (was 144 ad-hoc); 2 MatchPage/TacticPage wires (new `fieldCalibration` + `onDeleteShotIdx` props); 2 touchHandler surgical changes; 7-LOC drawPlayers delete. § 27 PASS. Build clean 5.84s; MatchPage 70.55 → 70.65 kB (+0.10 / +0.02 gzip); main bundle effectively unchanged 230.40 / 69.36. 8-step smoke (DEPLOY_LOG): scout opponent-half framing + tap-place + tap-delete + Undo + 🟢 pinch/pan/loupe + 🟢 dead-X gone + Tactic dual-lane + drag-move/menu noted as v2 non-goals. Earlier: § 85 B2 (c) link-to-global `c90b9fa9` (sequenced deploy by CC, migration SKIPPED per Option D); § 84 B2-hotfix `86f98a85`; § 83 B3 roster narrowing `30a03722`; § 82 B1 cache-leak `5c65f7a9`; § 81 ScoutedTeam immersive `3e0126c2`. **HIGH batch + canvas ladder consolidation both COMPLETE.** NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 85 B2 (c) link-to-global** — SHIPPED + deployed `c90b9fa9` (merge of `fix/b2c-link-to-global` / `857362ca`); sequenced deploy executed by CC after Jacek's GO. STEP 1 `firebase deploy --only firestore:rules` ✅ rules compiled clean + released to cloud.firestore. STEP 2 migration ⏭ **SKIPPED per Option D** — Jacek's decision: ~1-3 existing linked users w ranger1996 (głównie Jacek, reszta to inni z access tylko gdzie im przydzieli) → trade-off: jednorazowy re-link prompt 30 sec UX per existing linked user; new flow handles to transparently via § 85 picker filter + § 84 escape hatch. STEP 3 `npm run deploy` ✅ Published. Closes B2 (c) — architectural decision deferred from § 84. Self/admin link/unlink + `subscribeLinkedPlayer` MIGRATED from workspace `/workspaces/{slug}/players/{pid}` → GLOBAL `/players/{pid}`. Completes Phase 2.2 for the link write path that was overlooked when reads + structural writes moved global in Phase 2.2.b. **Security model:** workspace-scoped self-link carve-out na global `/players/{playerId}` block (rules `:415-423` → extended) — `isMember(resource.data.ownerWorkspaceId)` gate na każdym z 3 self-* carve-outs (self-link / self-edit / self-unlink) mirroring workspace block (L228-270). Bez tego scopingu user mógłby zclaimować dowolnego unlinked global playera (z cudzej drużyny) — gorsze niż obecny bug. STEP 0 GATE verdict: wszystkie 4 checks PASS — `ownerWorkspaceId` reliably backfilled (1066 docs, 0 errors per Phase 3.c.2), `isMember(slug)` primitive istnieje przy `firestore.rules:79-84`, `isLinkedSelfPlayer` switch jeden touch (body change workspace→global, signature preserved), `usePlayers` is all-global → STEP 4 picker filter IS needed (defense in depth). NOT escalate. **Write contract: global-only** (NIE dual) — workspace mirror by re-invokował workspace self-link rule wymagający `isPlayer(slug)` workspace role, failing dla exact users this fix targets (non-ranger1996, no workspace player role yet); workspace `linkedUid` STAYS as backstop per Jacek's decision (Phase 2.2.d cleanup). **isLinkedSelfPlayer helper:** body changed workspace→global read, signature `(slug, pid)` preserved (selfReports rule consumers `:295/:297` unchanged, still pass slug for `isPlayer(slug)` check). **Ownership-transfer invariant preserved:** none of 3 self-* carve-outs include `ownerWorkspaceId` in `affectedKeys().hasOnly` — Phase 3.c.2 invariant intact. **One-shot migration:** `scripts/migration/phase_85_linkeduid_to_global.cjs` (idempotent, conflict-safe — skip if global has different linkedUid, report for manual review). Picker filter at parent level (`PbleaguesOnboardingPage` + `ProfilePage`) via `useUserWorkspaces` + `claimablePlayers`/`selfClaimPlayers` memo — admin paths (UserDetailPage) keep unfiltered modal. PRE-FLIGHT gotchas wired: link writes use `updateDoc({field:value})` NOT `setDoc(merge)` dot-notation (`affectedKeys().hasOnly` would break); rules use canonical `data.get('linkedUid', null)` brittle-null form. § 27 PASS. Build clean 6.46s; main `index.js` 230.41 → 230.43 kB (+0.02 / +0.03 gzip — noise; lazy chunks absorb filter logic). 8-step smoke (DEPLOY_LOG): fresh signup workspace ≠ ranger1996 link **succeeds** + ranger1996 fresh signup + 🔒 cross-workspace security (rules REJECT + picker hides) + already-linked users post-migration resolve + self-unlink + admin link/unlink + selfReports gate + 🔒 ownership-transfer guard. **🔴 SEQUENCED DEPLOY (Jacek manual):** 1) `firebase deploy --only firestore:rules` 2) migration script 3) `npm run deploy`. Earlier today: § 84 B2-hotfix `86f98a85` (funnel hang escape); § 83 B3 roster narrowing `30a03722`; § 82 B1 cache-leak `5c65f7a9`; § 81 ScoutedTeam immersive `3e0126c2`; self-log gated off `84a3d140`; § 80 FS Stage 2 `c4642d1e`; § 79 A1 bump fix `ebf634ff`. **HIGH batch now COMPLETE.** NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 84 B2-hotfix b+a** — SHIPPED + deployed `86f98a85` (merge of `fix/b2-hotfix-funnel-hang` / `a6785c23`); PART B of HIGH batch. Stops the onboarding funnel hang on `PbleaguesOnboardingPage` — fresh users couldn't escape the "Czy to ja?" Confirm card when `selfLinkPlayer` failed silently (collection mismatch B2 (c) — global picker / workspace-scoped write/listener; **STILL OPEN — deferred to separate brief**). **(b) async hygiene:** `finally { setBusy(false) }` w `handleSelect` + `handleSkip` (był tylko w catch, success-path polegał na listener fire → workspace mismatch = listener never fires → busy stuck true forever); 8s watchdog na każdym `selfLinkPlayer` await (jeśli promise nie settles, watchdog clears busy + sets recovery error); usunięty `if (busy) return;` guard z `handleSkip`. **(b) modal-side error reflow:** `LinkProfileModal` gets new `error` prop + useEffect — gdy error transitions to non-null while `confirmTarget` set, drops back to list (gdzie `NoMatchFallback` skip-link reachable); transition-only via prevErrorRef bez loops. **(a) escape hatch:** topBar lifted to `z:110 > Modal z:100` w outer stacking context (math zweryfikowana — outer z:50 creates context, modal z:100 w outer context, topBar z:110 w outer context wins overlap); opaque `background:COLORS.bg` covers backdrop pixels; persistent "Pomiń na razie" + "Wyloguj się" buttons **always-enabled** (no `disabled={busy}`); new `handleSignOut` wrapper clears watchdog before delegating do `useWorkspace().signOutUser()`. Concurrent skip vs in-flight selfLink — accepted race (linkedPlayer beats linkSkippedAt in gate logic if link still lands in background). DESIGN_DECISIONS § 84 codifies hotfix scope + 2 pain points closed + z-index sandwich math + explicit non-goals (NO collection contract change). § 27 PASS. Build clean 6.88s; main bundle 230.41 kB / 69.35 kB gzip (effectively unchanged — gzip noise); PbleaguesOnboardingPage chunk lazy-loaded (small delta ~+0.5 kB for state + useEffect + useRef + watchdog). 6-step smoke (DEPLOY_LOG): link fails → escape works + watchdog 8s recovery + skip-during-busy + logout-during-busy + Confirm card escape via error → list + 🔴 explicitly NOT verified: linking actually working (B2 (c) territory). **NEXT:** B2 (c) collection contract decision needs Opus design pass (workspace-scoped vs global; touches dataService + firestore.rules + ProfilePage). Earlier today: § 83 B3 roster narrowing `30a03722` (PART A — scouted.roster division-filtered at write + admin-gated orphan-preserving repair); § 82 B1 cache-leak fix `5c65f7a9`; § 81 ScoutedTeam immersive `3e0126c2`; self-log gated off `84a3d140`; § 80 FS Stage 2 `c4642d1e`; § 79 A1 bump fix `ebf634ff`. NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 83 B3 roster narrowing** — SHIPPED + deployed `30a03722` (merge of `fix/b3-roster-division-filter` / `97449ab0`); PART A of HIGH batch. Closes SCOUT #1 (HIGH): roster picker pokazywał parent + WSZYSTKie child teams' players, niezależnie od dywizji turnieju. Write-time bug w `ScoutTabContent.buildScoutedPayload` — unconditional union `[teamId, ...childIds]` z `1a030508` (2026-04-20, fix opposite empty-roster bug) over-correct without division narrowing. Fix: per-team filter na `divisions[league] === finalDivision` z defensive fallback do pełnego union gdy `finalDivision` null lub filter pusty (zachowuje `1a030508` empty-roster fix przy incomplete team data). Schedule importers (`ScheduleCSVImport.jsx:349,372` + `ScheduleImport.jsx:277`) **NIE mają tego buga** — verified at pre-flight (używają pojedynczego `teamId`, nie union); diagnoza report była off. Repair migration: nowy `repairScoutedRostersForTournament(tid, league)` w dataService — mirror write-time fix logic + UNION z każdym pid już referowanym w `homeData.assignments`/`awayData.assignments` istniejących punktów (orphan-prevention contract — picker dalej resolve'uje nazwy już-przypisanych graczy nawet jeśli wypadają poza dywizję). Idempotent — set-equality check przed updateDoc. UI: admin-gated Btn w `CoachTabContent` (`useIsSuperAdmin`), nowa "ADMIN · B3 ROSTER REPAIR" card oddzielna od existing repair-divisions card. Role-gated visibility (nie symptom-gated) bo over-broad-roster shape NIE jest cheaply detectable z client side without walking points. STEP A0 verdict: orphan risk mitigable; tylko 1 write site (ScoutTabContent), nie 3 (correction from diagnosis report); NOT escalate. § 27 PASS. Build clean 6.59s; MainPage +1.42 kB / +0.28 kB gzip (CoachTabContent admin block); main `index.js` 228.28 → 230.41 kB (+2.13 kB / +0.66 kB gzip — repair helper ~2KB unminified). 6-step smoke (DEPLOY_LOG): fresh add narrows + admin repair scanned/updated/unchanged + orphan-prevention works + defensive fallback (no division) + Schedule importers unchanged + non-admin sees no admin card. **NEXT after PART A merge:** PART B = B2-hotfix (b+a) — async hygiene + escape hatch w PbleaguesOnboardingPage / LinkProfileModal (stop funnel hang; NIE fix linkowania — to (c) deferred). Earlier today: § 82 B1 cache-leak fix `5c65f7a9` (MatchPage edit-state lifecycle); § 81 ScoutedTeam immersive `3e0126c2`; self-log gated off `84a3d140`; § 80 FS Stage 2 `c4642d1e`; § 79 A1 bump fix `ebf634ff`; § 78 Draw Stage 2 `293576a8`. NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 82 B1 cache-leak fix** — SHIPPED + deployed `5c65f7a9` (merge of `fix/b1-edit-state-lifecycle` / `64d31fb0`). MatchPage edit-state lifecycle gets a coherent contract. Closes SCOUT #3 (HIGH, data integrity): edit punktu N → nawigacja na `?scout=X&mode=new` → save **NIE** nadpisuje N po cichu (Seq A); team-switch w edytorze czyści kontekst (Seq B); delete/clearAll nie promuje rosteru skasowanego punktu do "last used" (Seq C). Trzy zmiany skoordynowane: (a) nowy `exitEditMode()` helper czyści drafty + annotations + ancillary state, **zachowuje fieldSide + activeTeam** (perspektywa ≠ tożsamość punktu, invariant 2), czyści editingId **tylko** gdy `!isEmptyShellRef.current` (invariant 1 — § 18 concurrent empty-shell preservation via explicit ref; live code dead today, ref defensive forward-compat); (b) `lastAssign*` capture przeniesione z `resetDraft` do `savePoint` success-branch (była unconditional → Seq C); (c) nowy effect na `[scoutTeamId, searchParams]` z key-based ack (`lastFreshScoutKeyRef`) wykrywa fresh-scout intent (mode=new + zmiana scoutTeamId OR re-entry mode=new) i wywołuje `exitEditMode()` raz na intent; `?point=<id>` paths przez existing pointParamId effect, NIE resetuje. Dwa Back-from-editor sites (portrait + landscape) zamienione z `setEditingId(null) + 3 partial clears` na `exitEditMode()` — widely closes Seq-A exit path. DESIGN_DECISIONS § 82 codifies the lifecycle contract + 3 invariants + rationale dla explicit ref vs state-derived shell detection. § 27 PASS. Build clean 5.47s; MatchPage 70.11 → 70.55 kB (+0.44 kB) / 20.57 → 20.71 kB gzip (+0.14 kB); main bundle unchanged 228.28 / 68.70. STEP 0 verdict: `startNewPoint:834` (jedyny shell-create path) ma **zero callers** w live code; isEmptyShellRef pozostaje false → exitEditMode zawsze czyści editingId → matches simple model; ref reserved dla future revival of startNewPoint. 9-step smoke (DEPLOY_LOG): Seq A/B/C cleared + invariant 1/3 preserved + edit-existing-point still works + Back from editor + annotations cleared + repeated mode=new entries. Earlier today: § 81 ScoutedTeam immersive `3e0126c2`; self-log gated off `84a3d140`; § 80 FS Stage 2 `c4642d1e`; § 79 A1 bump fix `ebf634ff`; § 78 Draw Stage 2 `293576a8`. Yesterday: § 76 hotfix #2 `db08b059`, § 77 hotfix `6a3fea4d`, § 76 hotfix `d87abc4e`, Draw Stage 1 `cd9aa448`, FS Stage 1 `884937d8`, § 75 doc `686b73d3`, regression fix `6f7158f7`, training guest squad-persist `909e7105`, Canvas Steps #5/#4 `cb28a26a`/`2b6a473`. NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 81 ScoutedTeam immersive** — SHIPPED + deployed `3e0126c2` (merge of `feat/scoutedteam-region-overlay` / `785d7df0`). Heatmap-region full-viewport overlay model — third + final immersive shape. Decoupled od `useLandscapeMode`/`isLandscape` (rotacja dashboardu NIE promuje heatmapy; entry przez jawny `<FullscreenToggle placement="top-left">` na rozwiniętej heatmapie, obie orientacje, BEZ auto-on-landscape). Wrapper-style swap na TYM SAMYM JSX subtree (style swap conditional na `heatmapFullscreen`): inline = current borderRadius+overflow:hidden+position:relative; fullscreen = `position:fixed; inset:0; zIndex:60; background:COLORS.bg; display:flex; flexDirection:column; overflow:hidden; paddingBottom:env(safe-area-inset-bottom,0px)`. **No remount** → HeatmapCanvas DOM + DrawingOverlay + coachStrokes/coachCurrent/coachRedo/coachColor/coachSizeKey/coachEraser state wszystko preserved przez entry/exit. Scroll dashboardu zapisany na enter (`scrollContainerRef.current.scrollTop` → `scrollTopBeforeFsRef`), przywrócony na exit przez `requestAnimationFrame` after layout settle. FullscreenToggle (shared § 76 component) zyskał `placement` prop (default 'top-right' → Stage 1 callers (Match/Tactic/LayoutDetail) untouched verbatim; nowa wartość 'top-left' dla ScoutedTeam, safe-area-aware via `calc(8px + env(safe-area-inset-*, 0px))`, żeby nie kolidować z Rysuj chip na top-right). `isLandscape={false}` w callerze ScoutedTeam bypasses canvas-page rotation gate (meaningful tylko dla canvas-primary). Miniature 110px nadal bez Maximize (tap-to-expand primary). DESIGN_DECISIONS § 81 codifies the model + per-surface immersive eligibility table — **closes § 76 scope at 3 modeli:** canvas-primary chrome-hide (§ 76/§ 80, Match/Tactic/LayoutDetail), scroll-dashboard region-overlay (§ 81, ScoutedTeam), excluded canvas-secondary (§ 80, Bunker/Analytics). § 27 PASS. Build clean 4.99s; ScoutedTeam +0.51 kB / +0.24 kB gzip; main bundle 228.28 kB / 68.70 kB gzip unchanged. 7-step smoke (DEPLOY_LOG): Maximize2 → overlay + Draw w fullscreen + toggles w fullscreen + Minimize2 scroll restore + 🔴 no-auto-on-landscape + 🔴 arbiter regression (1-finger draw / 2-finger zoom-pan) + miniature bez Maximize. Earlier today: **Self-log entry points gated OFF** — SHIPPED + deployed `84a3d140` (merge of `feat/selflog-gate-off` / `309a0eaf`). Cleanup task per CC_BRIEF_SELFLOG_GATE_OFF: Jacek nie używa self-logu; MatchPage scout FAB (§ 35.2, amber MapPin + pendingCount badge) ukryty za nowym dynamic feature flagiem `selfLog` w `DYNAMIC_FLAG_DEFAULTS` (default `enabled:false`, `audience:'admin'`). **Non-destructive, w pełni odwracalne** — HotSheet, dataService self-log writes (setPlayerSelfLog/addSelfLogShot/training variants), schema `point.shots source:'self'`, collection-group indexes, breakoutVariants subcollection ZACHOWANE. Reaktywacja przez `/debug/flags`. STEP 0 verdict: tylko FAB live; OnboardingModal z 2026-04-20 NIE istnieje (zastąpiony PbleaguesOnboardingPage w 2026-04-24, to identity-link gate § 38.12, nie self-log); Tier 2 "Mój dzień" NIGDY nie wjechał (grep clean — tylko read-side aggregation). 5 plików / +74/−3 LOC: featureFlags.js (1 entry w DYNAMIC_FLAG_DEFAULTS z rationale comment), MatchPage.jsx (import useFeatureFlag + `selfLogEnabled` hook call + `selfLogEnabled &&` prefix na FAB build), DESIGN_DECISIONS § 35 dopisek, DEPLOY_LOG entry, NEXT_TASKS notka. § 27 PASS (color/elevation/typography/cards/navigation unaffected; anti-patterns ZERO). Build clean 4.94s; main bundle 228.23 → 228.28 kB (+0.05 kB) / 68.69 → 68.71 kB gzip (+0.02 kB). **Cosmetic:** merge-commit title ma word drop (` selfLog ` zjedzone przez bash backtick-interpolation w `-m` arg) — branch commit `309a0eaf` body + code intact; nie amended (force-push na pushed main niedozwolony). 5-step smoke (DEPLOY_LOG): FAB znika dla linked-player accounts + login bez self-log Onboarding + brak Mój dzień + core scouting nietknięty + admin re-enable przez /debug/flags działa. Earlier today: § 80 FS Stage 2 closeout `c4642d1e` (LayoutDetailPage immersive; BunkerEditor + LayoutAnalytics excluded per canvas-primary boundary); § 79 A1 bump fix `ebf634ff`; § 78 Draw Stage 2 `293576a8` (§ 75 Draw sequencing COMPLETE). Yesterday: § 76 hotfix #2 `db08b059`, § 77 hotfix `6a3fea4d`, § 76 hotfix `d87abc4e`, Draw Stage 1 `cd9aa448`, FS Stage 1 `884937d8`, § 75 doc `686b73d3`, regression fix `6f7158f7`, training guest squad-persist `909e7105`, Canvas Steps #5/#4 `cb28a26a`/`2b6a473`. **§ 75 Draw sequencing + § 76 Full-screen scope COMPLETE.** NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 80 FS Stage 2 closeout** — SHIPPED + deployed `c4642d1e` (merge of `feat/fs-stage2-layoutdetail` / `fdfa5050`). Closes § 76's fast-follow on the one surface where the wzorzec applies mechanically — **LayoutDetailPage** full immersive: destructure widened to `{ isLandscape, fsActive, immersive, setFullscreen, canvasMaxHeight }`, 6 chrome-hide / sizing gates swapped `isLandscape` → `immersive` (maxWidth, PageHeader, immersive floating Back/⋮ + edge tabs, toolbar Aā/½/◇ + BIG MOVE, tactics list, bottom New-tactic bar), `<FullscreenToggle>` mounted in canvas frame (sibling of InteractiveCanvas, parent already `position:relative`), `canvasMaxHeight(20, 200)` offset unchanged (canonical per hook table). § 76 hooks-order hotfix preserved; doc-comment updated for Stage 2's `isLandscape`-for-toggle-only retention. **BunkerEditor + LayoutAnalytics EXCLUDED (not deferred)** per **new § 80 canvas-primary boundary principle**: hide chrome only when canvas IS the primary content — BunkerEditor's bunker-naming form is the editing workflow (must stay visible); LayoutAnalytics's deaths/breaks tables ARE the analytic deliverable (canvas is thumbnail-scale). **ScoutedTeam = separate scroll-dashboard model** (heatmap is one tile among many — different UX problem). § 76 "Fast-follow" subsection annotated with `> UPDATE → § 80` (candidate list preserved per § 37 doc discipline). § 27 PASS — no new color use, gate variable swaps only. Build clean 5.70s; LayoutDetailPage bundle 27.05 kB / 7.57 kB gzip; main bundle unchanged. Per `feedback_precommit_bash_enoent` — precommit Windows false-negative, verified directly (zero console.log/debugger, zero Polish strings, 44px touch preserved via TOUCH.minTarget). 6-step smoke (DEPLOY_LOG): Maximize2 portrait→immerse + Minimize2 exit + landscape unchanged + portrait-FS persists across rotation + hooks-order load-while-loading + BunkerEditor/LayoutAnalytics show NO new behavior. Earlier today: § 79 A1 bump fix `ebf634ff` (arrow direction + scout shot-origin Option C); § 78 Draw Stage 2 `293576a8` (§ 75 Draw sequencing COMPLETE end-to-end). Yesterday: § 76 hotfix #2 `db08b059`, § 77 hotfix `6a3fea4d`, § 76 hotfix `d87abc4e`, Draw Stage 1 `cd9aa448`, FS Stage 1 `884937d8`, § 75 doc `686b73d3`, regression fix `6f7158f7`, training guest squad-persist `909e7105`, Canvas Steps #5/#4 `cb28a26a`/`2b6a473`. **§ 75 Draw sequencing + § 76 Full-screen scope COMPLETE.** NO ACTION: `adminUid` repoint stays unauthorized.

**Previous entry:** 2026-05-25 by CC (**§ 79 A1 bump fix** — SHIPPED + deployed `ebf634ff`. Two render-side fixes in `drawPlayers.js`: bump arrow tip now lands on `players[i]` (= end/destination per user spec; bezier reversed, arc bow side preserved by reusing OLD perpendicular vector so saved `bs.curve` values render same physical side) + new `bumpShotOriginAtStart` prop on drawPlayers/InteractiveCanvas (Option C — explicit opt-in). MatchPage scout passes `true` so `shots[]` lane origins from `bumpStops[i]` whenever a bump exists ("shoots from cover, then bumps"); Tactic / LayoutDetail tactic-preview / BunkerEditor keep default `false` so § 2.9 "Shot 1st (from player) / Shot 2nd (from bump)" dual-lane semantic stays intact verbatim. Misleading comments at drawPlayers L185 + L158 corrected. **DESIGN_DECISIONS § 79** codifies the bump data semantic (`bumpStops`=drag-START / `players`=drag-END per § 2.5) and the § 2.9 lane labels as render-source (NOT chronological) — first time written down. Build clean 7.86s, precommit pass, § 27 N/A (no UI surface touched — render-side data swap only). Main bundle unchanged 228.19 kB / 68.66 kB gzip. 6-step smoke (DEPLOY_LOG): bump arrow tip on destination + scout shot origin on bump-stop + no-bump no regression + Tactic preview unchanged + heatmap surfaces unaffected + runner/elim markers untouched. § 78 Draw Stage 2 shipped `293576a8` earlier (ScoutedTeam Plan coacha + Notatki scouta) — § 75 Draw sequencing COMPLETE. **NEXT after GO:** FS Stage 2 fast-follow (extend immersive pattern to ScoutedTeam / LayoutDetail / BunkerEditor / LayoutAnalytics — mechanical, same `<FullscreenToggle>`). Yesterday: § 76 hotfix #2 `db08b059`, § 77 hotfix `6a3fea4d`, § 76 hotfix `d87abc4e`, Draw Stage 1 `cd9aa448`, FS Stage 1 `884937d8`, § 75 doc `686b73d3`, regression fix `6f7158f7`, training guest squad-persist `909e7105`, Canvas Steps #5/#4 `cb28a26a`/`2b6a473`. NO ACTION: `adminUid` repoint stays unauthorized. Closes § 75 Draw sequencing — ScoutedTeam coach-summary heatmap gains two annotation layers. **2a Plan coacha** (editable per scouted-team, `scouted.annotations`, canonical, default ON, both orientations) + **2b Notatki scouta** (read-only aggregate from `point.annotations`, mirrored via extended `mirrorPointToLeft`, default OFF, respects `filterMatchId`). Shared `paintStroke()` extracted to `drawStrokes.js` (single source of truth — bug history from § 77 hotfix lives here now). HeatmapCanvas signature gained draw* + children pass-through (isomorphic with InteractiveCanvas Step #4) + annotation render-path props. ScoutedTeamPage wiring: 7 state hooks + 9 handlers + load/save effects + `✏ Rysuj` chip + DrawToolbar mount + 2 new toggle pills (Plan coacha / Notatki scouta). § 4.3 doc-correction landed (the "NO Positions/Shots toggle" claim was over-broad — scoped to Match B3 heatmap; ScoutedTeam has toggles). § 27 PASS. Build clean 5.89s; bundle: ScoutedTeam +2.64 kB / +0.90 kB gzip; MatchPage **−7.10 kB / −2.19 kB gzip** (paintStroke extraction shrunk DrawingOverlay internal logic); main `index.js` 228.19 kB / 68.66 kB gzip (+0.30 kB / +0.12 kB net). 5-step smoke: 2a portrait+landscape + 2b mirror + Plan coacha toggle + miniature read-only + arbiter regression. **§ 75 Draw sequencing COMPLETE** (FS Stage 1 + Draw Stage 1 + Draw Stage 2 + 3 hotfixes). NEXT: FS Stage 2 fast-follow (extend immersive pattern to ScoutedTeam / LayoutDetail / BunkerEditor / LayoutAnalytics — mechanical, same shared `<FullscreenToggle>`). Yesterday + earlier: § 76 hotfix #2 `db08b059` (heatmap landscape fit, closed #2), § 77 hotfix `6a3fea4d` (SVG path generator, closed #1), § 76 hotfix `d87abc4e` (hooks-order LayoutDetail + Tactic, closed #3); Draw Stage 1 `cd9aa448`, FS Stage 1 `884937d8`, § 75 doc `686b73d3`, regression fix `6f7158f7`, training guest squad-persist `909e7105`, Canvas Steps #5/#4 `cb28a26a`/`2b6a473`. NO ACTION: `adminUid` repoint stays unauthorized.)
**Live app:** https://epicsports.github.io/pbscoutpro
**Repo:** https://github.com/epicsports/pbscoutpro
**Main HEAD at last deployed change:** `d2fd4023` (BaseCanvas drawMode gate — Coach Plan Draw on ScoutedTeam, latent § 78 silent-fail; merge of `fix/coach-shot-drawer-desktop` / `25123f8f`). Deployed via `npm run deploy` 2026-05-28 18:xx. Pre-deploy HEAD: `a99e1344` (B3 CTA feedback). DEPLOY_LOG stamp commit: `54ceb472`. **Today's slate:** § 90 doc + Stage 1.B.1 selfReports dual-write + Stage 1.B.2 backfill (script + CG-optimization + clean LIVE execution: 52 scanned / 49 copied / 3 skip-existing / 0 conflicts / 0 errors / parity Legacy 52 == Flat 52) + 3 latent-bug fixes (admin ActionSheet onPress / B3 CTA feedback / BaseCanvas drawMode gate). **PARKED for tomorrow:** Stage 1.B.3 cutover branch `feat/phase2-stage1-selfreports-cutover` @ `1cb6777d` (composite index committed, awaiting Firebase deploy → daily quota out today). **Closed:** Stage 1.B.1 + Stage 1.B.2 (live). **Open (tomorrow):** Stage 1.B.3 cutover (PART A/B/C code) on top of the parked index commit → unblocks Phase 2.2.d. Active queue #2 (Phase 2.2.d + 2.3.d dual-write cleanup) still gated on Stage 1.B.3 stabilization.

> **League rename — now safe.** Jacek can rename "NXL" → "NXL Europe" via More → Super Admin → Leagues → edit `l_nxl`'s **Display name** (the resolution layer propagates it everywhere; `shortName` "NXL" stays the frozen key in all refs). NXL US import = a separate future brief (must use a distinct `shortName`; see § 71 for the `CSVImport:111` residue).

> **Discovery #2 — closed.** The "admin features hidden" symptom was NOT a role bug: the flags were `enabled:false`, and `DebugFlagsPage` mislabelled disabled flags as role-hidden (fixed). The **View-As-ghost hypothesis is DISPROVEN** — `ViewAsContext` is runtime-disabled (`viewAs` always `null`). Do not re-investigate role resolution / View-As.

### ✅ This session closed (2026-05-24..25 hotfix bundle + Stage 2 + A1 + FS Stage 2 closeout + self-log gate-off + § 81 immersive close)
- **§ 81 ScoutedTeam immersive — SHIPPED + deployed** (`3e0126c2`, merge of `feat/scoutedteam-region-overlay` / `785d7df0`). Heatmap-region full-viewport overlay — the **third + final immersive shape** after § 76 / § 80 canvas-primary chrome-hide. **Decoupled** from `useLandscapeMode`/`isLandscape` (rotation does NOT auto-promote; entry is always explicit Maximize2). Wrapper-style swap on the SAME JSX subtree → no remount; HeatmapCanvas DOM + DrawingOverlay state + coach draw state all preserved across enter/exit. Scroll position of dashboard saved on enter, restored via `requestAnimationFrame` on exit. `<FullscreenToggle>` gained `placement` prop (default `'top-right'` keeps Stage 1 callers byte-identical; `'top-left'` is safe-area-aware for the iOS-notch overlay context, used by ScoutedTeam to avoid colliding with the Rysuj chip). `isLandscape={false}` bypasses the canvas-page rotation gate (meaningful only for canvas-primary). Miniature 110px untouched — tap-to-expand stays primary, no Maximize there. **§ 81 closes the immersive scope** at 3 models: canvas-primary chrome-hide (§ 76 / § 80) / scroll-dashboard region-overlay (§ 81) / excluded canvas-secondary (§ 80). DESIGN_DECISIONS § 81 codifies the model + per-surface eligibility table. § 27 PASS. Build clean 4.99s; ScoutedTeam +0.51 kB / +0.24 kB gzip; main bundle unchanged. 7-step smoke (DEPLOY_LOG): Maximize2 → overlay + Draw in FS + toggles in FS + Minimize2 scroll restore + 🔴 no-auto-on-landscape + 🔴 arbiter regression + miniature no Maximize.
- **Self-log entry points gated OFF — SHIPPED + deployed** (`84a3d140`, merge of `feat/selflog-gate-off` / `309a0eaf`). Cleanup task per CC_BRIEF_SELFLOG_GATE_OFF — Jacek doesn't use self-log. MatchPage scout FAB (§ 35.2 amber MapPin + pendingCount badge) now gated by new dynamic feature flag `selfLog` in `DYNAMIC_FLAG_DEFAULTS`, default `enabled:false`, `audience:'admin'`. **Non-destructive, fully reversible** — HotSheet component, `setPlayerSelfLog`/`addSelfLogShot` dataService writes, `point.shots source:'self'` schema, collection-group indexes, `breakoutVariants` subcollection all preserved. STEP 0 confirmed FAB is the ONLY live entry point (OnboardingModal from 2026-04-20 was removed when PbleaguesOnboardingPage took over the unmatched-user flow in 2026-04-24; Tier 2 "Mój dzień" never shipped). 5 files / +74/−3 LOC. § 27 PASS. Bundle delta negligible (+0.05 kB main / +0.02 kB gzip). Admin reactivation via `/debug/flags`. Cosmetic: merge-title backtick-interpolation ate `selfLog` word; not amended (would require force-push to pushed main). 5-step smoke in DEPLOY_LOG.
- **§ 80 FS Stage 2 closeout — SHIPPED + deployed** (`c4642d1e`, merge of `feat/fs-stage2-layoutdetail` / `fdfa5050`). Closes § 76's fast-follow on the one surface where the wzorzec applies mechanically — **LayoutDetailPage** full immersive (6 chrome-hide / sizing gates swapped `isLandscape` → `immersive`; `<FullscreenToggle>` mounted in canvas frame; destructure widened; § 76 hooks-order hotfix preserved). **BunkerEditor + LayoutAnalytics EXCLUDED (not deferred)** per **new § 80 canvas-primary boundary principle**: BunkerEditor's bunker-naming form IS the editing workflow (must stay visible regardless of orientation); LayoutAnalytics's deaths/breaks tables ARE the analytic deliverable (canvas is thumbnail-scale, hiding chrome would zoom up secondary content while hiding primary). **ScoutedTeam = separate scroll-dashboard model.** § 76 Fast-follow subsection annotated with `> UPDATE → § 80` (candidate list preserved per § 37). DESIGN_DECISIONS § 80 codifies the principle + per-surface eligibility table. § 27 PASS (no new color use — gate variable swaps only). Build clean 5.70s; LayoutDetailPage bundle 27.05 kB / 7.57 kB gzip; main bundle unchanged. 6-step smoke in DEPLOY_LOG: portrait Maximize2 → immerse + Minimize2 exit + landscape unchanged + portrait-FS persists across rotation + hooks-order load-while-loading + BunkerEditor/LayoutAnalytics show NO new behavior.
- **§ 79 A1 bump fix — SHIPPED + deployed** (`ebf634ff`, merge of `fix/a1-bump-arrow-and-scout-shot-origin` / `b3067e74`). Two render-side fixes in `drawPlayers.js`: (1) bump arrow tip now on `players[i]` = destination per user spec (bezier reversed, arc bow side preserved); (2) new `bumpShotOriginAtStart` prop (Option C — explicit opt-in) routes MatchPage scout's `shots[]` lane to origin from `bumpStops[i]` when a bump exists. Tactic / LayoutDetail tactic-preview / BunkerEditor keep the default so § 2.9 dual-lane semantic stays intact. Misleading drawPlayers L185+L158 comments corrected. DESIGN_DECISIONS § 79 codifies the bump data semantic + § 2.9 lane labels = render-source not chronological. Render-side only, no UI/schema change. Bundle unchanged. 6-step smoke in DEPLOY_LOG.
- **§ 78 Draw Stage 2 — SHIPPED + deployed** (`293576a8`, merge of `feat/draw-stage2-scouted-annotations` / `0d135c6f`). Closes § 75 Draw sequencing. ScoutedTeam coach-summary heatmap gains TWO annotation layers: **2a Plan coacha** (editable, `scouted.annotations`, canonical, default ON, both orientations, reuses Stage 1 DrawingOverlay + DrawToolbar via HeatmapCanvas `children` slot) + **2b Notatki scouta** (read-only aggregate from `point.annotations`, mirrored via extended `mirrorPointToLeft`, default OFF, respects `filterMatchId`). Shared `paintStroke()` extracted to `drawStrokes.js` (single source of truth; perfect-freehand path bug from § 77 hotfix moves here). HeatmapCanvas gains isomorphic-with-InteractiveCanvas-Step-#4 draw* + children pass-through + annotation render-path props. ScoutedTeam: 7 state hooks + 9 handlers + load/save + chip + DrawToolbar + 2 new toggle pills. `§ 4.3` doc-correction landed (the "NO Positions/Shots toggle" claim was over-broad; scoped to Match B3 heatmap). § 27 PASS. Bundle: ScoutedTeam +2.64 kB; MatchPage −7.10 kB (paintStroke extraction shrunk DrawingOverlay internals); main `index.js` 228.19 kB / 68.66 kB gzip (+0.30 kB net). 5-step smoke: 2a portrait+landscape + 2b mirror + Plan coacha toggle + miniature read-only + arbiter regression.
- **§ 76 hotfix #2 — `sizingStrategy='fit'` for HeatmapCanvas** (`db08b059`, merge of `fix/heatmap-fit-sizing` / `232c1fdc`). Closes user-reported ScoutedTeam landscape image overflow (pre-existing). New `'fit'` strategy in `BaseCanvas.compute()`: `w = min(containerW, maxH × aspect); h = w / aspect` — object-fit:contain math; defaults `maxH` to `window.innerHeight`. HeatmapCanvas swaps `'width-first'` → `'fit'`. Portrait unchanged; landscape fits viewport. 2 files / +25/−2 LOC. Bundle unchanged. **All three 2026-05-24 user-reported regressions now closed.**
- **§ 77 hotfix — DrawingOverlay SVG path generator** (`6a3fea4d`, merge of `fix/drawing-overlay-svg-path` / `d7f26bb2`). User-reported #1: Match scout drawMode activated but nothing rendered, while Clear/Undo enabled (data WAS being stored). Root: `getSvgPathFromStroke` produced invalid SVG paths (SVG's `Q` needs control point + endpoint = TWO coord pairs; mine emitted only one). Browsers' Path2D silently no-ops bad paths → `c.fill` painted nothing. Fix = canonical perfect-freehand pattern (M + Q + N×4 chained control+midpoint pairs + Z). Strokes drawn during silent-fail window salvaged — already in `point.annotations`, render on reload after fix. 1 file, +17/−9 LOC, bundle unchanged.
- **§ 76 hotfix — `useLandscapeMode` hooks-order in LayoutDetailPage + TacticPage** (`d87abc4e`, merge of `fix/hooks-order-and-heatmap-fit` / `1248cc98`). User-reported: LayoutDetail "sypie błędami i nie pozwala wejść" on entry. Root cause: `useLandscapeMode()` called AFTER conditional early returns (`if (layoutsLoading) return ...` / `if (!tactic) return ...`) → React 18 "Rendered more hooks than during the previous render" because render N had fewer hooks than render N+1. Latent pre-existing — § 76 added 2 internal hooks to `useLandscapeMode` (useState for fsActive + useCallback for setFullscreen), bumping the conditional hook delta past React's tolerance. Fix = mechanical reorder. TacticPage proactively fixed (same pattern, would have crashed on cold tactic load). MatchPage / BunkerEditor unaffected (hook already above returns). Bundle unchanged. Open: #1 Match draw silent fail (awaiting console error), #2 ScoutedTeam landscape overflow (separate fix path).
- **§ 77 Draw Stage 1 — SHIPPED + deployed** (`cd9aa448`, merge of `feat/draw-stage1-overlay` / `238adfde`). Closes § 75 Stage 1 sequencing (full-screen + draw both shipped Stage 1). DrawingOverlay (render-only `pointerEvents:none`, perfect-freehand outlines, DPR-scaled, BaseCanvasContext for transform) + BaseCanvas arbiter `drawMode` branch (surgical `touchHandler.js` change; 1-finger draws, 2-finger STILL zoom/pan, 2nd-finger-mid-stroke aborts) + DrawToolbar (5 colors / 3 widths / Undo / Redo / sized point-eraser / Clear-confirm / Done) + drawStrokes pure helpers (Firestore object shape, `eraseAcrossStrokes`) + MatchPage wiring (state + 9 handlers + chip + toolbar; `editPoint` rehydrates, both savePoint branches write `point.annotations`). Entry chip `✏ Rysuj` LANDSCAPE-only on Match (portrait + portrait-FS = scouting / view-only, no draw). NATIVE-orientation coords (no mirror on write — Stage 2 mirrors at read). `perfect-freehand` MIT ^1.2.3 dep added. Bundle: main index.js unchanged 227.89 kB / 68.56 kB gzip; MatchPage +8.9 kB; vendor-react +2.09 kB (Lucide icons). **Stage 2** (separate brief, post-deploy): ScoutedTeam annotation aggregation + heatmap toggle (Pozycje/Strzały/Adnotacje) + per-match filter.
- **§ 76 Full-screen Stage 1 — SHIPPED + deployed** (`884937d8`, merge of `feat/fullscreen-stage1-immersive` / `5def9218`). Generalizes § 64.10 / § 64.9 step #11 (landscape coach view, ScoutedTeam-specific) to universal data-canvas full-screen capability. Two triggers: auto-on-rotate (existing) + portrait toggle (new `<FullscreenToggle>` button). Unified flag `immersive = isLandscape || fsActive` in `useLandscapeMode()`. Stage 1 scope = MatchPage scout + TacticPage (the two confirmed immersive surfaces on InteractiveCanvas). Fast-follow ticket: same mechanical pattern for ScoutedTeam heatmap + LayoutDetail + BunkerEditor + LayoutAnalytics. **BaseCanvas untouched** — lift of `<FullscreenToggle>` into BaseCanvas chrome deferred to a future § 64 rung after DrawingOverlay impl experience. `<FullscreenToggle>` lives in `src/components/canvas/FullscreenToggle.jsx` — Lucide Maximize2/Minimize2, 44px, amber on active (§ 27 carve-out for interactive toggles), null in landscape. `fsActive` is portrait-state with no auto-reset; stuck-state safety via persistent toggle in portrait-FS. Bundle: index.js unchanged 227.89 kB / 68.54 kB gzip; MatchPage +0.12 kB; vendor-react +0.73 kB (one-time Lucide icons). Smoke checklist (5 items) in DEPLOY_LOG — #1 = portrait Maximize2 → immerse; #4 = tap/drag-player regression must hold in portrait-FS. **§ 75 sequencing:** unblocks DrawingOverlay impl next (clickable toolbar mockup → impl, iPad/PencilKit arbitration model locked: BaseCanvas-as-arbiter, NOT event-forwarding).
- **InteractiveCanvas tap/drag regression fix** (`6f7158f7`) — surgical 1-file fix in `BaseCanvas.jsx:172-183`. Root cause: raw `useState` setters for `dragging` / `draggingBunker` froze `draggingRef.current` / `draggingBunkerRef.current` at null; `touchHandler` reads the refs in handleMove (drag-player) + handleUp (tap-detection). Bug introduced structurally at Step #2 BaseCanvas extraction (`ecc850ce`, 2026-05-23) but dormant; went live at Step #4 InteractiveCanvas migration (`71179616`, 2026-05-24). Fix = restore the wrapped-setter pattern from `FieldCanvas:81-86` 1:1 (`setDragging = (v) => { ref = v; _setDragging(v); }`). No state-drop — keeps draw-effect dep surface unchanged. Tap-element → toolbar + drag-element restored on MatchPage (scout hot-path), TacticPage, BunkerEditorPage, LayoutDetailPage. Build clean 7.48s, precommit pass, bundle unchanged 227.89 kB / 68.55 kB gzip. Anti-pattern codified in `PROJECT_GUIDELINES § 9 Architektura`. **§ 75 sequencing unblocked** — gesture grammar in BaseCanvas now consistent (ref-state sync); full-screen (#11 generalized) + DrawingOverlay can build on working grammar.
- **§ 75 Canvas Interaction Model doc** (`686b73d3`) — appended § 75 to DESIGN_DECISIONS.md. Apple HIG split: BaseCanvas owns universal gesture grammar (pinch-zoom / pan / full-screen); feature-children own contextual content. Three consistency tiers (Universal / Edit-family / Read-family) + screen-specific outliers. Generalizes § 64.10/#11 full-screen as universal. Enhanced DrawingOverlay → iOS-Markup-style annotation on `perfect-freehand` (MIT) — NOT tldraw / NOT Excalidraw. Mode-not-gesture; 1-finger draws, 2-fingers stay zoom/pan. Sequencing: fix InteractiveCanvas regression FIRST (now done), then full-screen → DrawingOverlay → toolbar mockup.
- **Training guest squad-persist fix** (`909e7105`) — two-path data-layer fix so every attendee ends up in `training.squads{}` regardless of invite order, closing the "Bez składu" / "unassigned" bucket in `TrainingCoachTab` for invited guests. **Option 1** (`AttendeesEditor`): atomic invite-time placement — `toggleAttendee` ADD branch + `applyPreset` combine `{ attendees, squads }` into single `updateTraining` write, picking smallest existing squad with `SQUAD_META` tie-break. Idempotent; no-op when no squads exist (Option 2 catches). **Option 2** (`SquadEditor`): persists the existing mount-time round-robin auto-distribute — new `squadsDiffer(a,b)` helper gates; `scheduleSave(next)` when `next !== stored`. Round-robin byte-for-byte unchanged; only the "throw away if no drag" behavior is fixed. Discovery brief (read-only, same session) verified verdict was container fix not architectural — coach view + identity layer already use training context correctly, the real root was squads-write-missing on invite. NO UI surfaces touched. Build clean 5.06s; precommit pass; bundle unchanged 227.89 kB / 68.54 kB gzip. Known limitation tracked in NEXT_TASKS: existing squad-vs-squad matchups have frozen `homeRoster`/`awayRoster` snapshots → guests NOT backfilled retroactively (separate ticket).
- **§ 64.9 Canvas Step #5** — HeatmapCanvas → BaseCanvas + FieldView deprecation (`cb28a26a`). In-place refactor (~300 LOC draw body moved verbatim to BaseCanvas's `draw` render-prop callback); 3 FieldView call sites migrated to direct `<HeatmapCanvas>` (ScoutedTeamPage:654/:674 + TrainingResultsPage:376); **§ 64.9 step #8 (FieldView deprecation) collapsed into step #5** — FieldView.jsx DELETED (207 LOC) after Step #4 reduced its non-heatmap branch to dead code. `pinchZoom`/`pan` opt-in default off (step #11 flips on for landscape coach view); **loupe NEVER** — naturally inert via two existing consumer-side gates (touchHandler.js:178/352 `editable||layoutEditMode` + drawLoupe call-site absence), zero touchHandler changes. **Sizing deviation:** brief STEP 3 specified height-first with cap — impl chose width-first (no `maxCanvasHeight`) because BaseCanvas:144-158 math would have rendered 1000×500 portrait → overflow:hidden left-crop. Today's `HeatmapCanvas:34-39` `min(aspectH, maxH)` reduces to width-first in portrait, landscape letterbox cap is dead code in step #5 scope (landscape activation = step #11). Documented in HeatmapCanvas header. Bundle −0.61 kB / −0.08 kB gzip.
- **First beneficiary unblocked** — Step #11 (landscape coach view on ScoutedTeamPage heatmap, § 64.10) now buildable on top of unified base (Steps 2 + 4 + 5).
- *(Step #4 — `2b6a473`, FieldCanvas → InteractiveCanvas — shipped earlier 2026-05-24 in the previous session; closeout `19c8a9fd` per session-reset checkpoint above.)*

### ✅ This session closed (2026-05-23)
- **Sentry `onToolbarAction` ReferenceError fix** (`touchHandler.js:309`) — `e4f188f`. Killed the empty-canvas-tap-with-toolbar-open `ReferenceError` (`?.()` doesn't protect against undeclared identifiers).
- **Phase 2.3.d — UI delete → retire + orphan cleanup** (`bf65242`). Both UI delete-team callers now `retireTeam`; 1 confirmed global orphan (`SKASUJ MNIE`) hard-deleted; global/workspace count balanced (298/298).
- **Firestore rules coverage audit** — clean: no real-exploitable holes; workspace isolation enforced server-side; no UI-only sensitive ops; no dev-open clauses. Snapshot: `docs/archive/audits/RULES_COVERAGE_AUDIT_2026-05-23.md`.
- **§ 49.10 rules tighten — selfReports cross-pid** (`c2fb9ba`) — audit gap #2. CREATE = `isLinkedSelfPlayer(slug,pid)`; UPDATE/DELETE = `isCoach OR isLinkedSelfPlayer` (carve-out required for § 70 matcher + § 70.11 Stage 4 override). `firebase deploy --only firestore:rules` ran first (live rules updated), then merge + `npm run deploy`. **Two-step deploy pattern recorded for future rules changes.**
- **Role-model + FAZA-2 direction + data-protection principle** captured in `DESIGN_DECISIONS.md § 74` (new).

### ✅ CLOSED — `adminUid` repoint (gap #1, 2026-05-27)
Audit gap #1 closed. `workspaces/ranger1996.adminUid` repointed from the dead pre-email-auth uid `JDDCmHSQcMQ6JADYtOr9VWnRgNQ2` to Jacek's live uid `OPAHJZa6fROpL7DPVCN3lQiQRr52` (resolved from `jacek@epicsports.pl` at script run time). Dead-uid tombstone in `userRoles` removed (585 → 584 keys — B15 cleanup still owed for the rest). `adminTransferredAt` server-timestamp stamped. One-shot at `scripts/migration/repoint_adminuid.cjs` (+ `repoint_adminuid.cmd` wrapper, `65cd563f`); executed by Jacek 2026-05-27 with admin-SDK creds. Idempotent — re-run is a no-op now. **Note:** doc-only entries since 2026-05-23 saying "NO ACTION: adminUid repoint stays unauthorized" are historical and no longer accurate.

### 🎯 Next session — start here
1. **Phase 3.c.2 Stage 7.4 — on-device UI smoke owed** (code/rules/DB ✅; 5-step checklist below). Then 3.c.3 PII scoping (= audit gap #3, § 65.3 Q4 — significant lift, must precede FAZA-2 onboarding).
2. **`adminUid` repoint** — *not on the queue*; explicit go required (see § "NO ACTION" above).
3. **Remaining fragility cluster** (NEXT_TASKS § 68 + post-NXL list, no ordering owed):
   - dead-code prune: `setPointWithId` / `setTrainingPointWithId` (`dataService:340-344, 705-709`).
   - `type:'practice'` dead discriminator (3 UI spots, 0 prod docs).
   - `leaveWorkspaceSelf` no `ADMIN_EMAILS` email fallback (narrower than `isSuperAdmin`).
   - cross-device same-UID presence banner (post-NXL follow-up).
   - § 42 docs update (point IDs now auto-generated).
   - `computeIsLastAdmin` blind to elevated.
   - `members[]` 569 dead-uid prune.
4. **Track A Canvas — InteractiveCanvas tap/drag regression ✅ FIXED** (`6f7158f7`, 2026-05-24). BaseCanvas wrapped-setter restoration. **Post-deploy smoke owed (🔴 #1 = scouting hot-path):** (a) MatchPage scouting → tap existing player → toolbar opens (Assign/Hit/Shot/Del); (b) MatchPage scouting → drag existing player → marker follows finger, canvas does NOT pan; (c) MatchPage scouting → tap empty space → places new player (regression check); (d) pinch-zoom + pan with no selection still work (regression check); (e) BunkerEditorPage → drag bunker anchor → bunker follows finger, NOT pan. Also still owed from prior shipments today: Canvas Step #5 (`cb28a26a`) — MatchPage heatmap tab toggles `showShots`/`heatmapSide`/`previewPointId`; TrainingResultsPage source-filter pills; ScoutedTeamPage collapsed+expanded. Training guest squad-persist (`909e7105`) — invite guest under existing squads vs invite-before-squads-formed. **Next active 🎯 (§ 75 sequencing unblocked):** **full-screen generalized** (#11 extended from ScoutedTeam to universal — both triggers: auto-on-rotate via `useLandscapeMode` + explicit btn) **OR** **DrawingOverlay impl** (composable, `perfect-freehand`-based, mode-not-gesture, available on Match scouting / Tactic / ScoutedTeam, contextual persistence). Pre-impl code-verify of existing TacticPage freehand recommended per § 75. **OR** classic Track A Step #6 (`LayoutAnalyticsPage → AnalyticsCanvas`) — pure technical refactor on top of BaseCanvas. Track B Phase 2.4 (TeamMemberships) still queued.
4b. **Training guest squad-persist fix ✅ shipped** (`909e7105`, 2026-05-24). Two-path data-layer fix (AttendeesEditor atomic invite-time + SquadEditor mount-time auto-distribute persist) so every attendee ends up in `training.squads{}`. **Post-deploy smoke owed:** (a) trening z istniejącymi składami → zaproś gościa z AttendeesEditor → Coach summary pokazuje go pod jakimś składem, NIE "Bez składu"; (b) nowy trening → zaproś kilku attendees PRZED uformowaniem składów → otwórz SquadEditor → po ~300 ms debounce wszyscy w składach po reloadzie; (c) free-play matchup regresja: stats gościa nadal liczone (free-play roster = attendees, niezależne od squads); (d) Sentry: zero nowych errorów na invite / SquadEditor mount. **Known limitation:** stare squad-vs-squad matchupy z zamrożonymi rosterami NIE są backfillowane (separate ticket — see NEXT_TASKS).
5. Queued (mockup-first): `LogRow` card enhancement — event + Rozbieg/Strzały labels (`§70.x deferred`, awaits an Opus mockup).
6. Queued (mockup-first): **Home / landing view** — app lands on Settings when all events are closed (`§ 73`). Direction = one shared role-aware home; awaits an Opus clickable mockup.
7. Open product threads (§ 74.3, not decided): (a) global-vs-workspace boundary for workspace_admin writes; (b) per-workspace isolation for global collections (= 3.c.3); (c) provisioning + subscription lifecycle; (d) multi-workspace user UX/data-flow.

**Stage 7.4 on-device UI smoke checklist** *(super_admin)*: AdminTeamsPage → edit a team (rename/league) ✓ no console error; retire a team ✓ moves to retired view; unretire ✓ comes back active; retire a parent ✓ children-orphan warning fires; Sentry has no fresh rules-permission errors during the run.

---

## 🚧 Currently in flight

**Nothing — all of today's work merged + deployed.** 16 ships landed 2026-05-27 (see "Recently shipped" below). Last deployed HEAD: `fad7dc7b` (B5 / § 89 scout autosave). Subsequent commits (`9e05c738`, `0561fd3b`, this one) are docs-only closeout.

**Open work** is documented under "Next on deck" + "Awaiting decision" below. No infrastructure changes pending (still Spark plan; Blaze upgrade scheduled for Phase 3/4 boundary per `MULTI_TENANT_MIGRATION_PLAN.md`).

**Phase 3 (permissions) status:** 3.a–3.c.1 + 3.c.2 SHIPPED. ✅ Phase 3.c.2 Stage 7.4 code/rules/DB smoke PASS (`b9f9bc1`, 2026-05-23). ✅ 2026-05-20 team-delete repro RESOLVED via Phase 2.3.d (`bf65242`). ✅ Audit gap #2 (selfReports cross-pid) shipped as § 49.10 (`c2fb9ba`). **Outstanding:** Stage 7.4 on-device UI smoke (super_admin, AdminTeamsPage — 5-step checklist in the Next-session block above). Then 3.c.3 (PII scoping = audit gap #3 — significant lift, must precede FAZA-2 onboarding), 3.d–3.f.

**Track C — Multi-source reconciliation (§ 70 / Klocek 2 / Phase 1b) — 🎉 COMPLETE (2026-05-22).** All stages shipped + deployed: 1 (Foundation), 1b (free-play coach UI), 2 (matcher + write-back propagator, VERIFIED on real data — PPT smoke 2 PROPAGATED · 0 BAD), 3 D1+D2 (granular read + event aggregation), **4 (manual override — "Needs review" queue on `TrainingResultsPage`, `e5d963e`/§ 70.11)**. Plus § 70.9 "Samoocena" (player self-logs on the profile), § 70.10 D1 self-log dot placement fix. Commits: `373cc84`, `01a93ed`, `184c04c`, `d46c1ff`, `000fa73`, `194c755`, `b500973`, `5cf783a`, `e5d963e` (dotted-path fix `56ee53f`).

---

## 🩺 Incident — 2026-05-20 leave / role-loss (resolved)

During Phase 3.c.2 Stage 7.4 smoke, Jacek found his player profile unlinked and himself missing from the members list. Root cause: he had clicked **Leave (Wyjdź) at 14:35 UTC** on the old guard-less client (~1h *before* Bug 1 was diagnosed — pre-fix). `removeMember` ran — wiped `userRoles[Jacek] → []` and unlinked his **workspace** player doc (`unlinkedAt` set); the **global** player doc stayed linked → a global/workspace split. `autoEnterDefaultWorkspace` re-added him to `members[]` but not his roles. **Resolved:** workspace player re-linked (global was already correct); `userRoles` re-assigned `scout/coach/admin`; the MembersPage § 68 fix now makes a super_admin with `userRoles=[]` visible regardless. **Lesson:** verify ship/state status against the repo, not memory or assumption — a "shipped" belief that was only "locked", and a post-incident value mistaken for a "long-standing" one. Fragility follow-ups in NEXT_TASKS.

---

## 🚢 Recently shipped (last ~10 days)

> **Source-of-truth caveat:** Opus does not have direct repo access this session. The following are reconstructed from memory + earlier deploy logs. **CC: please verify against `DEPLOY_LOG.md` and patch missing entries when updating this file.**

| Date | Branch / commit | Summary |
|---|---|---|
| 2026-05-27 | `fad7dc7b` (merge — `feat/b5-scout-autosave-draft` / `d5db7af4`) | **§ 89 / B5 scout point autosave draft** — localStorage debounced (2 s idle) snapshot of the in-progress scout point (`draftA + draftB + outcome + draftComment + isOT + annotations + fieldSide + activeTeam + editingId`) + restore on return + "zapisano" indicator in editor PageHeader subtitle (~4 s fade) + "Porzuć draft" in editor `MoreBtn` ⋮ menu (ConfirmModal). Helper `src/services/scoutDraft.js` (~120 LOC) mirrors § 48.8 WizardShell pattern; key `scout_draft__<kind>__<eventId>__<containerId>__<scoutingSide>__<editingId\|\|'new'>`; TTL 24 h; non-pristine guard. Commit path / Save gate / concurrent Firestore `status:'partial'` semantics **unchanged** — purely additive local layer. Restore precedence: Firestore `editPoint` wins (restore effect declared before the `?point=` auto-attach). Earlier B5 framing ("schema change + sparing rozkmina") superseded — autosave is local + event-model-agnostic. Bundle main 234.11 → 234.35 kB (+0.24 / +0.08 gzip). |
| 2026-05-27 | `f5a3b677` (merge — `feat/b10-logrow-labels` / `59248e32`) | **B10 LogRow eyebrow + Rozbieg/Strzały label gutter** — shared `LogRow` (3 mounts: `/player/log` own, PlayerStatsPage Samoocena, TrainingResultsPage needs-review). New optional `eventLabel` TRI-STATE prop: `undefined` (hide) / `string` (Calendar icon + "Trening · {name}") / `null` (orphan "Bez treningu" italic). Wired on PlayerStatsPage Samoocena only (cross-event surface) via `useTrainings()` lookup; other 2 mounts omit (already event-scoped). 2-col grid `gridTemplateColumns: 'auto 1fr'` replaces line-1/line-2 stack: 8/700 uppercase labels left, existing breakout + `shotsText` verbatim right. `shotsText` helper untouched. PL+EN keys: `logrow_breakout`, `logrow_shots`, `logrow_event_prefix`, `logrow_no_event`. Training-granularity only — `matchupId`/`pointNumber` always null in selfReports today (never backfilled) → matchup-level not reachable without schema change. |
| 2026-05-27 | `e4c7c585` (merge — `feat/a2-shotdrawer-v2-dragmove-menu` / `0c00c9d2`) | **A2 v2 ShotDrawer — drag-move-shot + tap-marker menu (Delete + Kill-toggle).** Closes the v2 follow-up explicitly deferred at § 86. New `draggingShotRef` + ref-wrapped `setDraggingShot` in BaseCanvas (PROJECT_GUIDELINES § 9 silent-death trifecta respected); `handleDown` shoot-branch arms on `findShot` hit + suppresses pan; `handleMove` fires `onMoveShot(pi, si, pos)` continuously past a 6-px clientXY threshold; `handleUp` dispatches `onShotMenu(pi, si)` if not moved (legacy `onDeleteShot` kept as fallback). `isKill` render added to ShotDrawer drawFn (red ring + 💀 glyph). `ShotMenuOverlay` child of BaseCanvas mirrors `InteractiveChrome.toolbarPos` math verbatim. MatchPage wiring: `onMoveShotIdx` (no pushUndo per move) + `onToggleKillShotIdx` (pushUndo on toggle — save-worthy mutation). § 9 trifecta verified: ref-wrap ✓, `editable:true` intact ✓, per-handler destructure ✓. |
| 2026-05-27 | `071c032b` (merge — `chore/dualwrite-orphans-b15` / `8367c7d1` + `c0595319`) | **PART 1 dual-write orphan removal + PART 2 B15 audit/cleanup scripts.** Removed `subscribePlayers` (`dataService.js:200`) + `subscribeTeams` (`:275`) — zero-caller since Phase 2.2.b / 2.3.b moved React consumers to global `usePlayers` / `useTeams`. Verified all other `subscribe*` exports have ≥1 caller. § 88 zone CRUD also zero-caller today but intentional future-API. Write-side dual-write (addPlayer/addTeam workspace mirrors) OUT OF SCOPE — Phase 2.2.d/2.3.d. **B15 scripts** (`scripts/migration/audit_dead_userroles.cjs` + `cleanup_dead_userroles.cjs` + `.cmd` wrappers) shipped, gated by `CLEANUP_DEAD_USERROLES_CONFIRMED=1`. Criterion (uid ∉ members AND uid ≠ adminUid AND `/users/{uid}` missing AND email ∉ ADMIN_EMAILS) candidate per brief; awaiting Jacek audit run + confirm before destructive cleanup. **PART 3** (Phase 2.2.d `linkedUid` backstop collapse) `[ESCALATE TO JACEK — DECISION]`, not touched. |
| 2026-05-27 | `e8ec169a` (merge — `fix/...kiosk-b14`) + `dd216cc9` (B13 + B19) + `98c6f24d` (B16-B18) | **B-series hygiene batch + KIOSK data-quality.** B13: `leaveWorkspaceSelf` ADMIN_EMAILS path added (mirrors all 3 `isSuperAdmin` paths). B14: `computeIsLastAdmin` (MoreTabContent + TrainingMoreTab) widened to all 4 admin paths matching `roleUtils.isAdmin/isSuperAdmin` — previously gated on role-array `'admin'` only, returned false for everyone in prod. B19: LivePointTracker "✓ Zapisz" CTA bumped 36→44 px (§ 27 touch-target). B16: dropped `setPointWithId` + `setTrainingPointWithId` (zero callers since 2026-05-15 NXL Czechy auto-ID hotfix). B17: `type:'practice'` dead discriminator removed from 4 spots (0 prod docs per § 69 backfill). B18: § 42.1 closeout note added (doc-ID scheme retired; merge semantics preserved). KIOSK `scoutedBy` data-quality: `writerUid = user?.uid \|\| activePlayer?.linkedUid \|\| kiosk.activePlayerId` — was misattributing to tapping player's linkedUid; not security (carve-out rides `isScout`). |
| 2026-05-27 | `295c6bcb` (merge — `fix/users-create-value-check` / `a25d4e88`) | **gap β sibling — `/users/{uid}` create-time `roles` + `globalRole` value-check (rules-only).** Closes the deferred defense-in-depth sibling from the 2026-05-27 3.c.2 write-discovery. `allow create` now constrains `roles` ∈ {`[]`, `['player']`} and `globalRole` ∈ {null, absent}. Mirrors `getOrCreateUserProfile` writes. Phase 3.a migration unaffected (admin SDK bypasses rules). Deployed via `firebase deploy --only firestore:rules`. **All known 2026-05-27 3.c.2-discovery hardening follow-ups CLOSED** (gap α + gap β + sibling). |
| 2026-05-27 | `29ecc13f` (merge — `fix/gap-alpha-shot-playerid` / `385297a4`) | **gap α — self-log shot `playerId` cross-check (rules-only).** `isSelfLogShotCreate` / `isSelfLogShotOwned` now `get(/players/{playerId})` and require `data.get('linkedUid', null) == request.auth.uid` (identical pattern to `isLinkedSelfPlayer`). KIOSK + post-hoc propagator + PPT all ride `isScout` / different collection — unaffected. selfLog flag default OFF → exposure was theoretical; hygiene before re-enable / workspace #2. Deployed via `firebase deploy --only firestore:rules`. |
| 2026-05-27 | `c716d5f8` (merge — `fix/gap-beta-selfrole-validation` / `b5514b71`) | **gap β — self-join + self-leave value/own-key gates on `userRoles` (rules-only).** Closes the § 49.11 latent privilege-escalation gap. Both envelopes now carry: (1) value gate via `isSelfJoinRoleValue(r) = r is list && (size == 0 \|\| r == ['player'])` on `userRoles[auth.uid]`; (2) own-key gate restricting the diff on `userRoles` to `[auth.uid]`. Both short-circuited by `!('userRoles' in affectedKeys)` → returning-member re-entry writes unaffected. PRE-FLIGHT confirmed SELF-KEY-ONLY client write semantics (Phase 2.2.b `setDoc(merge:true)` deep-merges Map fields; prod ~584 `userRoles` keys consistent with preserve semantics). |
| 2026-05-27 | `e53264f2` (merge — `feat/unified-zones-v1` / `518eda70` + `c4ab61af`) | **§ 88 Unified Zones v1 — model + editor + scouting pill + Strefy summary.** `layout.zones[]` shape replaces the 3 hardcoded fields with dual-write migration: each zone has `{id, name, color, polygon, type?}` — name editable (team callout vocabulary, e.g. "ORANGE"), color from `COLORS.zonePalette` (amber reserved). LayoutDetailPage editor: zone card list (swatch + name + pencil + trash) + "+ Dodaj strefę" + draw banner `Narysuj zakres strefy {NAME}`. MatchPage canvas: zone-colored pill below placed-player markers via `pointInPolygon` (first-zone-in-order wins; `bumpShotOriginAtStart` carve-out preserved). ScoutedTeamPage: net-new above-fold "Strefy" section between Strzelanie and Kluczowi gracze (zone-color dot + name + off-break % + count). Big Move retains its own pinned section with bunker-attribution (§ 87). `coachingStats.danger/sajgon` + `computeBigMoves` UNTOUCHED — fed via dual-written legacy fields. **Transit % PARKED** (opponent bump-rate 4.7% via `scripts/migration/count_opponent_movement.cjs` — capture-behavior gap, not model). |
| 2026-05-27 | `fadff8aa` | **§ 87 zones doc** — captures Jacek's domain expansion (Danger/Sajgon/Big Move/comms-callout zones all = ONE object type; names are data, never hardcoded) + unified-model decision + Big Move coexistence note pinning bunker-attribution analytic (`computeBigMoves`). |
| 2026-05-27 | `3126e339` (merge — `fix/b7-completeness-card` / `e1ae18e7`) | **B7 completeness card reposition + collapse-by-default (+ B6/B8 closures).** Match review: `CompletenessCard` moved from above the Points list to below (same scroll container, end of review content) per § 27 deference. Collapse-by-default via inline `useState(false)` + tappable `<button>` header (Lucide `ChevronRight/Down`, 44 px touch, subtle `surfaceLight` bg-on-press, neutral `textDim` chevron). No `localStorage` / no persistence. Bundled doc closures: **B6** Auto-swap → DONE (no-op; previously fixed 2026-04-28 via `teamSideMemoryRef`); **B8** Strzela% → DEFERRED — shot-% metric semantics parked pending dedicated "data trust / validation" workstream (Jacek doesn't currently trust the scouted data → tuning denominator premature). |
| 2026-05-27 | `2b074891` (script) + `e120cb42` (docs) | **adminUid repoint (gap #1) + Phase 3.c.2 audit closure.** One-shot `scripts/migration/repoint_adminuid.cjs` (+ `.cmd` wrapper) resolved Jacek's uid from `jacek@epicsports.pl` at run time; repointed `workspaces/ranger1996.adminUid` from dead pre-email-auth uid `JDDCmHSQ…` to live uid `OPAHJZa6…`; cleared dead-uid tombstone (585 → 584 `userRoles` keys); stamped `adminTransferredAt`. Idempotent. Closes rules-audit gap #1. **Phase 3.c.2 discovery (same session): rules already shipped via `8e8dda0c` + `f5adf292` weeks earlier — NEXT_TASKS row stale; reconciled in same closeout commit.** B12 also rediscovered as **shipped 2026-05-01** via QuickLog hotfix v3+v4 (`b8aa7cf2` + `0fec6b26`) — Option B contract; NEXT_TASKS row never reconciled until today's audit. |
| 2026-05-25 | `293576a8` (merge — `feat/draw-stage2-scouted-annotations`, 1 commit `0d135c6f`) | **§ 78 Draw Stage 2 — ScoutedTeam annotations (Plan coacha 2a + Notatki scouta 2b).** Closes § 75 Draw sequencing. **2a Plan coacha** (`scouted.annotations`, editable per scouted-team, canonical coords, default toggle ON, both orientations on the expanded heatmap — Match's landscape-only chip gate per § 77 does NOT apply here): `✏ Rysuj` chip top-right → drawMode → DrawToolbar mounts → DrawingOverlay shows live editing copy via HeatmapCanvas `children` slot → on Done: `ds.updateScoutedTeam(tid, sid, { annotations: strokesToFirestore(strokes) })` (uses existing updater — no new dataService fn). Load gated on `!coachDrawMode` (avoids clobbering in-progress edit on remote update). **2b Notatki scouta** (`point.annotations` from Stage 1, per-point, mirrored at READ time via extended `mirrorPointToLeft`, read-only display, default toggle OFF, respects `filterMatchId`): new private `mirrorAnnotations()` helper normalizes Firestore object/array shape and maps each stroke's `pts` through generic `mirrorPos`. `mapOnePointForTeam` propagates automatically via existing spread. **Shared refactor:** extracted `paintStroke()` to `drawStrokes.js` — single source of truth for perfect-freehand stroke render (DrawingOverlay live capture + HeatmapCanvas static aggregate). Constants `STROKE_SIZES`/`STROKE_COLORS`/`FREEHAND_OPTIONS` hoisted with re-export from DrawingOverlay for back-compat. § 77 hotfix SVG path bug history (Q needs 2 coord pairs) now lives with `paintStroke` so it can't drift. **HeatmapCanvas signature extension** (isomorphic with InteractiveCanvas Step #4): new props `drawMode`, `onDraw{Start,Move,End,Abort}`, `children`; self-closed `<BaseCanvas />` → `<BaseCanvas>{children}</BaseCanvas>` so DrawingOverlay composes via `useBaseCanvas()` context. New render-path props `showAnnotations`/`showCoachPlan`/`coachAnnotations` + two new branches in `drawHeatmap` (per-point mirrored scout notes + per-team canonical coach plan; coach plan hidden during drawMode to avoid stale-saved + live-edit double rendering). **ScoutedTeamPage wiring**: 7 state hooks + 9 handlers (eraser radius scales with stroke size at 1000×500 reference) + chip + DrawToolbar + 2 new toggle pills (Plan coacha + Notatki scouta, neutral amber styling per § 27 — multi-color stroke = no semantic color); chip + toolbar mount in expanded branch only (miniature 110px preview stays read-only). `flex-wrap` added to toggle row so 5 pills fit on narrow widths. **`§ 4.3` doc-correction landed** (the "NO Positions/Shots toggle" claim was over-broad; the toggle has existed on ScoutedTeam since § 60.1; now scopes the policy to Match B3 heatmap going forward). **Storage distinction:** `point.annotations` (Stage 1, per-point, mirrored at read) ≠ `scouted.annotations` (Stage 2, per-team, canonical) — same Firestore object shape + same `strokesToFirestore`/`strokesFromFirestore` helpers, different semantics. **Off-limits untouched** (`git diff --name-only`): MatchPage (only impacted by paintStroke import surface — no behavioral change, chunk shrunk **−7.10 kB**), TacticPage, BunkerEditorPage, LayoutDetailPage, LayoutAnalyticsPage, FieldCanvas legacy, BallisticsPage, ballisticsEngine, dataService (existing `updateScoutedTeam` covers 2a), schema (additive `annotations` field — no migration), Firestore rules. **§ 27 PASS** (amber on interactive toggles per carve-out, chip glass matches landscape pattern, z-stack clean DrawingOverlay 15 inside frame + chip 35 + toolbar 40 in outer wrapper, Lucide only, ConfirmModal for Clear via DrawToolbar reuse). Build clean 5.89s, precommit pass. Bundle: ScoutedTeam 47.22 kB / 11.89 kB gzip (+2.64 kB / +0.90 kB net); MatchPage 69.94 kB / 20.49 kB gzip (**−7.10 kB / −2.19 kB gzip net** — paintStroke extraction shrunk DrawingOverlay internals); main `index.js` 228.19 kB / 68.66 kB gzip (+0.30 kB / +0.12 kB net). 5-step smoke (DEPLOY_LOG): 2a portrait+landscape + 2b mirror + Plan coacha toggle + miniature read-only + **🔴 arbiter regression on HeatmapCanvas** (drawMode 1-finger draws + 2-finger STILL zoom/pan + 2nd-finger mid-stroke abort). **§ 75 Draw sequencing COMPLETE** (FS Stage 1 + Draw Stage 1 + Draw Stage 2 + 3 hotfixes). Next major track = FS Stage 2 fast-follow (extend immersive pattern to ScoutedTeam / LayoutDetail / BunkerEditor / LayoutAnalytics — mechanical, same `<FullscreenToggle>`). Rollback: `git revert -m 1 293576a8` (additive — `scouted.annotations` data persists in Firestore, won't render post-revert). |
| 2026-05-24 | `cd9aa448` (merge — `feat/draw-stage1-overlay`, 1 commit `238adfde`) | **§ 77 Draw Stage 1 (DrawingOverlay + Match capture, landscape-only entry).** Per § 75 sequencing step 3 — closes second § 75 piece on top of FS Stage 1 + the InteractiveCanvas regression fix grammar. **DrawingOverlay** (`src/components/canvas/DrawingOverlay.jsx`, NEW) — render-only `pointerEvents:'none'`, perfect-freehand outlines, DPR-scaled with rAF retry, reads transform via `useBaseCanvas()` so strokes track zoom+pan. Exports `STROKE_COLORS` (5: amber/white/red/cyan/green) + `STROKE_SIZES` (3/6/10 px). **BaseCanvas arbiter `drawMode` branch** — surgical addition to `touchHandler.js` (no rewrite). New `drawingRef` sentinel owned by BaseCanvas (sibling of `draggingRef`), threaded into `createTouchHandler`. Branches: `handleDown` pinch path now calls `onDrawAbort()` if drawing-in-progress BEFORE setting `pinchRef` (2nd-finger-mid-stroke abort); `handleDown` after pinch + panStartRef + before field-edit gate → `if (drawMode && 1-finger)` calls `onDrawStart`, sets `drawingRef.current=true`, returns; `handleMove` early-returns `if (drawMode && drawingRef.current && 1-finger)` → `onDrawMove`; `handleUp` early-returns `if (drawMode)` → calls `onDrawEnd` if was drawing, resets all gesture refs, skips ALL field-edit handleUp logic. **DrawToolbar** (`src/components/canvas/DrawToolbar.jsx`, NEW) — floating bar inside canvas frame bottom-center, `left:0; right:0; margin:auto; width:fit-content; flex-wrap`. 5 color swatches (amber-ring on selected) + 3 width pills (amber on active) + Undo + Redo + Eraser (toggle, amber when active) + Clear (Lucide Trash2, ConfirmModal "Cannot be undone via Undo") + Done (Lucide Check, amber CTA). All 44px `TOUCH.minTarget`. Lucide icons only (no emoji). **drawStrokes.js** (NEW pure helpers) — `strokesToFirestore` / `strokesFromFirestore` (Firestore-safe object shape `{"0":{color,size,pts:[{x,y},...]}}`, no nested arrays per § 9 anti-pattern), `eraseAtPoint` + `eraseAcrossStrokes` (sized point-erase: splits strokes where points fall within radius, surviving 2+ point runs become sub-strokes — NOT whole-stroke deletion). **InteractiveCanvas pass-through** — new props `drawMode`, `onDraw{Start,Move,End,Abort}`, `children` (forwarded to BaseCanvas; children render as siblings of `<InteractiveChrome>` inside BaseCanvas frame so DrawingOverlay can read context). **MatchPage wiring** — 7 state hooks (`drawMode`, `annotations`, `redoStack`, `currentStroke`, `drawColor`, `drawSizeKey`, `eraserMode`) + 9 handlers (`handleDrawStart/Move/End/Abort/Undo/Redo/Clear` + `enterDrawMode/exitDrawMode`). `editPoint` rehydrates via `strokesFromFirestore(pt.annotations)`. Both `savePoint` branches (concurrent + solo) write `annotations: strokesToFirestore(annotations)` into the point doc. `resetDraft` clears draw state. Entry chip `✏ Rysuj` (Lucide Pencil) rendered LANDSCAPE-only on Match (`isLandscape && !drawMode` — portrait + portrait-FS = scouting/view-only, NO draw per § 77 decision #1). DrawToolbar mounted when `drawMode`. `enterDrawMode` suspends `toolbarPlayer`/`quickShotPlayer`/`shotMode`/`selPlayer` so canvas is unambiguously the draw consumer. **`perfect-freehand` (MIT ^1.2.3) added to deps** (same lib tldraw uses underneath; tapered iPad/Adobe-style strokes via velocity-based thinning + smoothing; ~1ms outline computation for typical strokes). **Storage** = `point.annotations` Firestore object shape; **NATIVE-orientation coords (no mirror on write)** — Stage 2 will apply `mirrorPointToLeft` at read time. **Off-limits untouched:** TacticPage (existing freehand stays per Jacek decision; unify is FUTURE ticket), ScoutedTeamPage (= Stage 2), QuickLogView, BunkerEditor / LayoutDetail / LayoutAnalytics, FieldCanvas legacy, BallisticsPage, ballisticsEngine, dataService, schema (additive `annotations` = no migration), Firestore rules. **§ 27 PASS** — amber on interactive-active per toggle carve-out; ConfirmModal for destructive Clear; 44px touch everywhere; Lucide only; z-stack clean (DrawingOverlay 15 < InteractiveChrome 19-20 < FullscreenToggle 30 < ✏ chip 35 < DrawToolbar 40; chip + FullscreenToggle mutually exclusive via `isLandscape` gate). Build clean 5.53s, precommit pass. Main bundle index.js 227.89 kB / 68.56 kB gzip **unchanged**; MatchPage chunk 77.04 kB / 22.68 kB gzip (+8.9 kB / +2.87 kB net); vendor-react 171.28 kB / 53.80 kB gzip (+2.09 kB — one-time Lucide icon bump: Pencil/Undo2/Redo2/Eraser/Trash2/Check/Minus/Equal). **§ 75 unblock COMPLETE Stage 1** (FS + Draw both shipped). Next major piece = Draw Stage 2 (ScoutedTeam annotation aggregation + heatmap toggle). 5-step smoke (DEPLOY_LOG): #1 chip→draw, #2 toolbar paths, #3 persistence reopen, **#4 🔴 arbiter regression (1f draws + 2f STILL zoom/pan + outside drawMode tap/drag-player STILL works)**, #5 portrait no chip. Rollback: `git revert -m 1 cd9aa448` (perfect-freehand stays as harmless unused dep; `point.annotations` data persists but won't render). |
| 2026-05-24 | `884937d8` (merge — `feat/fullscreen-stage1-immersive`, 1 commit `5def9218`) | **§ 76 Full-screen Stage 1 (Match + Tactic).** Per § 75 sequencing step 2 (regression fix landed first earlier today). Generalizes § 64.10 / § 64.9 step #11 (landscape coach view, ScoutedTeam-specific) into a universal data-canvas full-screen capability. **`useLandscapeMode()` extended** — new `fsActive` state + `setFullscreen` + unified `immersive = isLandscape || fsActive`. `canvasMaxHeight(L,P)` uses landscape offset whenever immersive → portrait-FS = field fills viewport. Backward-compat preserved (legacy consumers reading only `{isLandscape, canvasMaxHeight}` still work). **`<FullscreenToggle>`** (new `src/components/canvas/FullscreenToggle.jsx`) — shared portrait-only trigger; Lucide `Maximize2`/`Minimize2`; 44px touch (`TOUCH.minTarget`); amber accent on active (§ 27 carve-out for interactive toggles); absolute top-right in canvas frame, z:30 above canvas; backdrop blur dark glass; returns `null` in landscape (rotation already immerses). **MatchPage scout** — 6 sites swapped `isLandscape` → `immersive` (maxWidth + 4 chrome-hide gates + 2 floating control gates); toggle mounted in canvas frame. **TacticPage** — 5 sites swapped same pattern; toggle mounted; draw-mode (`✏️`) becomes available in portrait-FS via the floating controls path (was landscape-only before). `isLandscape` retained at both pages only for the `<FullscreenToggle>` visibility gate. Landscape behavior **byte-for-byte unchanged** (`isLandscape ⇒ immersive`). `fsActive` is portrait-state with no auto-reset; stuck-state safety via persistent toggle in portrait-FS. **BaseCanvas untouched** in Stage 1 (lift of `<FullscreenToggle>` to BaseCanvas chrome = future § 64 rung after DrawingOverlay impl experience). Off-limits verified untouched: `BaseCanvas.jsx`, `touchHandler.js`, `InteractiveCanvas.jsx`, the 4 fast-follow surfaces (ScoutedTeam/LayoutDetail/BunkerEditor/LayoutAnalytics — separate ticket, same pattern), `BallisticsPage`, `ballisticsEngine.js`, `FieldCanvas` legacy, schema, dataService, rules. § 27 PASS (color discipline, elevation, typography, cards N/A, navigation N/A, zero anti-patterns). Build clean 6.04s, precommit pass. Main bundle `index.js` 227.89 kB / 68.54 kB gzip — **unchanged**; MatchPage chunk 68.13 kB / 19.81 kB gzip (+0.12 kB net); vendor-react 169.19 kB / 53.41 kB gzip (+0.73 kB — Lucide Maximize2/Minimize2 one-time icon bump). 5-step smoke (DEPLOY_LOG) — #1 portrait toggle immerse, #2 minimize exit, #3 rotate-landscape regression, **#4 tap/drag-player regression in portrait-FS** (the `6f7158f7` fix must hold), #5 Tactic draw-toggle in portrait-FS. Fast-follow ticket: ScoutedTeam heatmap / LayoutDetail / BunkerEditor / LayoutAnalytics — mechanical extension. **§ 75 unblock:** closes one of two § 75 sequenced items (full-screen #11 generalized); DrawingOverlay is the next major piece, gated on clickable toolbar mockup; iPad/PencilKit arbitration model locked (BaseCanvas-as-arbiter, NOT event-forwarding — see NEXT_TASKS session decisions). Rollback: `git revert -m 1 884937d8` (single-shot). |
| 2026-05-24 | `6f7158f7` (merge — `fix/basecanvas-dragging-ref`, 1 commit `009de46c`) | **Fix InteractiveCanvas tap/drag regression — restore wrapped dragging setters in BaseCanvas.** Per § 75 sequencing (fix regress FIRST), this clears the runway for full-screen + DrawingOverlay impl. **Root cause:** `BaseCanvas.jsx:172-177` used raw `useState` setters for `dragging` / `draggingBunker` while keeping `draggingRef` / `draggingBunkerRef` side-by-side. `touchHandler.js` reads the **refs** (handleMove:338,444 drag-player; handleUp:471,614 tap-detection), so `setDragging(hit)` updated React state but the ref stayed frozen at null. Tap-element → toolbar and drag-element → move both died silently; zoom/pan/place stayed alive (don't depend on draggingRef); the pan path even won what should have been drag-player gestures (`if panStartRef && dragging === null && draggingBunker === null`) → "drag dead, pan wins" symptom matched exactly. Same bug applied to bunker drag. **Provenance:** introduced structurally at Step #2 BaseCanvas extraction (`ecc850ce`, 2026-05-23) but dormant (no consumer rendered BaseCanvas yet); went live at Step #4 InteractiveCanvas migration (`71179616`, 2026-05-24) when MatchPage / TacticPage / LayoutDetailPage / BunkerEditorPage moved onto BaseCanvas. Step #2's "additive only, bundle hash bit-identical" claim was true at deploy time — the latent bug shipped under it. **Fix:** restore wrapped-setter pattern from `FieldCanvas:81-86` **1:1** — `setDragging = (v) => { draggingRef.current = v; _setDragging(v); }` + same for `setDraggingBunker`. State preserved (eslint-disable matches legacy). **NO state-drop** in this PR per Opus guidance — keeps draw-effect dep surface unchanged on the hot path; state-drop cleanup deferable separately. **Off-limits untouched** (`git diff --name-only` = `BaseCanvas.jsx` only): touchHandler, InteractiveCanvas, the 4 consumers, FieldCanvas legacy (works on BallisticsPage), ballisticsEngine. No schema / dataService / rules change. **§ 75 unblock:** gesture grammar in BaseCanvas now consistent (ref-state sync); per-screen drift root removed structurally. Full-screen (#11 generalized) + DrawingOverlay impl can build on working grammar. **Anti-pattern codified:** `PROJECT_GUIDELINES § 9 Architektura` — new bullet pinning the bug + fix SHA for future archaeology ("gesture state read by touchHandler via ref MUSI mieć wrapped setter updating ref + state; raw `useState` setter freezes ref → silent tap/drag death"). Build clean 7.48s, precommit pass, bundle unchanged 227.89 kB / 68.55 kB gzip. 1 file +9/−2 LOC. **Rollback:** `git revert -m 1 6f7158f7` (re-introduces the regression — only roll back if the fix itself causes a new symptom). |
| 2026-05-24 | `909e7105` (merge — `fix/training-guest-squad-persist`, 1 commit `6b9bd55b`) | **Training guest squad-persist fix — invite-time + auto-distribute squad persist.** Closes "Bez składu" / "unassigned" bucket in `TrainingCoachTab` for invited guests. Two-path data-layer fix so every attendee lands in `training.squads{}` in Firestore regardless of invite order. **Option 1 (`AttendeesEditor.jsx`):** new `placeIntoExistingSquads(baseSquads, pidsToPlace)` helper — idempotent, smallest-squad with `SQUAD_META` tie-break, returns null when no squads exist (Option 2 catches). `toggleAttendee` ADD branch combines `{ attendees, squads }` into single `updateTraining` write — guest lands in smallest squad atomically, no reliance on SquadEditor being opened. `applyPreset` prunes squads first (existing `syncSquads`) then places newly-added pids on cleaned squads. REMOVE branch unchanged. Imports `SQUADS as SQUAD_META`. **Option 2 (`SquadEditor.jsx`):** new file-level `squadsDiffer(a,b)` set-equal helper. `scheduleSave` + cleanup moved above mount effect so the effect can reference it. Mount effect (line ~27, round-robin logic byte-for-byte unchanged): if `squadsDiffer(initial, next)` → `scheduleSave(next)`. Previously the redistribution was local-only — coach who opened editor and navigated away without dragging anyone threw the recovery away. Effect deps gain `scheduleSave` (stable per `trainingId`). **Discovery same session:** verdict was CONTAINER FIX not architectural — `TrainingCoachTab` leaderboard at `:51` already iterates `training.attendees` and resolves via global `playersById` (NOT team roster); `usePPTIdentity:83-86` already composes team + attendees; `PlayerStatsPage` scope=training is `playerId`-keyed not team-gated. The real root was the squads-write-missing on invite, not a team-vs-attendee resolution bug. Three orthogonal dimensions (player↔team / workspace-membership / training-attendees) kept distinct throughout. Off-limits verified untouched by `git diff --name-only`: TrainingCoachTab, TrainingScoutTab matchup roster snapshot, PlayerStatsPage, useUserNames identity, canvas (BaseCanvas/InteractiveCanvas/HeatmapCanvas/FieldCanvas), BallisticsPage, ballisticsEngine, schema, dataService, rules, new collections. §27 PASS — write-path only, no UI surfaces touched; auto-placement was existing § 32 / § 53 UX-intended state. Build clean 5.06s, precommit pass, bundle unchanged 227.89 kB / 68.54 kB gzip. 2 files +87/−18 LOC. **Known limitation (separate ticket in NEXT_TASKS):** existing squad-vs-squad matchups have frozen `homeRoster`/`awayRoster` snapshots (`TrainingScoutTab:99-101 / :111-112`) → guests NOT backfilled retroactively; future matchups + free-play + coach-summary label all fixed. Rollback: `git revert -m 1 909e7105`. |
| 2026-05-24 | `cb28a26a` (merge — `feat/canvas-step5-heatmapcanvas`, 1 commit `5d640716`) | **§ 64.9 Canvas Step #5 — HeatmapCanvas → BaseCanvas + FieldView deprecation.** In-place refactor: ~300 LOC draw body moved verbatim to BaseCanvas's `draw` render-prop callback (plain arrow function — re-fires on every render → toggle props repaint via closure refresh; matches Step #4 InteractiveCanvas pattern). **§ 64.9 step #8 (FieldView deprecation) collapsed into step #5** — pure-passthrough audit confirmed all 3 FieldView call sites = variant (a) straight delete; FieldView.jsx DELETED (207 LOC) after Step #4 reduced its non-heatmap branch to zero callers. 3 call sites migrated to direct `<HeatmapCanvas>`: ScoutedTeamPage:654 (collapsed 110px preview) + :674 (expanded) + TrainingResultsPage:376 (source-filtered training heatmap with All/Scout/Coach/Player pills). MatchPage:1413 (direct caller, pre-existing) code untouched — props 1:1 compatible with new HeatmapCanvas signature; renderer changed → **#1 priority post-deploy smoke** (scouting hot-path). `pinchZoom` + `pan` opt-in default off (matches today's no-gesture behavior; step #11 will flip on for landscape coach view); **loupe NEVER** per § 64.4 — naturally inert via two existing consumer-side gates (touchHandler.js:178/352 `editable\|\|layoutEditMode` check + drawLoupe call-site absence in HeatmapCanvas draw layer). Zero touchHandler changes — discovery STEP 2 proved loupe-off is free; option (b) chosen over a defensive guard or split. **Sizing deviation from brief STEP 3:** width-first via BaseCanvas (no `maxCanvasHeight` cap) instead of height-first — BaseCanvas:144-158 math with the brief-spec `maxCanvasHeight=canvasMaxHeight(200,200)` would have rendered 1000×500 portrait → overflow:hidden left-crop (broken read-equivalence). Today's `HeatmapCanvas:34-39` `min(aspectH, maxH)` reduces to width-first in portrait (`aspectH=175 ≪ maxH=500` on every phone), landscape letterbox cap is dead code in step #5 scope. Documented in HeatmapCanvas header docblock; step #11 may need `sizingStrategy='width-first-with-cap'` added to BaseCanvas API. DPR `×2` hardcoded literal at `HeatmapCanvas:49` REMOVED — BaseCanvas owns runtime `window.devicePixelRatio \|\| 2` per § 64.8.5 (one of 3 sites flagged in § 64.11 finally migrates). Off-limits invariants verified untouched: FieldCanvas/InteractiveCanvas/BaseCanvas/BallisticsPage/ballisticsEngine/touchHandler/drawLoupe/field-draw\*/MatchPage. § 27 PASS — zero new UI surfaces; all color/typography/elevation/touch preserved (gradients = data viz; HERO amber ring preserved; kill 💀 + danger zone preserved). Frame styling moved canvas-element → BaseCanvas inner-frame div (RADIUS.lg `12 → 10` same as Step #4). Bundle: index.js 227.89 kB / gzip 68.55 kB (was 228.50 / 68.63 — **−0.61 kB / −0.08 kB gzip** net; FieldView delete savings barely offset HeatmapCanvas slight growth + MatchPage chunk +0.08 kB). 4 files changed, +85/−275 LOC net **−190 LOC**. Build clean 5.59s, precommit pass. **Unblocks Step #11** (landscape coach view on ScoutedTeamPage heatmap, § 64.10 — first beneficiary). Rollback: `git revert -m 1 cb28a26a` (single-shot revert of refactor + 3 call-site migrations + FieldView delete). |
| 2026-05-21 | `01a93ed` (merge — `feat/multisource-stage1b-freeplay-ui`, 4 commits) | **Klocek 2 § 70 Stage 1b — free-play coach UI.** "+ Wolna gra" entry in TrainingScoutTab → squad-less QuickLogView `freePlay` mode (pick → zone → per-player survived/eliminated → save); free-play points `outcome:null`, `_meta source:'coach'`. STEP 2.4 winRate-over-decided fixes (TrainingResultsPage + playerStats). Two-squad QuickLogView untouched (`freePlay` defaults false). |
| 2026-05-21 | `56ee53f` (merge — `fix/multisource-meta-array-write`) | **§ 70 dotted-path write-back fix.** `propagateSelfReportToPoint` wrote `_meta` via dotted `field.slot` paths → Firestore converted the array to a map, destroying other slots. Now reads fresh + writes whole per-slot arrays (`normaliseSlots`). Pre-existing since KIOSK Phase 1a; found by the Stage 2 smoke. Anti-pattern → PROJECT_GUIDELINES § 9. |
| 2026-05-21 | `1ddafd7` (merge — `fix/kiosk-lobby-router-context`) | **KIOSK lobby crash hotfix.** `KioskLobbyOverlay` was mounted outside `<HashRouter>`; its `useNavigate` (Brief D deep-link) threw with no Router context → crash on "Przekaż graczom". Moved both KIOSK overlays inside HashRouter. Pre-existing latent bug — unrelated to § 69 / Klocek 2. |
| 2026-05-21 | `41a5ab8` (merge — branch `feat/events-index-model-c`, 5 commits) | **Events Model C — `events_index` (§ 69).** Additive cross-type event index at `/workspaces/{slug}/events_index/` — 1:1 thin mirror of every tournament/sparing/practice/training, written atomically into the event-mutation `writeBatch`. `useEvents()` hook (no consumer yet). Staged deploy rules→client→backfill (rules-first — the index write rides the event batch). Backfill wrote 14/14, 0 errors. Chosen over Model B full unification. New `docs/architecture/FIRESTORE_DATA_MODEL.md` ground-truth DB map. |
| 2026-05-21 | `955508f` (merge — branch `fix/members-visibility-2026-05-20`, 2 commits) | **MembersPage visibility — elevated-member surfacing (§ 68)** — super_admin / adminUid members with `userRoles=[]` now appear in the active list (`isElevated` filter, zero queries); MemberCard "Admin workspace" badge; `RoleChips` row skipped when empty. Fixes the 2026-05-20 Jacek-invisible incident. Limbo bucket dropped (570 no-role members, 569 dead post-purge). 3-item fragility-cluster backlog logged (`adminUid`→non-member, dead-uid prune, super_admin detection scope). |
| 2026-05-20 | `89d5caf` (merge — branch `feat/phase-3-c-2-ownership-rules`, 5 commits) | **Phase 3.c.2 — ownership rules on global /teams/ + /players/.** Per § 65.2. `ownerWorkspaceId` backfilled on 1066 docs (0 errors); `addTeam`/`addPlayer` set it; rules gate create/update by `isSuperAdmin() OR isWorkspaceAdminOf(ownerWorkspaceId)` + immutability clause; delete super_admin-only. Staged deploy (client→backfill→rules), clean compile. § 65.7.5. Stage 7.4 formal smoke + team-delete repro deferred to next session. |
| 2026-05-20 | `dc8288e` (merge — branch `fix/ux-bugs-bundle-2026-05-20`, 3 commits) | **UX bug bundle (Bug 1/2/4)** — Wyjdź disabled for super_admin + adminUid holder (Bug 1); `MembersPage` `isCurrentUserAdmin` → 4-path so super_admin can open `UserDetailPage` (Bug 2/3); `MoreBtn` forwards the event → admin-page kebab TypeError fixed across `/admin/teams\|players\|leagues` (Bug 4). Live-UX-diagnosed; 3 of 4 brief hypotheses corrected against code at pre-flight. |
| 2026-05-20 | `0aac3c1` | **Phase 3.c.1: rules helpers + super_admin awareness (§ 67)** — `firestore.rules` refactor: `isBootstrapAdmin` / `isSuperAdmin` / `isAdmin` 4-path; 5 hardcoded `token.email` sites centralized; dead `/notes/` block removed; § 67 Firestore Rules Architecture doc. Backwards compatible — zero behaviour change. Deployed `firebase deploy --only firestore:rules` (clean compile). Test harness + `isViewer` deferred to 3.c.2. |
| 2026-05-20 | `bddeb10` | **Phase 3.b: super_admin globalRole editing** — Scope reconciled at pre-flight (brief's `/admin/users` console would ~80% duplicate existing MembersPage/UserDetailPage). Minimal path per Jacek: `ds.setUserGlobalRole` + "Global role" section on UserDetailPage (useIsSuperAdmin-gated, super_admin only) + SUPER ADMIN badge on MemberCard + 11 i18n keys + § 65.7.3. First useIsSuperAdmin UI consumer. No new route/page/modal. |
| 2026-05-20 | `8f77d62` | **Phase 3.a: globalRole + isAdmin 4th path + useIsSuperAdmin** — Surgical per § 66.5. Adds `users.globalRole` field, 4th path to `isAdmin` (backwards compat — optional 3rd param), `isSuperAdmin` helper, `useIsSuperAdmin` hook, `userProfile` propagation through both `isAdmin` call sites in useWorkspace. ZERO refactor of § 38 v2.1 infra (§ 66.6). § 65.7.2 doc patch. Code deployed; migration `phase_3_a_globalrole.cjs` ⏳ DEFERRED (no service account on build machine — idempotent, run when available; code backwards-compat so no rush). Phase 3.b unblocked. |
| 2026-05-20 | `docs: § 66` (doc-only — single commit) | **§ 66 § 65↔§ 38 reconciliation (doc-only)** — Unblocks Phase 3.a. Documents § 65 semantic role names ↔ § 38 v2.1 backend mapping. Anti-patterns codified. § 38 stays operational truth; § 65 stays forward-looking permission semantics; § 66 is the bridge. NEXT_TASKS Phase 3.a flipped 🚧 BLOCKED → 🎯 NEXT. No code, no schema, no migration, no deploy. |
| 2026-05-20 | `2997cca` (single commit — doc patch + feature flag + 8 file gates) | **§ 65 Permissions Architecture locked + AI Vision OCR import disabled — DEPLOYED (autonomous, no Firestore rules change).** Pre-Phase-3 prep. Per Jacek-locked Q1-Q4 decisions in chat 2026-05-20. **(1) DESIGN_DECISIONS § 65:** 5 roles (super_admin / workspace_admin / coach / scout / pending_user) + Teams single owner via `ownerWorkspaceId` (Phase 3 addition; `originWorkspace` clarified as audit only, NOT authz signal) + Players tri-mode editing (PBLeagues canonical = super_admin / manually created = workspace_admin in own ws / annotations Phase 3.1+ deferred) + full resource × operation matrix authoritative for Phase 3 rules refactor + 9 anti-patterns codified (no `originWorkspace` authz, no self-elevation, no client-side Anthropic key, no cross-workspace writes by workspace_admin, etc.) + Phase 3 sub-task plan 3.a-g + 3.1+ with risk + deps. Brief asked § 64 but already taken by Canvas Architecture (5952) → landed § 65 per escalation #3 with internal anchors renumbered (§ 64.x → § 65.x). **(2) AI Vision OCR client-side calls DISABLED** via `STATIC_FLAGS.ENABLE_VISION_API: false` (was true but unused — repurposed cleanly): Layout Wizard collapses 3-step → 2-step (Vision Scan step hidden; existing step 2 Finish → bunker editor for manual entry; ProgressBar + subtitle adjusted); LayoutDetailPage `OCRBunkerDetect` render gated (dead code from prior work — `setOcrOpen(true)` had no callers, gate adds defense in depth against future rewire); ScoutTabContent "Import schedule (zdjęcie)" Btn hidden + underlying `ScheduleImport` render gated. All 3 Anthropic API call sites (`VisionScan.jsx`, `OCRBunkerDetect.jsx`, `ScheduleImport.jsx`) get early-return guards inside async handlers (defense layer 3) with comment block referencing § 65 + future Cloud Function migration. Scope expansion vs brief: brief enumerated 2 affordances; 3rd (ScheduleImport schedule OCR) added per Jacek pre-confirmation go-ahead — § 65.5 anti-pattern "Bundle Anthropic API key in client bundle" applies uniformly. **ScheduleCSVImport ("Import harmonogramu (CSV)") UNAFFECTED** — CSV-based schedule import path continues working. Existing Vision-scanned layouts continue rendering (Vision only ran at layout-creation time; stored bunker positions are stable data with zero ongoing API dependency). localStorage Anthropic key reads preserved behind flag — no .env vars touched (previous 2026-04-25 audit removed `VITE_ANTHROPIC_API_KEY` env fallback, this commit closes the localStorage path too at execution level). 8 files +265/-12. Bundle size unchanged. Build clean 5.13s, precommit pass. Re-enable requires Cloud Function migration per § 65.5 anti-pattern — flag flip alone re-opens attack surface. **Unlocks:** Phase 2.4 TeamMemberships design (ownership semantics defined § 65.2), Phase 3 implementation track (clear sub-task ordering § 65.6), tenant onboarding planning. |
| 2026-05-20 | `6638c54` (code + § 63.15.2.X.1 doc patch + 21-consumer refactor + 5 new admin files + 3 new dataService fns) + `970cd76` (post-deploy doc flip) | **Phase 2.3.c: Super admin UI for Teams CRUD + sister team picker + duplicate resolution (§ 63.15.2.X.1) — DEPLOYED (autonomous, no rules change).** Closes Phase 2 Step 3 (Teams) admin path; only 2.3.d cleanup deferred. New `/admin/teams` route lazy-loaded (AdminGuard wrap). **5 new components:** AdminTeamsPage (search/filter/paginate over 132 docs, 8 filter pills incl. ⚠ Duplicates + 🗄 Retired, duplicate banner at top when externalId dups detected, per-row ⚠ prefix + Resolve duplicate action); TeamFormModal (Identity = name + extId + leagues pills + per-league divisions; Sister team relationship = card-style parent + children with Change/Remove inline actions; collapsed Audit section; Retire button in footer); TeamPickerModal (search-and-select for parent/child picker, excludes self + descendants + retired, mode-aware); TeamDuplicateResolutionView (inline modal with side-by-side comparison cards, weighted recommendation heuristic = children × 100 + playerRefs × 1 + recency 0–50, RECOMMENDED green badge, action checkboxes with "re-point tournament/player references" DISABLED per § 63.15.2.X.1 deferral, dynamic safety note green/yellow/red); ChildrenOrphanWarning (3-option radio modal — re-point to selected new parent / cascade retire children / orphan, mirrors Phase 2.2.c aliasIds safety adapted for parent-child). **3 new dataService fns** (Phase 2.3.b dual-write pattern preserved): `retireTeam(id, options)` — soft delete via retiredAt + retiredBy + retirementReason + canonicalReplacementId, handles childAction option; `unretireTeam(id)` — clears all 4 retire fields; `setParentTeam(id, parentTeamId)` — cycle prevention via recursive validateNoCycle helper (depth-limited at 10). **New `useActiveTeams` hook** (asymmetric design): `teams` array filtered to retiredAt == null (for iteration in pickers/lists), `teamsById` map preserves ALL teams (spot lookups by ID still resolve retired teams — MatchPage opponent display, PlayerStatsPage player.teamId, AttendeesEditor guest player team, etc., avoiding "Unknown" rendering when team retired after reference was written). **21 React consumers** mechanically refactored useTeams → useActiveTeams (NewTournamentModal, MembersPage, MatchPage, MainPage, PlayersPage, UserDetailPage, ScoutedTeamPage, TrainingSetupPage, TeamsPage, PlayerPerformanceTrackerPage, ProfilePage, PlayerStatsPage, TeamDetailPage, TournamentPicker, CoachTabContent, AttendeesEditor, ViewAsPlayerPicker, LinkProfileModal, ScoutTabContent, TrainingMoreTab, usePPTIdentity). AdminTeamsPage stays on raw useTeams (admin needs to see retired). **Schema additions** all nullable additive (existing 132 docs treat absent fields as active): retiredAt + retiredBy + retirementReason + canonicalReplacementId. **§ 63.15.2.X.1 doc patch** landed in same commit — locks mockup-reviewed UX patterns: card-style sister picker + cycle prevention + 2-level hierarchy convention; duplicate banner + filter pill + per-row indicator surfaces; recommendation heuristic weights + tournament-refs deferral rationale; soft delete contract + restore flow; reference re-pointing DEFERRED to 2.3.d / manual; children orphan safety pattern (3 options); useActiveTeams asymmetric design rationale; dataService fn signatures with cycle prevention. **Defense in depth admin gate** (3 layers): L1 AdminGuard wraps /admin/teams; L2 component !effectiveIsAdmin early return; L3 Firestore rules /teams/{teamId} delete restricted to jacek@epicsports.pl (from Phase 2.3.b — no rules change in this brief). **NO Firestore rules update.** Reference re-pointing UI checkbox shown DISABLED with explanation — retired team docs remain queryable via teamsById, references resolve with potential "(retired)" indicator (deferred to 2.3.d). RANGER vs Ranger Warsaw externalId dup (1 known case from Phase 2.3.a) surfaces in banner + filter pill — admin can resolve via the new flow (auto-recommendation RANGER as canonical due to 3 children + 300 points; Ranger Warsaw retired). Bundle: AdminTeamsPage chunk lazy-loaded — zero cost for non-admin users. Build 6.03s clean, precommit pass, § 27 self-review PASS (all 7 sections + anti-patterns zero). |
| 2026-05-20 | `97af95a` (code) + `47c32ad` (pending-deploy doc) + `d16ec76` (post-deploy doc flip) | **Phase 2.3.b: useTeams global + dual-write + /teams/ rules (§ 63.15.2.X) — DEPLOYED (sequenced rules + code).** Second Phase 2 Step 3 sub-task; closes the workspace consumption decoupling half. `useTeams` hook refactored in `src/hooks/useFirestore.js:65` — switched from `ds.subscribeTeams` workspace-path subscription to direct `onSnapshot` on global `/teams/`, returns `{ teams, teamsById, loading, error }` (additive — existing `{ teams, loading }` destructures keep working across all 20 React consumers; `teamsById` map provides O(1) lookup for parentTeamId resolution in usePPTIdentity + NewTournamentModal + TeamsPage etc.). Sentry captureException on fetch error. **No helper hooks** (useTeam/useChildTeams deferred) — `teamsById[id]` map covers the parentTeamId use case cleanly without wrapper indirection, matches Phase 2.2.b usePlayers minimalism. **NO consumer file changes for reads** — all 20 React consumers (TeamsPage, TeamDetailPage, MatchPage, ScoutedTeamPage, MainPage, MembersPage, PlayersPage, UserDetailPage, TrainingSetupPage, PlayerPerformanceTrackerPage, ProfilePage, PlayerStatsPage, NewTournamentModal, TournamentPicker, AttendeesEditor, ViewAsPlayerPicker, LinkProfileModal, CoachTabContent, ScoutTabContent, TrainingMoreTab, usePPTIdentity) already imported the centralized hook → automatic pickup. Identical scope-collapse to Phase 2.2.b where usePlayers was also centralized. `dataService.js` `addTeam` + `updateTeam` dual-write to BOTH `/teams/{tid}` global AND `/workspaces/{slug}/teams/{tid}` legacy paths — addTeam adds `originWorkspace` tag on the global write (matches Phase 2.3.a bootstrap schema), updateTeam uses setDoc(merge:true) on global. `deleteTeam` stays workspace-only — global delete deferred to Phase 2.3.c admin UI (parentTeamId children + externalId duplicate safety per § 63.15.2.X needs deliberate admin handling). 9 call sites unchanged (TeamsPage:62/66, TeamDetailPage 93/100/112/153, CSVImport 283/288, ScheduleCSVImport:384, ScheduleImport:242) — dual-write happens transparently inside dataService funcs. `firestore.rules` `/teams/{teamId}` block ADDED (read: auth, create+update: auth, delete: jacek@epicsports.pl) mirroring Phase 2.2.b `/players/` pattern; Phase 2.1c `/leagues/` + Phase 2.2.b `/players/` blocks PRESERVED; nested `/workspaces/{slug}/teams/` rule (covers legacy dual-write + breakoutVariants subcollection via recursive `{document=**}` wildcard) PRESERVED unchanged. **breakoutVariants subcollection** at `/workspaces/{slug}/teams/{tid}/breakoutVariants/` UNTOUCHED per § 63.15.2 "workspace-specific scouting context" decision finalized in Phase 2.3.a pre-flight — 4 dataService functions still read/write via `bp()` workspace path; legacy dual-write keeps parent docs alive so subcollection reads work unchanged. `subscribeTeams` dataService fn at line 259 now orphan (no callers post-refactor) — kept as dead code mirror of Phase 2.2.b's `subscribePlayers` orphan; cleanup deferred to Phase 2.3.d. **Sequenced deploy by Jacek** (rules first then code per locked pattern; reverse order = ~1-2 min blast radius of broken team rendering across TeamsPage + NewTournamentModal + match opponent display + ScoutTab match cards). 3 files modified, 81 insertions / 5 deletions, bundle unchanged. Build clean 5.87s, precommit pass, § 27 N/A (no UI changes). Smoke verified working post-deploy (Jacek confirmed). |
| 2026-05-20 | `732dd8e` (§ 63.15.2.X docs) + `a8cb308` (scripts + initial reports) + `3d8ea9c` (execute report + post-execute docs) | **Phase 2.3.a EXECUTED: Teams bootstrap to global /teams/ (§ 63.15.2 + § 63.15.2.X).** First Phase 2 Step 3 sub-task; closes the discovery + bootstrap half. 3 scripts added (audit + dryrun + execute, .cjs CommonJS + GOOGLE_APPLICATION_CREDENTIALS env var + PHASE_2_3_A_EXECUTE_CONFIRMED gate, mirrors Phase 2.2.a template). § 63.15.2.X locked decision (`732dd8e`) preceded scripts — codifies per-league externalId canonicality (PBLeagues only canonical for NXL today; PXL/DPL/local = in-app docId), sister team curation as always in-app, Phase 2.3.c admin UI requirements (both manual-create AND PBLeagues-imported workflows first-class), and verbatim hoist policy deferring forward-looking § 63.15.2 spec fields (pbliTeamId/leagueId/divisionId/brandId/shortName/active/createdBy) to post-Phase-2 reconciliation. Discovery audit: 1 workspace (ranger1996), 132 team docs, 125 parents + 7 children, 0 orphans, 79 with externalId (60% — NXL imports) + 53 null (40% — manual PXL/DPL creates), 9 intra-workspace name overlaps (all legitimate brand-multi-division pairs per § 63.15.2 — Wild Dogs PRO + Wild Dogs PRO3v3 etc.), 1 externalId dup group (RANGER parent-of-3-children + Ranger Warsaw orphan-parent legacy artifact). Per Jacek decision: **Option α (preserve workspace docIds as global IDs)** — zero downstream rewrite since player.teamId / scoutedTeam.teamId / tournament.homeTeam-awayTeam refs continue resolving. **Two-pass execution** — Pass 1 writes 125 parents + builds writtenParentIds Set; Pass 2 writes 7 children only if parentTeamId ∈ writtenParentIds (0 orphans). **NO automatic dedup** per § 63.15.2.X — auto-merge of RANGER pair would orphan 3 children whose parentTeamId points to RANGER docId; both migrate as separate global docs, admin curates via 2.3.c. Schema preserved VERBATIM (name, leagues[], divisions{} name-string map per Phase 2.1b precedent, parentTeamId, externalId) + 3 migration tracking fields (originWorkspace='ranger1996', migratedAt serverTimestamp, createdAt/updatedAt preserved from workspace). Execute result: 132 created (125+7), 0 errors, 0 skipped, 0 orphans. Post-execute verification: /teams/ size = 132 matches expected. Idempotency verified post-execute (re-run dryrun shows 132 would-skip / 0 would-create). breakoutVariants subcollection at /workspaces/{slug}/teams/{tid}/breakoutVariants/ untouched — out of 2.3.a scope, Phase 2.3.b decides hoist-vs-keep (recommend keep workspace-scoped per § 63.15.2 spec for workspace-specific scouting context). Legacy /workspaces/ranger1996/teams/ **UNCHANGED** — app continues reading from legacy path until Phase 2.3.b consumption refactor. **Reports committed:** initial audit (06:04 UTC), pre-execute dryrun (07:27 UTC), execute (07:49 UTC), post-execute idempotency dryrun (07:50 UTC) — full 4-snapshot trail in scripts/migration/reports/. App impact: ZERO (no code changes, no rules changes, no consumer migration yet). Unlocks Phase 2.3.b (useTeams hook + dual-write + useTeam(id) parent-resolution helper) — pattern from Phase 2.2.b directly applies. |
| 2026-05-19 | `7de12d4` | **Phase 2.2.c: Super admin UI for global Players CRUD (§ 63.15.3) — DEPLOYED.** Closes Phase 2 Step 2 (Players) admin path. New `/admin/players` route → `AdminPlayersPage` (lazy-loaded, 15.87kB/5.22kB gzip) with searchable + filterable + paginated 50/page list (~19 pages for 934 docs). URL-backed state (`useSearchParams` for search/filter/sort/page) makes filtered views bookmarkable + shareable for admin debugging. Filter pills: All / Linked (PBLI) / Unlinked / HERO. Sort: name ↑ (default), updated ↓, originWorkspace. Per-row MoreBtn → ActionSheet (Edit / Delete). Defense in depth admin gate (3 layers): L1 AdminGuard route wrap (App.jsx pattern), L2 component `!effectiveIsAdmin` early return, L3 firestore.rules `/players/{playerId}` admin-email delete gate (from Phase 2.2.b — no rules change in this brief). New `PlayerFormModal.jsx` (sibling to Phase 2.1c LeagueFormModal): mode-aware create/edit with sections Identity / PBLeagues link / Attributes / Audit (collapsed, read-only id+originWorkspace+createdAt+updatedAt+migratedAt+linkedUid+linkedAt+unlinkedAt+aliasIds[]). Form explicitly excludes `teamId`/`teamHistory` per § 63.15.3 (workspace-scoped, Phase 2.4 /teamMemberships/ territory). Reuses Phase 2.2.b dual-write `addPlayer` + `updatePlayer` — create flow chains addPlayer (narrow workspace subset) → updatePlayer for admin-only fields (hero, pbliIdFull, photoURL, comment), both dual-write paths converge. New `deletePlayerGlobal(id)` in dataService — global-only hard delete, leaves workspace copy as recovery cushion until Phase 2.2.d cleanup. **Delete confirmation Modal branches on `aliasIds[].length`:** empty/null → standard "cannot be undone, workspace preserved"; non-empty → enhanced warning with full alias ID list (mono font, scrollable max-h 120px) + explicit "orphan legacy `point.assignments[]` references — will render as Unknown" callout in red + "Delete anyway" CTA (informed consent per user data-loss waiver, NOT hard block — admin can proceed deliberately). More tab admin section gains "Players" link below "Leagues". 5 files, 632 insertions. Build clean, precommit pass, autonomous deploy (no rules change — Phase 2.2.b shipped /players/ rules covering admin writes + admin email delete gate). Smoke test pending — see DEPLOY_LOG 2026-05-19 for 13-step walkthrough (alias delete safety the critical case to verify). |
| 2026-05-19 | `8614a9b` (code) + `efd7ca8` (docs) | **Phase 2.2.b: usePlayers global + alias resolution + workspace consumption refactor (§ 63.15.3) — DEPLOYED (sequenced rules + code).** Second Phase 2 Step 2 sub-task and closing brief for Players workspace decoupling. `usePlayers()` hook in `src/hooks/useFirestore.js` refactored from workspace subcollection read to `collection(db, 'players')` global onSnapshot listener — returns `{ players, playersById, loading, error }` where `playersById` is a memoized map containing BOTH canonical IDs AND the 42 Phase 2.2.a alias mappings as keys, all pointing to the canonical doc. Single-source resolver in the hook means `point.assignments[]` entries pointing to either canonical or alias IDs Just Work without per-consumer logic. `dataService.js` 5 player write functions (addPlayer, updatePlayer, changePlayerTeam, setPlayerHero, deletePlayer) now dual-write to both `/players/{id}` global and `/workspaces/{slug}/players/{id}` legacy paths — global writes tagged with `originWorkspace` field; deletePlayer remains workspace-only (global hard-delete deferred to Phase 2.2.c admin UI with aliasIds[] reference safety). 7 call sites unchanged — centralized in the dataService funcs. New `firestore.rules` `/players/{playerId}` block: read=auth, create+update=auth, delete=admin email (matches Phase 2.1c pattern, gates against the Phase 2.2.c admin UI delete). 11 React consumer files updated: KioskLobbyOverlay (3 sites), KioskPostSaveSummary (1), TrainingCoachTab (1), TrainingScoutTab (2), MatchPage (3 — rosterA, rosterB, selfPlayerId), PlayerStatsPage (1), ScoutedTeamPage (2), TeamDetailPage (2), TrainingResultsPage (1) — pattern `players.find(p => p.id === id)` → `playersById[id]` (O(n) → O(1) + alias-aware). Sequenced deploy executed by Jacek: `firebase deploy --only firestore:rules` first (covers both /leagues/ from 2.1c and new /players/ — they had stacked up), then `npm run deploy`. **Hook reduction was the key insight** — naive Phase 0 scope showed 20 React consumers, but recognizing the centralized usePlayers hook collapsed it to a 14-file refactor (1 hook + 1 dataService + 1 rules + 11 consumers) with the alias resolution living in the hook so consumers don't carry that complexity. Build clean, precommit pass, bundle +1.5kB. Smoke target: sample alias mapping `adRjU9q6NOKYrEylUzFo` (canonical Szymon Wierzbicki) with alias `56Ne3QxIVqeBtH50fiUm` — old `point.assignments[]` referencing the alias should still render canonical Szymon, not "Unknown". |
| 2026-05-19 | `ab1319c` (scripts + execute run) | **Phase 2.2.a EXECUTED: Players migrated to global /players/ (§ 63.15.3).** First Phase 2 Step 2 sub-task. 3 scripts added (audit + dryrun + execute, .cjs CommonJS + GOOGLE_APPLICATION_CREDENTIALS env var pattern + PHASE_2_2_A_EXECUTE_CONFIRMED gate). Discovery: 1 workspace (ranger1996), 976 player docs, 813 linked + 163 unlinked, 42 intra-workspace duplicate pbliId pairs (all same-name batch-import dups from 2026-04-16 + 2026-05-08, zero name conflicts, zero cross-workspace overlap). Per Jacek decision: **Option α (preserve workspace doc IDs as global IDs)** — canonical per pbliId group = earliest createdAt (tie-break: lex-smallest docId), others tracked as `aliasIds[]` on canonical doc. Empty-pbliId players 1:1 migration. New schema fields per brief: `originWorkspace`, `aliasIds`, `migratedAt`. Execute result: 934 created, 0 errors, post-write verification matches expected (934). Idempotency verified post-execute (re-run dry-run shows 934 skip, 0 would-create). Legacy `/workspaces/ranger1996/players/` **UNCHANGED** — downstream `point.assignments[]` / `point.selfLogs[playerId]` continue resolving via legacy path until Phase 2.2.d cleanup (deferred). App unchanged — workspace UI still reads from legacy path (consumption refactor is Phase 2.2.b). Reports in scripts/migration/reports/. **🚨 Pre-req for Phase 2.2.b:** Firestore rules for `/players/{playerId}` (currently default-deny for client reads — Admin SDK bypassed migration, but app hooks will fail without rule). |
| 2026-05-19 | `96e9951` | **🎉 Phase 2.1c + Phase 2 Step 1 (Leagues) COMPLETE (§ 63.15.1).** Super admin UI at `/admin/leagues` closes Phase 2 Step 1. New AdminLeaguesPage + LeagueFormModal (lazy-loaded). CRUD service fns (createLeague, updateLeague, deactivate/reactivateLeague, generateDivisionId helper). Defense in depth admin gate: AdminGuard route wrap (existing pattern from /debug/flags) + component-level effectiveIsAdmin check + new firestore.rules `/leagues/{leagueId}` block (email gate). Soft delete only — deactivated leagues hidden from useLeagues consumers (now filters active=true by default), useAllLeagues exposed for admin view. Division id auto-regenerated on rename (warning shown); stored name strings unaffected per backward-compat design. More tab admin section gets "Leagues" link (admin-only via existing effectiveIsAdmin gate). 7 files, 413 insertions. **🚨 Requires `firebase deploy --only firestore:rules`** to activate the new rules block — until deployed, /leagues/ writes default-deny (admin UI errors on save) AND useLeagues reads continue silent-falling-back to constants per default-deny since Phase 2.1a (Sentry has been logging `useLeagues fetch failed` since 2.1b deploy). Rules deploy unblocks BOTH paths. Phase 2.1a bootstrap previously "worked" because Admin SDK bypasses rules. Pattern template ready for Phase 2.2/2.3/2.4 (Players/Teams/TeamMemberships). |
| 2026-05-19 | `957a3de` | **Build: chunkSizeWarningLimit + Firebase chunk exception (PROJECT_GUIDELINES § 11).** Vite build warning post-Phase-2.1b (chunks > 500kB) addressed. Pre-flight: vite.config.js already had optimal manualChunks design from April 2026 (vendor-react with explicit per-library matchers, vendor-firebase, vendor-sentry, vendor-misc). Only ONE chunk exceeds 500kB — `vendor-firebase` at 567kB raw / 135kB gzipped — and Firebase imports already minimal (firestore + auth + app only, no full SDK/storage/functions). Sub-500kB physically unattainable for current Firebase v11 SDK without deeper refactor. Per Jacek decision: accept as documented exception. Raised `chunkSizeWarningLimit: 600` + added new PROJECT_GUIDELINES § 11 "Bundle chunking strategy" with full pattern rationale, chunk layout table, firebase exception analysis (sub-chunk + dynamic-import options analyzed and deferred), guidance for new deps, history recording April 2026 React precedent and 2026-05-19 firebase exception. **manualChunks function UNCHANGED** — April 2026 white-screen risk class does NOT apply; chunks bit-identical pre/post commit. |
| 2026-05-19 | `2f81b2b` | **Phase 2.1b: useLeagues hook + workspace consumption refactor (§ 63.15.1).** App-code refactor moving React UI from direct `theme.js` LEAGUES/DIVISIONS reads to Firestore-backed `useLeagues()` hook with constants fallback. 3 new files: `buildLeaguesFromConstants.js` (shape adapter), `useLeagues.js` (main hook + Sentry on error), `useLeagueDivisions.js` (per-shortName convenience). 6 React consumers refactored: NewTournamentModal, LayoutDetailPage, LayoutWizardPage, MainPage (EditTournamentModal), TeamDetailPage, TeamsPage. **Additive pattern with sync fallback** — hook initial state comes from constants adapter (no loading state), background `getDocs` upgrades to Firestore data when available, on error stays at fallback + `captureException`. **Stored value format preserved** — all dropdowns use `option.value = d.name` (not `d.id`), so new writes match legacy data format (`tournament.division = 'PRO'`, not `'pro'`); no data migration needed. Hooks-in-loop avoided via `divisionsByShortName` lookup map in 2 components iterating per-league. Utility consumers (CSVImport.jsx normalizeDivision, divisionAliases.js) stay with constants per scope discipline. `LEAGUE_COLORS` constant separate from this refactor — consumers continue importing from theme.js. Foundation ready for Phase 2.1c super admin UI (CRUD on /leagues/ → all consumers see updates on next page load). |
| 2026-05-19 | `324f380` (script + Firestore data) | **Phase 2.1a: Leagues collection bootstrap (§ 63.15.1).** First Phase 2 implementation. New file `scripts/migration/phase_2_1a_bootstrap_leagues.cjs` — populates `/leagues/` from `src/utils/theme.js` `LEAGUES` + `DIVISIONS` constants. Idempotent (skip-if-exists). Init pattern matches Phase 1.3 (.cjs + `GOOGLE_APPLICATION_CREDENTIALS` env var). Ran autonomously by CC (additive operation, brief explicit authorization). Output: 3 docs created (`l_nxl` + 7 divisions PRO/SEMI-PRO/D2/D3/D4/PRO3v3/WNXL, `l_pxl` + 3 divisions Div.1-3, `l_dpl` + 3 divisions Div.1-3), 0 errors. Schema per § 63.15.1: id `l_{shortName}`, name + shortName preserved from constants, region/parentLeagueFamily null initially, divisions `{id, name, order}` with lowercase-hyphenated ids (`pro`, `semi-pro`, `div-1` etc.), active true, createdBy 'bootstrap' sentinel. Display `name` field preserves original casing because Phase 2.1b workspace consumption refactor must read same display strings. Idempotency verified via post-write dry-run (3 SKIP, 0 would-create). App behavior unchanged — workspace UI continues reading theme.js constants until Phase 2.1b refactor lands. |
| 2026-05-19 | `e560151` (script + same-day run) | **🎉 Phase 1.3 + Phase 1 schema foundation COMPLETE (§ 63.3 Option α).** Migration script `scripts/migration/phase_1_3_delete_users_workspaces.cjs` committed, then run by CC after Jacek GO (CC found existing service account JSON in `~/Downloads/` from prior `purge-anonymous-users.cjs` run, escalation gate #1 cleared). Migration output: 21 user docs total, 18 with `workspaces` field (most empty `[]`, three with `["ranger1996"]`), 18 deleted, 0 errors. Post-write verification dry-run confirmed 0 docs with field remaining. **§ 63.3 Option α fully implemented end-to-end:** Phase 1.1 (`b90ffed`) shipped collectionGroup query hook · Phase 1.2 (`6c9ad4f`) removed sole signup writer + bootstrap SoT comments · Phase 1.3 (`e560151`) cleaned stored data. `workspace.userRoles[uid]` is sole source of truth — code AND data. Phase 2 (Leagues + Players + Teams + TeamMemberships hoist) unblocked. |
| 2026-05-19 | `6c9ad4f` | **Phase 1.2: Drop users.workspaces write path + bootstrap refactor (§ 63.3 Option α).** `users/{uid}.workspaces` field is now fully orphan in code — zero readers AND zero writers in src/ (verified by post-change grep). Removed sole writer at `dataService.js:getOrCreateUserProfile` (was writing `workspaces: []` on signup). Updated /users/{uid} canonical schema doc comment to reflect Option α. Added inline SoT comments at 3 `userRoles` write sites in `useWorkspace.jsx` (enterWorkspace self-join + brand-new-workspace bootstrap + autoEnterDefaultWorkspace self-join). No firestore.rules change (user-doc create rule unconditional). Bootstrap auto-join behavior preserved per § 49 + § 51. Phase 1.1 hook (useUserWorkspaces) unaffected. Field still in stored data — Phase 1.3 migration deletes. **Discovery note:** Phase 1.1 finding "no current direct write" was based on reads-only grep (`.workspaces` dot-property); Phase 1.2 pre-flight ran field-name grep (`workspaces:` colon) and surfaced the signup writer. |
| 2026-05-19 | `b90ffed` | **Phase 1.1: useUserWorkspaces hook (multi-tenant § 63.3 Option α).** First multi-tenant Phase 1 implementation. New `src/hooks/useUserWorkspaces.js` queries user's workspace memberships via `workspace.userRoles[uid]` map field — first consumer of the Option α source-of-truth approach (every other userRoles[uid] read today operates on already-fetched single workspace doc). One-shot Firestore `getDocs` query on auth user change, returns `{ workspaces, loading, error }`. No real-time listener (defer until proven needed by switcher UI). No firestore.rules change (existing `allow read: if request.auth != null` permits filtered list query). No composite index pre-deployed (Firestore auto-indexes map subfields). Additive — hook is unused until switcher UI consumes it (separate brief). Foundation for Phase 1.2 (drop `users.workspaces` write path) + Phase 1.3 (one-shot field deletion migration). Smoke test instructions in DEPLOY_LOG 2026-05-19. |
| 2026-05-19 | `5f12f7d` | **Canvas Step 1: drawZones.js i18n cleanup (§ 64.8.6).** First implementation step of the § 64 canvas refactor sequence. Moved 5 hardcoded canvas labels (DISCO/ZEEKER/DANGER/SAJGON/BIG MOVE) from `drawZones.js` to `i18n.js` dictionary as `zone_label_*` keys (PL+EN, identical English values initially per paintball jargon convention). `drawZones` now accepts `t` accessor via options. `FieldCanvas.jsx` imports `useLanguage`, calls `const { t } = useLanguage()` in body, passes through to drawZones, added to draw useEffect deps. Mechanical refactor — no behavior change, no visual regression, all rendering logic preserved (pill bg, font sizes, position offsets, all 5 zone colors unchanged). Build clean, precommit pass. Browser visual verification deferred to first production hit — smoke test instructions in DEPLOY_LOG 2026-05-19. Frees future § 63.8 i18next migration to be mechanical conversion. |
| 2026-05-15 | `15ae8e2` (merge) · branch `fix/heatmap-density-removal` · 1 commit (`acdcc00`) | **§ 62 — Heatmap density removal + stroked markers (NXL Czechy day 1).** Player position heatmaps (coach team summary § 28/§ 60 + match review § 21) — radial density blob layer removed entirely. `drawDot` + `drawTriangle` now take separate `fillColor` + `strokeColor`; both render solid team fill at alpha 1 + 2 px stroke for shape separation on overlap. Team A green family (`COLORS.success` + `COLORS.successDim`), Team B teal fill (`COLORS.zeeker`) + neutral dark stroke (`COLORS.surfaceDark`) because no dark-teal token exists in the palette and Jacek explicitly forbade adding new tokens. Marker radii unchanged. § 25 HERO amber halo preserved as outermost layer. Bump density + shot density untouched (different signal). Eliminated markers untouched (§ 31 X stays). Deaths heatmap (§ 61) on different canvas, untouched. Live scouting markers (FieldCanvas + drawPlayers.js) untouched — different visual class. § 62 appended to DESIGN_DECISIONS with full rule set + out-of-scope list. Density was actively hiding the circle (gun-up) vs triangle (runner) encoding when markers stacked on the same bunker — cluster size already conveys density by stacking, blob was redundant. |
| 2026-05-15 | `e0e3e6b` (merge) · branch `fix/schedule-import-scouted-division` · 1 commit (`859e9ef`) | **Schedule import scouted-division repair + source fix (NXL Czechy day 1).** Coach tab Teams empty for NXL Czechy because schedule import (ScheduleCSVImport Pass 0 / match / create branches + OCR ScheduleImport ensure-scouted loop) called `addScoutedTeam` without `division` → null landed in Firestore → CoachTabContent's client-side division filter (`tournament.divisions[0]` default) excluded everything. Jacek's initial hypothesis "Pass 0 silently failed" disproved by code analysis (no `deleteScoutedTeam` exists; matches existing proves scouted creates succeeded). Wrong-shape, not missing. Source fix at all four call sites + per-team try/catch with success/failure counts in import log. Repair surface: `dataService.repairScoutedDivisionsForTournament(tid, league)` UPDATES null-division rows from `team.divisions[league]` (no creates → zero duplicate risk), idempotent. UI affordance is a self-gated Btn in CoachTabContent that renders only when `scouted.length > 0 && divisionScouted.length === 0` and vanishes after onSnapshot settles. Diagnostic walkthrough in chat 2026-05-15 — 4-item discovery (path, shape, Pass 0 code, Coach query) plus disproof of the deletion-was-the-cause path. |
| 2026-05-15 | `3b236cf` (merge) · branch `fix/multi-device-overwrite` · 1 commit (`2f696f5`) | **Multi-device point-overwrite hotfix (NXL Czechy day 1).** Per-coach point stream used deterministic doc IDs `{matchKey}_{coachShortId}_{NNN}` with a localStorage-keyed counter; two devices on same UID computed identical IDs → `setDoc` silently overwrote prior point docs. Jacek lost 6 points mid-tournament. Opus option (b) shipped: drop deterministic IDs entirely, `savePointAsNewStream` now uses `addPoint`/`addTrainingPoint` (auto-ID), `index` computed reactively from live `points` array. `endMatchAndMerge` already groups by `coachUid` + sorts by `index` field (not doc ID), so no data migration. `useCoachPointCounter` deleted. `setPointWithId`/`setTrainingPointWithId` exports retained as dead code — post-NXL cleanup. Deferred: cross-device same-UID presence banner (claim system retired in Brief F), Sentry `onToolbarAction` ReferenceError, § 42 docs update. Two-device smoke validation pending Jacek on the tournament floor. Full diagnostic in chat 2026-05-15 (state-machine walk of 7 hypotheses; auto-ID + reactive index pin the bug structurally rather than papering it). |
| 2026-05-14 | `d4653ef` (merge) · branch `feat/schedule-auto-match-workspace` · 1 commit (`40fe366`) | **Schedule CSV: workspace auto-match + auto-attach.** Real-data follow-up to yesterday's schedule CSV ship. First Jacek run with the 229-row file pushed all 76 teams to the resolver because schedule import's auto-match was tournament-scouted-only — teams existed workspace-wide (from 2026-05-12 player CSV) but weren't attached to the new NXL Czechy tournament. Auto-match now does two passes: tournament-scouted first, then workspace-wide fallback (case-insensitive name + exact division). Exactly-one workspace hit triggers auto-attach during import (new Pass 0 creates scouted entry, roster pre-populated). 0 or 2+ workspace hits fall through to the resolver (2+ = ambiguous parent/child). Resolver UI shows new `🔗 Z workspace (zostaną dopięte)` counter. Result: 76 manual taps → 0 for Jacek's case. Merge + deploy done Jacek-side via terminal (CC approval channel was intermittent at the time). |
| 2026-05-13 | `5b1e15f` (merge) · branch `feat/schedule-csv-import` · 5 commits (`76f7a1f`, `d916347`, `be74c61`, `eb2e1d4`, `f3eb5f1`) | **Schedule CSV import + match list grouping (pre-NXL Czechy).** New `divisionAliases.js` shared util (SCHEDULE_DIVISION_ALIAS 7-entry map, normalizeScheduleDivision, parseScheduleDateTime, dayShort, stageRank/Label, groupMatchesByStage). New `ScheduleCSVImport.jsx` modal — separate from OCR `ScheduleImport`. Tournament picker filtered to NXL; year inherited from selected tournament. Hard-stop on unknown division or unparseable date with row number. Structured resolver for unmatched teams (Dopasuj / Utwórz / Pomiń per row). `addMatch` extended additively with `scheduledAt` / `field` / `round` / `group`. MatchCard pill now shows `Czw 14:20 · NXL Pro` (day-short + time + field) with legacy `date + time` fallback. Scout tab Scheduled section now grouped by stage (`Eliminacje` + `Sunday Club` — two buckets per Jacek 2026-05-13 directive, all bracket rounds collapse into Sunday Club) and sub-grouped by Grupa within Eliminacje. OCR ScheduleImport untouched. |
| 2026-05-12 | `06b4ec1` (merge) · branch `feat/csv-import-division` · 1 commit (`0b67166`) | **CSV import: Dywizja → team.divisions.NXL.** PBLeagues NXL exports now auto-map the Dywizja column to team.divisions.NXL during CSV import (zero manual per-team editing). DIVISIONS.NXL extended from 5 → 7 values (`PRO`, `SEMI-PRO`, `D2`, `D3`, `D4`, `PRO3v3`, `WNXL` — last two new per brief). `SEMI-PRO` casing preserved for backward compat; CSV `'Semi-PRO'` normalizes case-insensitively to canonical `'SEMI-PRO'` on import (option a from discovery — zero data migration). New `normalizeDivision(raw, league)` helper. New `teamDivision` MAPPABLE entry on the import UI; `'dywizja'`/`'division'` detect keywords moved from `playerClass` (where they were mis-mapping a TEAM-level field) to `teamDivision`. `addTeam` extended to accept `data.divisions`. Preview gains Dywizja stat row + intra-import collision count (dev console.warn). Import-log tags `[NXL: {div}]` per team line. All existing DIVISIONS.NXL consumers pick up the 7-value list via their `DIVISIONS[league].map(...)` iteration — no per-file updates needed. |
| 2026-05-12 | `ae3627f` (merge) · branch `fix/match-postclose-edit` · 1 commit (`c6e8749`) | **Match post-close edit + scout preservation (Bug 7 + Bug 2c).** New `match.editLockReleased` boolean lets a closed match flip into editable mode without changing status; reopen flow restored after April 2026 removal but with safer mechanics (no live/claim involvement, status stays 'closed'). MatchPage review-view sticky button branches: End match (open red ghost) / Odblokuj edycję (locked neutral ghost) / Zamknij ponownie (unlocked neutral ghost → confirm + recomputeMatchAggregates). Header pill: FINAL gray when locked, ODBLOKOWANY amber when reopened. New dataService helpers: `setMatchEditLockReleased`, `recomputeMatchAggregates`. MoreBtn menu omits End match while unlocked. Bug 2c: savePoint preserves `homeData/awayData.scoutedBy` on post-close edits (only when editingId AND status='closed'); open-match + new-point-during-unlocked behavior unchanged so Scout Ranking § 33 attribution is correct in all cases. Back-button half audited as already-working — no save-on-back logic exists, brief's first claim was preemptive. 8 i18n keys × PL+EN. Training matchups not in scope (parallel codepath via endMatchupAndMerge — separate follow-up if needed). |
| 2026-05-12 | `112fff9` (merge) · branch `fix/deaths-heatmap-table-scroll` · 1 commit (`dc3a76e`) | **Deaths heatmap table scroll regression (Bug 6).** Bug 5 stopped halfway — aligned width to responsive pattern but left `height: '100dvh'` on the page outer. That triggered the classic flex+overflow gotcha on iOS Safari (`flex: 1, overflowY: 'auto'` inner refuses to shrink below content size without `min-height: 0`), so the deaths table rendered but landed below the silently-broken scroll boundary. Single-line fix `height: '100dvh'` → `minHeight: '100vh'` matches ScoutedTeamPage / BallisticsPage / MatchPage + 8 other pages' canonical pattern. Document scrolls naturally now. Brief's four hypothesized causes (overflow:hidden, position:fixed, orientation conditional, container restructure) all wrong — real cause was layout-strategy mismatch only reproducible on iOS Safari. |
| 2026-05-12 | `3737705` (merge) · branch `fix/deaths-heatmap-landscape-width` · 2 commits (`d1dad51`, `607a5eb`) | **Deaths heatmap landscape width (Bug 5).** LayoutAnalyticsPage had hardcoded `maxWidth: 640` → 86 px dead margin in iPhone landscape. Initial fix (`d1dad51`) overcorrected to pure `width: 100%`; followup (`607a5eb`) aligned to canonical app-wide responsive pattern (`maxWidth: R.layout.maxWidth \|\| 640` from `responsive(device.type)`) used by MatchPage scouting canvas + 10 other pages. Mobile fills viewport, tablet caps at 768, desktop at 1200 — same look and feel as the rest of the app. Per Jacek "i want to have the same look and feel across app". Three relax-paths sketched in commit for global tighter-fit if needed (raise tablet maxWidth, tighten useDevice touch override, or per-page bypass); not in scope here. |
| 2026-05-12 | `555a634` (merge) · branch `fix/deaths-heatmap-cluster-zorder` · 2 commits (`9b13960`, `b548907`) | **Deaths heatmap cluster radius + z-order (Bugs 3+4).** Bug 3: shooter cluster bucket bumped from implicit 1% to 2% via new `SHOOTER_CLUSTER_BUCKET = 0.02` named constant — fewer, larger markers; tunable in one place. Applied at both `attributionData` + `linkMap` useMemos so shooterId keys stay aligned for cross-filter. Bug 4: render restructured into two passes when filter active — faded markers (both types at 0.3 alpha) first, highlighted markers (both types at 1) last — so a highlighted skull is never covered by a faded shooter at the same coord. Extracted `drawSkull` / `drawShooterMarker` local helpers; no-filter z-order preserved. |
| 2026-05-12 | `2125793` (merge) · branch `fix/deaths-heatmap-hotfix` · 1 commit (`c5dbb5e`) | **Deaths heatmap hotfix Bug 1 — shooter coords to right half.** Brief B Stage 5 spec incorrectly normalized shooter markers via `forceLeft` ("same as skulls"); production showed shooters stacking on top of skulls on the left half. Hotfix introduces `forceRightX` helper applied at both `attributionData` + `linkMap` useMemos so skulls cluster left (defender side) and shooters cluster right (shooter side). Cross-filter linking is attribution-based, not spatial — IDs continue to match. § 61.8 coord-frame note in DESIGN_DECISIONS anticipated this fix path. Bug 2 (canvas overflow / pan+zoom) ESCALATED to separate brief: raw canvas → `FieldCanvas` migration is architectural-scope. |
| 2026-05-12 | `a5bb51e` (merge) → DEPLOY_LOG entry · branch `feat/deaths-heatmap-v2` · 7 commits (`b1f32a2`/`3fe3b90`/`b024889`/`d9dc88b`/`71dfd71`/`4276639`/`ed82311`) | **Brief B — Deaths Heatmap v2 (§ 61).** LayoutAnalyticsPage `mode='deaths'` overhaul isolated to one screen. New `deathAttribution.js` helper (precision 10% + zone match + 1/N split, fractional credits via `formatKills`); progressive-disclosure scope pills Layout/Tournament/Match/Point with `ActionSheet` pickers; `_ctx` ids additive on `fetchLayoutDeaths`; attribution useMemo runs per filtered point per side; density auto-hide < 5 points; zero-point empty state; scope-aware count-line wording; `Pozycja strzelca` 7th table column via O(1) `attributionByDeath` lookup; shooter markers on canvas with team-color credit badges (zero-kill markers NOT rendered); bidirectional cross-filter linked highlighting (skull ↔ shooter via precomputed `linkMap`); status pill `📍 Eliminacja na D1 — N strzelców · ✕`. § 30 + all global kill displays preserved. Three deferral decisions documented in § 61.6: instant `globalAlpha` flip instead of 200 ms fade animation, no toast for unattributed-skull (pill carries `brak strzelca`), zero-kill markers gated out. Coord-frame check (§ 61.8) awaits real-data validation. 10-step smoke walkthrough in DEPLOY_LOG. Brief archived: `docs/archive/cc-briefs/CC_BRIEF_DEATHS_HEATMAP_V2_2026-05-12.md`. |
| 2026-05-12 | `36104cb` (merge) → `3a1ffed` (DEPLOY_LOG) → `8327d4f` (§ 60 docs) · branch `feat/pre-nxl-refinements` | **Brief A — Pre-NXL Refinements (§ 60).** 8/9 SAFE-tier items shipped, PLAYER #1 deferred (§ 60.9). Coach view changes: heatmap promoted to top + expanded (§ 60.1); Tendencja demoted to Additional sections (§ 60.2); Rozbiegi `Zagrań` + `W pkt` columns (§ 60.4, `computeBreakSurvival` extended); Strzelanie reliability banner (<80% amber alert, reuses `computeCompleteness.shotPct`, § 60.5); match-level scope filter — `Ostatni mecz` + `Mecz ▾` picker, URL `?scope=lastMatch` / `?scope=match&mid=X` (§ 60.6); ADD MATCH removed from coach summary (§ 60.7); ShotDrawer 80%/max-340 → 70vw/max-520 (§ 60.8). 10 i18n keys × PL+EN added. Brief archived to `docs/archive/cc-briefs/CC_BRIEF_PRE_NXL_REFINEMENTS_2026-05-12.md`. Discovery contradictions documented in archive: SCOUT #6 current state was 80%/max-340 (perceived as 40% due to maxWidth-cap on iPhone Pro Max landscape); brief said `§ 39` but renumbered to `§ 60` (§ 39 had been taken since 2026-04-21). |
| ~2026-05-02 | `d5d32ab` (DEPLOY_LOG `be9cead`) | **PlayerStatsPage redesign** — chemistry duo/trio, depth metric disabled, data-source pills per section, large overlapping avatars (40/48px), new naming convention for all section titles. Brief archived to `docs/archive/cc-briefs/`. |
| ~2026-05-02 | `fix/hotfix-bundle-2026-05-02` | **Hotfix bundle** — Issue #1 (winner-pick buttons appearing during Live tracking Stage 3 — legacy artifact removed) + Issue #2 (chemistry card avatars now use canonical `PlayerAvatar.jsx` with `photoURL` fallback). |
| earlier | various | KIOSK MVP, security audit (Tier A/B/C), ADMIN_RUNBOOK, anonymous user bulk-delete, vendor bundle splitting, feature flags + Sentry, training mode, bottom tab navigation. See `DEPLOY_LOG.md`. |

Older entries up to 2026-04-20 covered by the previous HANDOVER snapshot (`git log docs/ops/HANDOVER.md`).

---

## 🎯 Next on deck (priority order)

**Last refreshed 2026-05-27 end-of-day.** Stale ranking from prior sessions (Phase 1 schema / Canvas refactor / PSR Tier 2 / SCOUT-COACH backlog / BreakAnalyzer) collapsed into the single ranked list below — most line items either shipped during the 2026-05-27 16-ship day or fold into "Awaiting decision."

### 1. Active queue #2 — Phase 2.2.d + 2.3.d dual-write cleanup
Bigger refactor than the orphan-`subscribe*` removal already landed in `071c032b`. Decommissions the workspace-side write half of the Phase 2.2.b / 2.3.b dual-write (`addPlayer` / `updatePlayer` / `changePlayerTeam` / `addTeam` / `updateTeam` workspace mirrors). § 85 link-to-global already shipped; only the workspace-`linkedUid` backstop + the write-mirror calls remain. **Needs Opus brief** — touches dataService + workspace player/team subcollection state. Coordinates with PART 3 below (linkedUid backstop collapse).

### 2. B15 dead-`userRoles` cleanup
Audit + cleanup scripts shipped (`071c032b`): `scripts/migration/audit_dead_userroles.cjs` (READ-ONLY) + `cleanup_dead_userroles.cjs` (gated by `CLEANUP_DEAD_USERROLES_CONFIRMED=1`; `.cmd` wrapper sets it). Jacek runs at desktop with creds. Audit-first → review classification + criterion → cleanup. Closes the 569-straggler post-anonymous-purge debris.

### 3. B4 Home view (role-aware landing)
Per § 73. Needs Opus clickable mockup across device sizes — fresh-head design pass (spawns per-role product questions: pure-player no-tournament landing, scout/coach event-cluster overview, post-tournament wrap, etc.). NOT a single screen — a card system.

### 4. B9 squad-matchup roster backfill · B20 cross-device same-UID presence
Labels only — each starts with a discovery PRE-FLIGHT. B9 = choose option (a) UI refresh affordance / (b) dataService walker on attendee-add / (c) accept as documented limitation. B20 = non-trivial design surface (PWA presence heartbeat doc + banner + privacy boundary).

### 5. LayoutDetailPage UX pass
**Parked for discussion** per Jacek 2026-05-27 post § 88 deploy: "ux konfiguracji layoutu jest mało przyjazny … niespójna nawigacja i miejscami trudny interfejs." Multiple modal-buried surfaces (Lines & Zones / Info / Mirror / Calibrate / OCR / Delete) compete with toolbar toggles + inline actions; § 88 zone editor inherits the pattern problem by extending the modal rather than redesigning the page. **No code unilaterally** — design pass owed. Folds the two § 88 v1 caveats: (a) toolbar 3 zone shortcut buttons retired (extra tap to draw); (b) scouting pill renders regardless of `showZones` toggle.

### 6. Owed smoke checklists (Jacek on prod)
Active queue #6 in `NEXT_TASKS.md` enumerates the debt: § 81 7-step · self-log gate 5-step · § 80 6-step · § 79 6-step · § 78 5-step · § 77 5-step (#4 🔴 arbiter regression) · § 76 5-step · § 61 iPhone Deaths heatmap coord-frame · Brief F two-device · Phase 3.c.2 Stage 7.4 super_admin · plus today's 16-ship smoke (DEPLOY_LOG entries each carry their own list — biggest visible ones: § 88 zones / A2 v2 ShotDrawer / B10 LogRow eyebrow / B5 autosave indicator).

### 7. Long-running tracks (status only — Opus does not pick these unilaterally)
- **Phase 2.4 — TeamMemberships junction** — ready for brief when Jacek prioritizes; unlocks multi-league memberships per § 63.15.4.
- **Phase 3.c.3 — PII read-scoping** — deferred until multi-tenant onboarding; depends on workspace #2 onboarding moving.
- **Phase 3.d — Workspace admin UI** — soft-blocker for Paintball FIT onboarding. § 65.7 (workspace_admin scope) needs Jacek decision first (see "Awaiting decision" below).
- **Custom zones extension** — § 88 v1 closed the unified-zone primary surface; future enhancements (e.g. zone overlap rules, per-tenant naming conventions, callout vocabulary tooling) are open but not actively scoped.
- **BreakAnalyzer module tuning** — `docs/architecture/BREAK_ANALYZER_SPEC.md`; engine scaffolded but Opus territory.

---

## ⏸️ Awaiting decision (needs Jacek input)

| Topic | Question | Blocks |
|---|---|---|
| **PART 3 — workspace `linkedUid` backstop collapse** | The § 85 B2 (c) carve-out kept workspace-scoped `linkedUid` writes as a rollback net during the global-`/players/` migration. Global has been sole source ~1 week with no rollback. Collapse now (idempotent null script, drops rollback net) vs keep the net (revisit after another tournament cycle). **Default: KEEP** unless Jacek explicitly says "collapse." | Phase 2.2.d + 2.3.d cleanup (Active queue #2) — without this decision, the dual-write retirement brief can't lock its scope. |
| **FIT onboarding track — workspace_admin scope** | § 65.7 still open: can a `workspace_admin` grant arbitrary roles (incl. another `workspace_admin`) inside their own workspace, or is role-granting reserved for `super_admin`? Custom-zones prereq now shipped (§ 88) so the technical blocker is gone — only this permission decision remains. | Phase 3.d (workspace admin UI); Paintball FIT external-tenant onboarding. |
| **Sparing / events model A/B/C — standing architecture rozkmina** | Sparing collection affiliation + sticky-state localStorage keying + wizard host resolution + copy/UI context assumptions + events unification (Model A status-quo / B single `events` collection / C lightweight `events_index`). **Note:** B5 partial-save was de-coupled from this — autosave shipped as local-only with no event-model dependency. | PPT picker fix, sparing implementation, player claim flow brief. |
| **Factual observation reconciliation strategy (Phase γ formal lock)** | Preliminary preference locked 2026-05-19: trust-one-source + manual super admin escape hatch (per § 63.16.5). Formal lock deferred to Phase γ when actual conflicting data exists to inform choice. | Composite aggregation mode (Phase γ) implementation brief |
| **Data residency** | Firestore region for US team (+100ms latency if EU region). Single region for v1 or multi-region from start? | Multi-tenant Phase 0 (decision before workspace creation for US team) |
| **GDPR / data privacy implementation** | Player data removal mechanism. Right to portability. Cross-workspace data export per player. | Multi-tenant Phase 1 schema (data model must support deletion) |
| **Subscription model details** | Payment flow (Stripe?), billing cycle, plan tiers (free/pro/enterprise). Granular per-layout decision locked but UX details open | Multi-tenant Phase 6 (aggregation Phase 2 + tier UI) |
| **Tier gating Phase 1 default** | Admin-only Phase 1 soft-confirmed. Verify before Phase 5 implementation begins | Multi-tenant Phase 5 (aggregation Phase 1) |
| **`PlayerSelfReportV4.jsx` mockup** | Provide mockup or accept extrapolated UI as-is? | Polish pass on Tier 1 + Tier 2 UI |
| **Coach / scout role system** | Explicit scout role + per-match-subject permissions, or current `workspace.role` (coach/viewer/admin) enough? | Tier 2 edit permissions in PlayerSelfReport |
| **Tactic schema shots support** | Add shot field to tactics, or skip tactic-page suggestions? | SelfLog Integrations Commit 3 |
| **F5/F6/F7 priority** | Three user-reported features with overlapping scope. Which next? | Which brief gets written |
| **BreakAnalyzer ship target** | Block on which other work, or run in parallel? | Engineering capacity allocation |

---

## 🍞 Data-trust workstream (breadcrumbs)

Accumulating evidence that a dedicated "data trust / validation" pass needs to happen before any metric tuning starts paying dividends. Not a brief — just the breadcrumb pile. Scope a workstream when it gets heavy enough.

- **B8 Strzela% denominator** — DEFERRED 2026-05-27 (B7+B8 closure batch). Jacek's note: "nie ufam jeszcze danym". Tuning a metric denominator is premature when input quality is uncertain.
- **KIOSK `scoutedBy` misattribution** — FIXED 2026-05-27 in `0ccdb400` (data-quality follow-up to gap α deploy). `writerUid` was the linked-uid of the TAPPING player, not the device user; flowed to shot `scoutedBy` + `_meta` provenance. Not a security issue (carve-out rides `isScout`) but misleads any future attribution-driven analytics. Historical KIOSK shots NOT backfilled (read-only drift; leave for the workstream if it ever matters).
- **4.7% opponent-movement capture** — measured 2026-05-27 via `scripts/migration/count_opponent_movement.cjs`. Drove the § 88 transit % PARK. Capture-behavior gap (scouts tap-place opponents, rarely drag movement) not a model gap.
- **B5 autosave drafts are local-only by design** — cross-device draft sharing was explicitly declined (would have required committing to Firestore `status:'partial'` with cross-device merge concerns). Drafts staying per-device keeps the local resilience layer simple but creates a "draft drift" surface if a coach switches devices mid-edit (will be invisible to the other device). Acceptable for v1 single-coach-per-device workflows.

---

## 🏛️ Architecture decisions reference

Long-form architecture docs in `docs/architecture/`. Opus should read the relevant doc before drafting specs touching that area.

| Doc | Topic | When to read |
|---|---|---|
| [`BALLISTICS_SYSTEM.md`](../architecture/BALLISTICS_SYSTEM.md) | Euler integration, hitboxes, 3-channel visibility | Anything touching `src/workers/ballisticsEngine.js` or BallisticsPage |
| [`BREAK_ANALYZER_SPEC.md`](../architecture/BREAK_ANALYZER_SPEC.md) | BreakAnalyzer Phase 1 spec | Before BreakAnalyzer feature work |
| [`BREAK_ANALYZER_DOMAIN_v2.md`](../architecture/BREAK_ANALYZER_DOMAIN_v2.md) | Domain vocabulary + constants | Same as above |
| [`BUNKER_RECOGNITION.md`](../architecture/BUNKER_RECOGNITION.md) | NXL 2026 bunker taxonomy (15 types) | Before Vision scan, bunker editor, shape-aware features |
| [`HALF_FIELD_SPEC.md`](../architecture/HALF_FIELD_SPEC.md) | Mirror system, `computeMirrors()`, calibration transform | Before FieldCanvas changes or bunker storage schema |
| [`PLAYER_SELFLOG.md`](../architecture/PLAYER_SELFLOG.md) | Two-tier model, synthetic coords, flywheel thresholds | Before Tier 2 / Integrations work |
| [`TACTIC_WORKFLOW.md`](../architecture/TACTIC_WORKFLOW.md) | Tactic editor scouting-style flow, bump, curve cycling | Before tactic page changes |

---

## 📐 Recent design decisions

`docs/DESIGN_DECISIONS.md` — newest sections. Next available section number = § 90 (verify before commit).

| § | Topic | Date | Notes |
|---|---|---|---|
| 89 | Scout point autosave draft (localStorage) | 2026-05-27 | Local pre-commit buffer for MatchPage scout editor. Debounced (2 s idle) snapshot of in-progress point; restore on return; clear on commit / explicit discard. Pattern mirrors § 48.8 WizardShell (NOT pptPendingQueue). Key `scout_draft__<kind>__<eventId>__<containerId>__<scoutingSide>__<editingId\|\|'new'>`; TTL 24 h; non-pristine guard; restore precedence Firestore-wins on edit-existing flows. Commit path / Save gate / concurrent Firestore `status:'partial'` unchanged — purely additive. Discard via editor `MoreBtn` ⋮ → ConfirmModal. `exitEditMode` deliberately does NOT clear (back-tap preserves; matches § 48.8 semantics). Explicit non-goals: no Firestore writes, no Save-gate change, no schema change, no cross-device (localStorage is per-device), no sparing-rozkmina dep. |
| 88 | Unified Zones v1 — `zones[]` model + editor + scouting pill + Strefy summary | 2026-05-27 | All field zones (Danger, Sajgon, Big Move, future comms-callout) collapsed to one object type `{id, name, color, polygon, type?}`. Names are data, never hardcoded — team callout vocabulary. Built-ins (Danger/Sajgon/Big Move) become seeded zones with default names + colors (red/blue/amber), fully renamable. Universal "off-break %" stat. Scouting pill in zone color when placed player inside polygon. ScoutedTeamPage net-new "Strefy" section between Strzelanie and Kluczowi gracze. Big Move retains pinned bunker-attribution section (§ 87 coexistence). Dual-write migration: `layout.{dangerZone,sajgonZone,bigMoveZone}` → `layout.zones[]`; legacy readers untouched. **Transit % PARKED** — opponent bump-rate 4.7% via the read-only count script (capture-behavior gap, not model). Forward-compatible (stable id, ordered path base→bump→end) for the future movement / shot-by-zone pass. |
| 87 | Zones — domain semantics + Big Move ↔ unified-zones coexistence | 2026-05-27 | Snapshot of the domain expansion before § 88's spec landed: Danger/Sajgon = "off-the-break threat region against scouted team"; Big Move = wider polygon also covering center + own side; comms/callout zones = in-game vocabulary (player yells the zone name, e.g. "ORANGE"). All zones = ONE object type per § 88. Big Move's bunker-attribution analytic (`computeBigMoves`) preserved pinned via `type:'bigMove'`. |
| 86-v2 | ShotDrawer — drag-move + tap-marker menu | 2026-05-27 | Follow-up to § 86 v1 (BaseCanvas migration shipped 2026-05-26). v2 adds the two affordances explicitly deferred: drag-move existing shot (new `draggingShotRef` + ref-wrapped `setDraggingShot` per PROJECT_GUIDELINES § 9; 6-px clientXY threshold disambiguates tap vs drag; pan suppressed while shot armed); tap-marker opens floating menu with Delete + Kill-toggle (`ShotMenuOverlay` child of BaseCanvas mirrors `InteractiveChrome.toolbarPos` math verbatim). `isKill` render added to ShotDrawer drawFn (red ring + 💀) so kill-toggle has visible in-drawer feedback. § 9 destructure trifecta verified: ref-wrap ✓, `editable:true` intact ✓, per-handler destructure ✓. |
| B10 | LogRow event eyebrow + Rozbieg/Strzały labels (no new § — backlog item) | 2026-05-27 | Shared `LogRow` (3 mounts) gains optional `eventLabel` tri-state prop (`undefined` hide / `string` "Trening · {name}" / `null` "Bez treningu" orphan italic) + 2-col grid label gutter ("Rozbieg" / "Strzały") replacing the implicit line-1/line-2 stack. PlayerStatsPage Samoocena wires the eyebrow via `useTrainings()` (cross-event surface); TodaysLogsList own + TrainingResultsPage omit (already event-scoped). `shotsText` helper untouched. Training-granularity only — `matchupId`/`pointNumber` always null on selfReports today; matchup-level event ref not reachable without schema change. |
| 75 | Canvas Interaction Model — gesture grammar vs action content | 2026-05-24 | Apple HIG split. **BaseCanvas owns the universal gesture grammar** (pinch-zoom / pan / full-screen); feature-children own contextual content (edit menus / read overlays / screen-specific). Three consistency tiers: Universal (every canvas), Edit-family (Match scouting / Tactic / Layout / BunkerEditor — tap-elementu → menu, drag → move, long-press → loupe; same grammar, different menu content), Read-family (heatmaps — zoom/pan only; loupe-off free because read-only has nothing to place); plus screen-specific outliers (half-field/viewportSide on Match, zone/line on Layout, visibility on Ballistics off-limits, point-preview on Match heatmap, source pills on Training, landscape edge tabs on Layout/Analytics). 2026-05-24 InteractiveCanvas regression (tap-elementu + drag dead, zoom/pan/place alive) = grammar-split must be enforced structurally in BaseCanvas, not drift per-screen. **Full-screen generalized** from § 64.10/#11 (ScoutedTeam-specific) to a universal capability — both triggers (auto-on-rotate via `useLandscapeMode` + explicit expand btn in portrait); chrome hides, fit-to-viewport, content-first. **Enhanced DrawingOverlay** (composable per § 64) becomes an iOS-Markup-style annotation tool on **`perfect-freehand`** (MIT, same lib tldraw uses underneath) + own state/storage; **NOT tldraw** ($6k/yr commercial) **NOT Excalidraw** (whiteboard mismatch with custom field canvas). Mode-not-gesture (explicit enter/exit + toolbar); 1-finger draws, 2-fingers stay zoom/pan. Toolbar: colors, 1–2 brush widths, undo, stroke-eraser, clear all. Available on Match scouting / Tactic / ScoutedTeam (coach flow: set tactics / overlay scouted data / draw counter). Persistence is contextual (tactic doc / per-point annotation / scouted doc) — tool identical, target screen-specific. Sequencing: **fix InteractiveCanvas tap/drag regression FIRST**, then code-verify freehand → impl full-screen → impl DrawingOverlay → toolbar mockup. |
| MULTI_TENANT_MIGRATION_PLAN.md | Strategic per-phase plan consolidating rozkminy 1+2+3 outcomes | 2026-05-19 | New file `docs/architecture/MULTI_TENANT_MIGRATION_PLAN.md`. Per-phase scope, sub-brief breakdown, validation gates, rollback procedures, infrastructure triggers, Blaze upgrade gate at Phase 3/4 boundary, cross-phase concerns (Sentry, Firestore costs, migration script patterns, data preservation, documentation discipline). Total multi-tenant Phases 1-4 timeline: 12-20 weeks. Canvas refactor parallel. |
| 63.16 | GlobalEvents Architecture — Option B locked + α/β/γ/δ phases + trust-one-source reconciliation preliminary | 2026-05-19 | Cross-workspace dedup via `/globalEvents/{geid}` registry + optional workspace event linkage. Composite + aggregate aggregation modes. Three options A/B/C analyzed; A (heuristic fingerprint) + C (Cloud Function auto-dedup) rejected. Phase β post-multi-tenant Phase 2, γ post-β volume, δ when UI justified. Reconciliation: trust-one-source authority designation + manual escape hatch. Phase γ formal reconciliation lock deferred. |
| 63.15 | Global Resources Architecture — Leagues + Players + Teams + TeamMemberships schemas | 2026-05-19 | Decisions 1+2+2b+2c from rozkmina #3 packet. Leagues = configurable global resource (super admin write, all read), pre-populate from current LEAGUES/DIVISIONS constants. Players global with `pbliId` dedup. Teams global with `pbliTeamId` dedup, workspace-managed but globally visible. Multi-league multi-team relationships via `/teamMemberships/` junction (one player can have simultaneous memberships in NXL US Semi-Pro + NXL EU Pro + PXL). Migration: Phase 2 hoists all four resources together. |
| 64 | Canvas Architecture — Component Model + Drawing Layer | 2026-05-19 | Option B locked (BaseCanvas + specialized InteractiveCanvas/HeatmapCanvas/AnalyticsCanvas + composable DrawingOverlay). 11 sub-decisions packaged: FieldView deprecation, AnalyticsCanvas extraction, viewportSide resurrection, useLandscapeMode hook, DPR runtime detection, drawZones.js i18n cleanup pre-refactor, hybrid drawing persistence, 6-color P0 palette, single-user MVP. Migration via per-view briefs (Etap 5). First beneficiary: landscape coach view (ScoutedTeamPage heatmap). |
| 63.3 | § 63.3 schema choice RESOLVED as Option α (drop unused `users/{uid}.workspaces` field, single source of truth in `workspace.userRoles[uid]`, switcher uses collectionGroup query) | 2026-05-19 | Phase 0 finding (zero consumers) invalidated original (a)/(b)/(c) framing — all three created duplicate role storage. Option α unlocks Phase 1 schema work without Blaze dependency. Phase 1 implementation work is separate brief. Coupled `users.activeWorkspace` decision deferred to separate small session. |
| 63 (Phase 0 findings) | Phase 0 CC discovery — Canvas + Multi-Tenant § 63 | 2026-05-19 | Canvas audit ❓/🟡 cleared. § 63.X Findings sub-blocks added per subsection. Critical findings: zero consumers of users.workspaces (option a free), Spark→Blaze needed for Phase 5, NewTournamentModal already has 3-type selector, HeatmapCanvas zero gestures. § 63.14 updated: player identity RESOLVED global, teams PRELIMINARY global, globalEvents architecture NEW PARKED with full rozkmina material. |
| 63 | Multi-Tenant Architecture — SaaS foundation | 2026-05-19 | 8 architectural decisions for multi-tenant SaaS pivot. Unified events collection, multi-workspace membership + Super Coach role (extends § 49 foundation, schema sub-option a/b/c deferred to Phase 0), hybrid layout library, phased aggregation, workspace slug in URL, unified wizard, mixed copy strategy, i18next migration. Migration plan ~6 months. Triggered by US PRO team onboarding + monetization of cross-workspace layout insights. |
| 61 | Deaths heatmap v2 | 2026-05-12 | **Shipped (merge `a5bb51e`, deployed; see DEPLOY_LOG 2026-05-12).** Isolated `deathAttribution.js` helper (precision + zone + 1/N split, fractional credits via `formatKills`), scope filter pills (Layout/Tournament/Match/Point — no global) with `ActionSheet` pickers + cascading state, density auto-hide < 5 points, shooter markers on canvas with team-color badges, cross-filter linked highlighting (skull↔shooter via precomputed `linkMap`), status pill, "Pozycja strzelca" 7th table column. § 30 explicitly preserved for global consumers. Animation + unattributed-skull toast deferred per CLAUDE.md smaller-scope rule (documented in § 61.6). |
| 60 | Coach view refinements — pre-NXL | 2026-05-12 | **Shipped (merge `36104cb`, deploy `3a1ffed`).** Heatmap to top of ScoutedTeamPage, Tendencja demoted to additional, Rozbiegi +2 columns (`Zagrań` / `W pkt`), Strzelanie reliability banner, match-level scope filter (`Ostatni mecz` + `Mecz ▾`), ADD MATCH removed, precision drawer 70vw. PLAYER #1 BottomNav deferred (§ 60.9). |
| 37 | Documentation discipline | 2026-04-20 | Where decisions live, CC brief lifecycle, chat-is-not-SoT rule |
| 36 | Adaptive picker thresholds | 2026-04-20 | Breakout < 5, shots < 20, weighted hit=2/miss=1/unknown=0.5 |
| 35 | Player Self-Report UI patterns | 2026-04-20 | Two-tier model, FAB, bootstrap collapse, cycle-tap shots, shared variants |
| 34 | Field Side Representation Standard | 2026-04-18 | `SideTag` canonical component, terminology (dorito/snake/center, NOT disco/zeeker for sides) |
| 33 | User Accounts + Scout Ranking | 2026-04-15 | Email/password auth, scout leaderboard |
| 32 | Training Mode | 2026-04-14 | Squads, drag-drop, matchups reuse MatchPage |
| 31 | Bottom Tab Navigation — App Shell | 2026-04-14 | Scout/Coach/More, tournament picker |
| 30 | Metric Formulas | 2026-04-14 | Coaching stats, performance, kill attribution v1 (still authoritative for global consumers; Brief B's helper is isolated) |
| 27 | **Apple HIG Compliance — MANDATORY** | 2026-04 | Touch 44px, elevation, amber = interactive only, anti-patterns |

Earlier sections (§ 1–§ 26) cover foundational decisions. Full list: `grep '^## [0-9]' docs/DESIGN_DECISIONS.md`.

---

## 🔄 Update protocol

**At the end of every Opus chat** (or Claude Code session that changes direction):

1. Patch **this file** with changes in:
   - "Currently in flight" (new branch? complete?)
   - "Recently shipped" (add row to table if something deployed)
   - "Next on deck" (promotions / demotions / new tasks)
   - "Awaiting decision" (add new blockers / mark resolved)
   - "Recent design decisions" (add row if §N+1 appended to DESIGN_DECISIONS)

2. Bump **Last updated** date + author + **Main HEAD at last update**.

3. Commit: `chore(docs): update HANDOVER.md — <summary of changes>`

4. If Opus chat generated a new patch for `DESIGN_DECISIONS.md` or `PROJECT_GUIDELINES.md`, ship that patch in the same or adjacent commit, before the chat ends (per § 37.2 chat-is-not-SoT rule).

**Rule of thumb:** this file should be the first thing a fresh Opus chat reads to get situational awareness. If it's stale, fix it before asking Opus anything strategic.
